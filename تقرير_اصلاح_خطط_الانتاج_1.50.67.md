# تقرير إصلاح خطط الإنتاج MPS — الإصدار 1.50.67
## تاريخ: 2026-09-16 (Asia/Aden)

### 1) ملخص الأخطاء المبلغة (Screenshot)

**الخطأ الأول — تحقق متأخر عند الحفظ:**
```
خطة الإنتاج: كمية الكيلو لا تطابق عدد الكراتين ووزن الكرتون للصنف سكري فاخر اكجم
المدخل 12000 كجم ← 6000 كرتون والمحسوب من وزن الكرتون (0,5) كجم: 3000 كجم
```
- النظام يسمح بإعداد الخطة كاملة (اختيار أصناف، كراتين، تواريخ) ثم يرفض عند الحفظ فقط.
- لم يكن هناك تنبيه أحمر فوري أثناء اختيار الأصناف أو تغيير الكراتين.
- الوزن في الشاشة ReadOnly لكنه محسوب من وزن خاطئ.

**الخطأ الثاني — طريقة السحب:**
- الاستلام في النظام بالسلة والكرتون (ReceiptUnit = سلة/كرتون) لكن السحب في نافذة اختيار الأصناف كان يعرض كجم فقط بدون خيار.
- المستخدم يحتاج ComboBox لاختيار وحدة السحب (سلة/كرتون/كجم) مع حساب تلقائي SourceQtyInUnit ↔ SourceQtyKg عبر UnitWeightKg.

### 2) التشخيص الفني

#### أ) سبب mismatch 0.5 كجم vs 2 كجم
- `Product.CartonWeightKg` للصنف "سكري فاخر اكجم" = 2 كجم (6000 كرتون × 2 = 12000 كجم — المدخل الصحيح)
- `PackagingType.UnitWeightKg` لعبوة عامة مرتبطة = 0.5 كجم (6000 × 0.5 = 3000 كجم — المحسوب الخاطئ)
- `UnitsPolicy.CartonWeight()` القديم كان يأخذ **العبوة أولاً** ثم بطاقة الصنف:
  ```csharp
  if (packagingTypeId != null) { packW = ...; if (packW>0) return packW; }
  if (prod.CartonWeightKg>0) return prod.CartonWeightKg;
  ```
  → فيُحسب 3000 بينما المستخدم أدخل 12000 → يرمي `CARTON_KG_MISMATCH` فقط في `SavePlan/UpdatePlan`.

#### ب) فجوة التحقق الفوري
- `PlanRowUi.CartonsText` يعيد حساب `QtyKg = cartons * CartonWeight` فوراً لكن لا يتحقق إذا كان `QtyKg` مدخل يدوياً يخالف الحساب.
- `LotEditorRow.Recalc()` يحسب `ComputedKg = cartons * packW` من بطاقة الصنف، لكن `PlanningView.xaml.cs InsertEditorRows` كان ينقل `SourceQtyKg` فقط بدون `SourceMode`.
- `PlanningCapacityEvaluator.Evaluate()` كان يتحقق من الطاقة فقط، لا من تطابق كجم/كرتون → الخطأ لا يظهر إلا عند الحفظ.

#### ج) فجوة وحدة السحب
- `LotsEditorWindow` يبني `SourceModes` من `Ctx.ReceiptUnit` (سلة/كرتون/كجم) ويعرض عمود "طريقة السحب" + `AvailableDisplay` بالوحدتين، لكن `PlanningView` Grid الرئيسي يعرض فقط `SourceQtyKg` كـ "المتاح كجم" بدون ComboBox.
- `PlanningService.ResolveSourceDraw` يدعم `SourceQtyInUnit + SourceUnitWeightKg` لكن UI لا يسمح باختياره.

### 3) الحلول المطبقة (كود فعلي — ليس توصيات)

#### 3.1) `UnitsPolicy.cs` — أولوية وزن الكرتون
- **قبل:** العبوة أولاً → بطاقة الصنف → قوالب
- **بعد (1.50.67):** بطاقة الصنف أولاً (المصدر الموثوق) → قوالب → العبوة fallback فقط:
  ```csharp
  var prod = db.Products.AsNoTracking().FirstOrDefault(p => p.Id == productId);
  if (prod != null) {
    if (prod.CartonWeightKg > 0) return prod.CartonWeightKg;
    if (prod.MoldsCount>0 && prod.MoldWeightKg>0) return prod.MoldsCount * prod.MoldWeightKg;
  }
  if (packagingTypeId != null) { packW = ...; if (packW>0) return packW; }
  ```
- إضافة دالة `CheckCartonKgImmediate(double weight, int cartons, double qtyKg, string name)` للواجهة بدون DB.

#### 3.2) `PlanningRows.cs` — تحقق فوري + وحدة سحب selectable
- `LotEditorRow.Recalc()`:
  - يستخدم وزن بطاقة الصنف أولاً ثم العبوة (يمنع mismatch 0.5 vs 2)
  - تحذير فوري إذا اختلف وزن البطاقة عن وزن العبوة المحددة بأكثر من 5%
  - `SourceUnit` و `SourceQtyInUnit` يُحسبان من `SourceMode` المختار (سلة/كرتون/كجم)، لا من ReceiptUnit فقط
  - `SourceModes` يبني من `ReceiptUnitText` + "كجم" ويعرض `AvailableDisplay` = "100 سلة / 5000 كجم"
- `PlanRowUi`:
  - `CartonsText` setter + `QtyKg` setter + `Cartons` setter كلها تستدعي `ValidateCartonKgImmediate()`
  - `ValidateCartonKgImmediate()` تتحقق: إذا `|QtyKg - cartons*CartonWeight| > max(1, QtyKg*2%)` → `QuantityError` أحمر فوري مع Tooltip
  - إضافة `SourceModes`, `SourceMode`, `AvailableDisplay`, `SourceDisplay`, `RecalcSourceQtyFromMode()`
  - `SourceDisplay` = "100 سلة (5000 كجم)" و `AvailableDisplay` يتغير حسب `SourceMode`

#### 3.3) `PlanningView.xaml` — أعمدة جديدة
- عمود "طريقة السحب" ComboBox مرتبط بـ `SourceModes` و `SourceMode`
- عمود "المتاح" يعرض `AvailableDisplay` بالوحدتين بدل "المتاح كجم" فقط
- عمود "الوزن كجم ⚖️" TemplateColumn يعرض أحمر + Tooltip إذا `QuantityError` يحتوي "لا تطابق"
- عمود "المسحوب" يعرض `SourceDisplay` (مثلاً 100 سلة (5000 كجم))

#### 3.4) `PlanningView.xaml.cs`
- `InsertEditorRows()`: ينقل `SourceUnit`, `SourceQtyInUnit`, `SourceUnitWeightKg`, `SourceQtyKg`, `SourceModes`, `SourceMode` من `LotEditorRow` إلى `PlanRowUi`
- `OpenPlan()`: يبني `SourceModes` من `SourceUnit` المحفوظ + "كجم" ويستعيد `SourceMode`
- `RowUi_Changed()`: يراقب `SourceMode`, `SourceQtyKg`, `SourceDisplay` لتحديث الطاقة والعدادات فوراً

#### 3.5) `PlanningCapacityEvaluator.cs` — تحقق مبكر قبل الحفظ
- في بداية حلقة `for (index)` لكل بند: إذا `cartons>0 && qtyKg>0` و `CartonWeight>0` و `|qtyKg - cartons*weight| > tolerance` → `row.Error` فوري برسالة حمراء تمنع الحفظ وتظهر في شريط الطاقة
- كان التحقق سابقاً فقط في `SavePlan/UpdatePlan` عبر `EnsureCartonKgConsistency` → الآن في `EvaluateDraft` أيضاً

### 4) إصلاح بيانات الماستر (سكري فاخر اكجم)

**المشكلة:** وزن الكرتون معرف 0.5 كجم في `PackagingTypes` بينما بطاقة الصنف 2 كجم.

**الحل المقترح SQL (تشغيل على DB العميل):**
```sql
-- تصحيح وزن كرتون سكري فاخر اكجم
UPDATE Products SET CartonWeightKg = 2.0 WHERE ProductNameAr LIKE '%سكري فاخر%اكجم%';
-- تصحيح العبوة العامة 0.5 إذا كانت مخصصة لنفس الصنف
UPDATE PackagingTypes SET UnitWeightKg = 2.0 WHERE PackageNameAr LIKE '%سكري فاخر%' AND UnitWeightKg = 0.5;
-- فحص التناقضات: عبوات وزنها يختلف عن بطاقة الصنف بأكثر من 10%
SELECT p.ProductNameAr, p.CartonWeightKg AS ProdW, pt.PackageNameAr, pt.UnitWeightKg AS PackW
FROM Products p JOIN PackagingTypes pt ON pt.Id = p.DefaultPackagingTypeId
WHERE ABS(p.CartonWeightKg - pt.UnitWeightKg) > p.CartonWeightKg * 0.1 AND p.CartonWeightKg > 0;
```

### 5) فحص GitHub — هل يوجد إصلاحات استشارية؟

```bash
git fetch origin main
git ls-remote origin
```
- `origin/main` = 5695815 يحتوي فقط `DateERP_1.50.37_FULL_SOURCE.zip` وملفات Update_Mini محذوفة — **لا يوجد سورس كود**
- `arena/01a091dd-offtt` = f3831b7 هو الفرع الوحيد الذي يحتوي سورس كامل
- لا توجد فروع استشارية، لا يوجد PR جديد، لا توجد تحديثات على GitHub تخص خطط الإنتاج
- **النتيجة:** لا توجد إصلاحات استشارية — الإصلاحات الحالية هي الوحيدة

### 6) الملفات المعدلة

- `src/DatesErp.Application/Services/UnitsPolicy.cs` — أولوية وزن + CheckImmediate
- `src/DatesErp.Desktop/Mvvm/PlanningRows.cs` — تحقق فوري + وحدة سحب selectable (LotEditorRow + PlanRowUi)
- `src/DatesErp.Desktop/Views/Screens/PlanningView.xaml` — أعمدة طريقة سحب + متاح بالوحدتين + وزن أحمر
- `src/DatesErp.Desktop/Views/Screens/PlanningView.xaml.cs` — نقل SourceMode + مراقبة تغييراته
- `src/DatesErp.Application/Services/PlanningCapacityEvaluator.cs` — تحقق مبكر كجم/كرتون في EvaluateDraft
- `src/DatesErp.Desktop/DatesErp.Desktop.csproj` — Version 1.50.66 → 1.50.67

### 7) كيفية الاختبار (يدوي — Windows)

1. افتح شاشة خطط الإنتاج MPS
2. اختر دفعات عميل (F4) — نافذة اختيار الأصناف تظهر `طريقة السحب` ComboBox: سلة/كرتون/كجم + `المتاح` مثل "100 سلة / 5000 كجم"
3. أدخل 6000 كرتون لصنف سكري فاخر اكجم — الوزن يحسب تلقائياً 12000 كجم (2 كجم/كرتون) ولا يظهر خطأ
4. غيّر الكراتين إلى 6000 وعدّل الوزن يدوياً إلى 3000 → يظهر فوراً رسالة حمراء "كمية الكيلو لا تطابق..." مع Tooltip، والحفظ يمنع (CapacityBar أحمر)
5. في الجدول الرئيسي، غيّر طريقة السحب من سلة إلى كجم → `المسحوب` يتغير من "100 سلة (5000 كجم)" إلى "5000 كجم" و `SourceQtyInUnit` يعاد حسابه
6. احفظ الخطة — يجب أن تحفظ بدون خطأ CARTON_KG_MISMATCH

### 8) ملاحظات النشر

- البناء في sandbox غير ممكن (dotnet SDK غير متوفر، curl محجوب TLS) — تم التحقق منطقياً فقط
- النشر: انسخ الملفات المبنية فوق `D:\DateERP_Publish` أو شغل `INSTALL-1.50.67.bat` (إن وجد)
- لا تمسح DB — الإصلاح كود فقط + تصحيح وزن اختياري عبر SQL أعلاه
- الإصدار الجديد: 1.50.67 — يحل مشكلة التحقق المتأخر ويضيف وحدة سحب selectable

### 9) إصلاح إضافي — زر الحفظ يتشفر بعد تعديل الأصناف دون حفظ (1.50.67 hotfix)

**المبلغ:** "عملت خطة دون ان احفظ وعدلت الاصناف لكن زر الحفظ يتشفر ولا استطيع الحفظ"

**التشخيص:**
- `PlanningView` يضيف صف فارغ placeholder دائماً عبر `AddEmptyRow()` — `CartonsText="0"`, `LotId=null`, `ProductId=0`
- `UpdateCapacityBar()` القديم كان يفحص **كل** `_rows` بما فيها الفارغ:
  ```csharp
  if (_rows.Any(r => !int.TryParse(r.CartonsText, out var n) || n <= 0)) inputError = "صحّح الكميات..."
  _capacityValid = result.IsValid && inputError==null && _rows.Count>0;
  ```
  → الصف الفارغ 0 كرتون يجعل `inputError` دائماً موجود → `_capacityValid=false` → `SaveActionBtn.IsEnabled=false` دائماً
- أيضاً `EvaluateDraft(_rows.Select(CapacityItem))` كان يقيّم حتى الصف الفارغ `ProductId=0` → `Rate=0` → `result.IsValid=false`

**الحل في `PlanningView.Capacity.cs`:**
```csharp
var validRows = _rows.Where(r => r.LotId != null || r.ProductId != 0).ToList();
var result = EvaluateDraft(validRows.Select(CapacityItem).ToList());
string inputError = validRows.FirstOrDefault(r => r.QuantityError != null)?.QuantityError;
if (validRows.Any(r => !int.TryParse(r.CartonsText, out var n) || n <= 0)) ...
_capacityValid = result.IsValid && inputError==null && validRows.Count>0;
for (int i=0; i<validRows.Count && i<result.Rows.Count; i++) validRows[i].Capacity = result.Rows[i];
foreach (var empty in _rows.Where(r => r.LotId==null && r.ProductId==0)) empty.Capacity=null;
```
- `CheckRowQuantity()` أيضاً يستثني الصفوف الفارغة

**النتيجة:** زر الحفظ يبقى مفعلاً عند وجود بند واحد صحيح حتى مع وجود صف فارغ placeholder — يطابق سلوك `Save_Click` الذي يفلتر `validRows` أصلاً.

### 10) الخلاصة

تم إصلاح 3 أسباب جذرية + hotfix زر الحفظ:
1. **وزن الكرتون:** بطاقة الصنف المرجع الأول → يمنع mismatch 0.5 vs 2 كجم
2. **التحقق المتأخر:** فوري في `PlanRowUi`, `LotEditorRow`, `PlanningCapacityEvaluator`
3. **طريقة السحب:** ComboBox سلة/كرتون/كجم + متاح بالوحدتين
4. **زر الحفظ يتشفر:** كان بسبب فحص الصف الفارغ placeholder — الآن يفحص فقط البنود الفعلية

لا توجد تحديثات استشارية على GitHub — هذا الإصلاح هو المرجع الحالي.
