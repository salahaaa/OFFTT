# تقرير الإصدار 1.50.63 — نظام الأصناف المساعدة للإنتاج (12 بند)

**التاريخ:** 2026-09-14  
**الفرع:** arena/01a091dd-offtt  
**الحالة:** 95% مكتمل — الكود والواجهات جاهزة، البناء عبر BAT على جهاز المستخدم

---

## 1. ملخص التنفيذ

تم تنفيذ نظام الأصناف المساعدة المتكامل حسب الـ 12 بند المطلوبة، بفصل كامل عن الخام:

| البند | الوصف | الحالة |
|-------|-------|--------|
| 1 | شاشة تهيئة الأصناف المساعدة مرتبطة ببطاقة الصنف (مجموعة=مساعدة، وحدة، وزن وحدة، طريقة صرف ByUnit/ByKilo/PerProduction، NeedsIssueOnOrder، فعال/غير فعال، منع تكرار) | ✅ |
| 2 | ربط بالصنف التام — شاشة مكونات/احتياجات الصنف التام (سكري 8كجم → كرتون 8كجم 1/كرتون، ملصق 1، كيس 1، مادة X 0.020 كجم/كرتون)، الكمية مرتبطة بمواصفة التام لا الخام | ✅ |
| 3 | حساب المطلوب عند أمر الإنتاج: كمية الأمر × معامل (3500 كرتون → 3500 ملصق، 3500 كيس، 70 كجم مادة X) وعرض قبل الصرف | ✅ |
| 4 | شاشة صرف: جدول مواد مطلوبة (صنف مساعد، المطلوب، المصروف، المتبقي، حالة المخزون) + إصدار صرف | ✅ |
| 5 | تحقق مخزون قبل اعتماد الصرف: مقارنة مطلوب vs متاح، رسالة "غير كافٍ - المطلوب/المتاح/العجز"، منع الصرف الكامل إلا بصلاحية صرف جزئي | ✅ |
| 6 | صرف جزئي: دعم صرفات متعددة حتى اكتمال (مطلوب/مصروف/متبقي لكل أمر) | ✅ |
| 7 | فصل عن الخام: الخام من شحنة/دفعة بتتبع، المساعدة مخزون مستقل من مستودع مساعدة WAUX مرتبط بأمر إنتاج بلا شحنة | ✅ |
| 8 | تتبع كل صرف: رقم أمر، صنف مساعد، مطلوب، مصروف، وحدة، تاريخ/وقت، مستخدم، رقم مستند صرف، مستودع مصدر، رصيد قبل/بعد، قابلية استعلام "كم استهلك OP-001" | ✅ |
| 9 | الصرف مرتبط بأمر الإنتاج لا الخطة فقط، تسلسل: خطة→أمر→احتياجات→صرف→تنفيذ→إغلاق | ✅ |
| 10 | عند تعديل كمية الأمر: إعادة حساب، لا تعديل حركات سابقة، المتبقي = مطلوب جديد - مصروف سابق | ✅ |
| 11 | عند إلغاء/تقليل: لا حذف صرف، إنشاء إرجاع من الإنتاج للمخزن للكميات غير المستخدمة | ✅ |
| 12 | تقارير: استهلاك حسب أمر (OP-001 3500 كرتون → ملصقات مطلوب/مصروف...) واستهلاك صنف مساعد خلال فترة (إجمالي سبتمبر 25k) | ✅ |

**قاعدة:** بطاقة صنف تحدد مساعد، مواصفة تام تحدد كمية لكل وحدة، أمر إنتاج يحدد كمية فعلية، نظام يحسب ويصرف وينقص مخزون بتتبع. UI بسيط: المطلوب|المصروف|المتبقي|المتاح|صرف.

---

## 2. الملفات الجديدة والمعدلة

### كيانات جديدة (Core)
- `src/DatesErp.Core/Domain/Entities/AuxiliaryManagement.cs`:
  - `AuxiliaryProductConfig` (تهيئة مساعد: Group/Unit/Weight/Method/NeedsIssue/IsActive)
  - `ProductAuxiliaryRequirement` (BOM: Finished→Auxiliary، QtyPerCarton decimal، CalculationMethod PerCarton/PerKg/PerProduction)
  - `AuxiliaryIssueTransaction` (تتبع صرف: Order/Product/Required/Issued/Unit/Date/User/Doc/Warehouse/BalanceBefore/After decimal)
  - `AuxiliaryReturnTransaction` (إرجاع فائض)
- `src/DatesErp.Infrastructure/Persistence/DatesErpDbContext.cs`: DbSets للكيانات الجديدة

### خدمة شاملة (Application)
- `src/DatesErp.Application/Services/AuxiliaryManagementService.cs` (696 سطر):
  - `GetAuxiliarySetups()` / `SaveAuxiliarySetup()` — تهيئة مع منع تكرار
  - `GetRequirementsForFinished()` / `SaveRequirement()` / `DeleteRequirement()` — BOM مع مثال سكري 8كجم
  - `CalculateNeedsForOrder()` — حساب مطلوب: تجميع كراتين×QtyPerCarton، دعم PerKg/PerProduction، تفاصيل حسابية
  - `IssueAuxiliary()` — صرف مع تحقق مخزون WAUX برسالة مفصلة (المطلوب/المتاح/العجز)، صرف جزئي، تتبع BalanceBefore/After
  - `IssueAllRemaining()` — صرف جماعي للمتبقي
  - `GetIssueHistory()` / `GetConsumptionByOrderReport()` / `GetPeriodConsumption()` — تقارير
  - `RecalculateOnOrderChange()` — عند تعديل كمية الأمر: لا تعديل حركات سابقة، المتبقي = جديد - مصروف
  - `ReturnAuxiliary()` — إرجاع فائض بسبب إلغاء/تقليل مع سبب إجباري

### واجهات WPF (Desktop) — 3 شاشات جديدة
- `src/DatesErp.Desktop/Views/Screens/AuxiliarySetupView.xaml/.xaml.cs` (MRPAUX1001):
  - ComboBox: ProductBox (أصناف مساعدة)، GroupBox (مساعدة/تغليف/مواد)، UnitBox (وحدة/كجم/كرتون...)، MethodBox (بالوحدة/بالكيلو/حسب الإنتاج)
  - حقول: وزن وحدة، NeedsIssueOnOrder، IsActive، Notes
  - Grid: الأصناف المهيأة مع طريقة الصرف وحالة فعال
  - أزرار: حفظ/تعديل/تحديث، AttachChrome

- `src/DatesErp.Desktop/Views/Screens/ProductBOMView.xaml/.xaml.cs` (MRPAUX1002):
  - ComboBox: FinishedBox (أصناف تامة)، AuxBox (مساعدة)، UnitBox، CalcBox (لكل كرتون/لكل كجم/حسب الإنتاج)
  - مثال: سكري 8كجم → كرتون 8كجم 1/كرتون، ملصق 1، كيس 1، مادة X 0.020 كجم/كرتون
  - Grid: احتياجات الصنف التام مع الكمية لكل وحدة
  - أزرار: إضافة/حذف/تحديث

- `src/DatesErp.Desktop/Views/Screens/ProductionAuxiliaryView.xaml/.xaml.cs` (MRPAUX1003):
  - OrderBox: اختيار أمر إنتاج، OrderInfo: تفاصيل الأمر
  - NeedsGrid: صنف مساعد | المطلوب | المصروف | المتبقي | المتاح | حالة مخزون (متوفر/جزئي/غير متوفر) | الوحدة | التفاصيل | صرف
  - RowStyle: غير متوفر → InvalidRowBrush
  - HistoryGrid: تتبع كامل (رقم أمر/صنف/مطلوب/مصروف/وحدة/تاريخ/مستخدم/مستند/مستودع/رصيد قبل/بعد)
  - أزرار: صرف صنف واحد (IssueOne) مع InputDialog، صرف الكل (IssueAll)، إرجاع فائض (Return)، تفاصيل، تحديث

### تكامل مع أوامر الإنتاج
- `src/DatesErp.Desktop/Services/AppContainer.cs`: تسجيل `services.AddScoped<AuxiliaryManagementService>()`
- `src/DatesErp.Desktop/Screens/ScreenCatalog.cs`: إضافة 3 شاشات للإنتاج:
  - `aux-setup` (🧩 تهيئة مساعدة، MRPAUX1001)
  - `product-bom` (📋 مكونات إنتاج، MRPAUX1002)
  - `aux-issue` (🧪 صرف مساعد، MRPAUX1003)
- `src/DatesErp.Desktop/Views/Screens/ScreenFactory.cs`: مصانع WrapPlain للشاشات الثلاث
- `src/DatesErp.Application/Services/ProductionOrderService.cs`:
  - توسيع `CalculateMaterials()` لدعم النظامين:
    - القديم: `ConsumptionFormula` → aggOld
    - الجديد: `ProductAuxiliaryRequirements` مع تحقق `AuxiliaryProductConfig.NeedsIssueOnOrder/IsActive` وطرق `PerCarton/PerKg/PerProduction`
    - مثال: 3500 كرتون سكري 8كجم → 3500 كرتون 8كجم، 3500 ملصق، 3500 كيس، 70 كجم مادة X
- `src/DatesErp.Desktop/Views/Screens/OrdersWindows.cs` (OrderDocumentPanel):
  - استبدال أعمدة materialsGrid إلى: صنف مساعد/المطلوب/المصروف/المتبقي/المتاح/حالة المخزون/الوحدة/التفاصيل
  - مصدر البيانات عبر `AuxiliaryManagementService.CalculateNeedsForOrder` مع fallback للنظام القديم

### تقارير
- `src/DatesErp.Application/Services/ReportService.cs`:
  - `aux_consumption_by_order` — استهلاك حسب أمر (OP-001 3500 كرتون → ملصقات مطلوب/مصروف/متبقي/تفاصيل)
  - `aux_period_consumption` — استهلاك صنف مساعد خلال فترة (إجمالي سبتمبر 25k ملصق)
  - `aux_setup` — تهيئة الأصناف المساعدة
  - `aux_bom` — مكونات الإنتاج BOM

### ثيم
- `src/DatesErp.Desktop/Themes/DateErpTheme.xaml`: إضافة ألوان مساعدة:
  - `AuxWarningLightColor` (#FEF9C7)، `DarkTitleColor` (#123544)، `MutedTextColor` (#526474)، `LightMutedColor` (#6B7280)
  - Brushes مقابلة
- `tools/ci/screen_hex_baseline.json`: إضافة 3 شاشات جديدة بـ 0 hex (بعد إصلاح الألوان المضمّنة)

---

## 3. منطق العمل (Workflow)

```
1. بطاقة صنف → تهيئة مساعد (AuxiliarySetupView)
   - صنف موجود (Auxiliary/Pack/Group 004)
   - مجموعة=مساعدة، وحدة، وزن وحدة، طريقة صرف ByUnit/ByKilo/PerProduction
   - هل يحتاج صرف عند إصدار أمر؟ NeedsIssueOnOrder
   - فعال/غير فعال، منع تكرار

2. مواصفة تام → BOM (ProductBOMView)
   - سكري 8كجم → كرتون 8كجم 1/كرتون، ملصق 1، كيس 1، مادة X 0.020 كجم/كرتون
   - الكمية مرتبطة بمواصفة التام لا الخام
   - طريقة حساب: PerCarton/PerKg/PerProduction

3. خطة → أمر إنتاج (ProductionOrderService.CalculateMaterials)
   - كمية الأمر 3500 كرتون → 3500 ملصق، 3500 كيس، 70 كجم مادة X
   - حفظ في ProductionOrderMaterials مع AuxiliaryProductId

4. أمر → احتياجات → صرف (ProductionAuxiliaryView + OrdersWindows)
   - جدول: المطلوب|المصروف|المتبقي|المتاح|حالة مخزون|صرف
   - تحقق مخزون WAUX قبل الصرف: رسالة "غير كافٍ - المطلوب/المتاح/العجز"
   - صرف جزئي: عدة صرفات حتى اكتمال، كل صرف يسجل BalanceBefore/After + User/Doc/Warehouse

5. تنفيذ → إغلاق
   - الخام من شحنة/دفعة بتتبع، المساعدة من WAUX بلا شحنة (فصل كامل)
   - عند تعديل كمية الأمر: إعادة حساب، لا تعديل حركات سابقة، المتبقي = جديد - مصروف
   - عند إلغاء/تقليل: لا حذف صرف، إرجاع فائض بسبب إلغاء/تقليل مع سبب

6. تقارير
   - استهلاك حسب أمر: OP-001 3500 كرتون → ملصقات مطلوب/مصروف/متبقي/تفاصيل
   - استهلاك صنف خلال فترة: سبتمبر 25k ملصق
   - تهيئة + BOM
```

---

## 4. اختبارات التحقق

```bash
python3 tools/ci/structural_checks.py
# Legacy double members: 108; no new identities allowed
# Inline hex baseline: monitored for 29 screens
# Structural checks: PASS ✅
```

- لا يوجد `double` جديد في الكيانات (تحويل إلى `decimal` ليتوافق مع CI)
- لا يوجد ألوان مضمّنة hex في الشاشات الجديدة (0 hex لكل شاشة، موارد الثيم فقط)
- DI مسجل، Catalog/Factory محدثة، ReportService يضم 4 تقارير جديدة

---

## 5. تعليمات التشغيل للمستخدم

### البناء (على جهازك — لا يوجد dotnet في sandbox)
```bat
cd /d D:\DateERP_1.50.43_FULL_SOURCE
build.bat
```
- سيبني الحل وينسخ الملفات إلى مجلد النشر
- تأكد من `WAUX` موجود في المخازن (كود المخزن WAUX)

### الاستخدام
1. **تهيئة مساعد:** الإنتاج → 🧩 تهيئة الأصناف المساعدة → اختر صنف (كرتون/ملصق/كيس/مادة) → حدد وحدة/وزن/طريقة صرف → حفظ
2. **BOM:** الإنتاج → 📋 مكونات الإنتاج → اختر صنف تام (سكري 8كجم) → أضف مساعدات (كرتون 1، ملصق 1، كيس 1، مادة X 0.020 كجم) → حفظ
3. **أمر إنتاج:** الخطة → إصدار أمر 3500 كرتون → سيحسب تلقائياً المطلوب مساعد
4. **صرف:** الإنتاج → 🧪 صرف الأصناف المساعدة → اختر أمر → راجع المطلوب|المصروف|المتبقي|المتاح|حالة المخزون → صرف واحد أو صرف الكل
5. **تتبع:** في نفس الشاشة → جدول History يظهر كل صرف: رقم أمر/صنف/مطلوب/مصروف/وحدة/تاريخ/مستخدم/مستند/مستودع/رصيد قبل/بعد
6. **تقارير:** التقارير → المواد → استهلاك حسب أمر / استهلاك خلال فترة / تهيئة / BOM

### حالات خاصة
- **تحقق مخزون:** إذا المتاح < المطلوب → رسالة "غير كافٍ للصرف — المطلوب: X — المتاح: Y — العجز: Z"
- **صرف جزئي:** اصرف 1000 ثم 1000 ثم 1500 — النظام يتتبع كل صرفة
- **تعديل كمية الأمر:** بعد تعديل من 3500 إلى 4000، المتبقي = 4000 - مصروف سابق (لا يعدل حركات سابقة)
- **إلغاء/تقليل:** استخدم زر إرجاع → أدخل كمية + سبب → ينشئ حركة إرجاع WAUX

---

## 6. المتبقي (5%)

- [ ] بناء الحل عبر BAT على جهاز المستخدم واختبار فعلي مع بيانات حقيقية
- [ ] إنشاء ZIPs للإصدار 1.50.63 (Full + Update) مع تسمية جديدة
- [ ] توثيق فيديو/صور للشاشات الثلاث (اختياري)
- [ ] مراجعة صلاحيات `materials:Post` و `materials:OverIssue` للمستخدمين

---

## 7. ملاحظات تقنية

- **CI Guard:** تحويل كل `double` جديد في الكيانات إلى `decimal` — قاعدة المشروع تمنع double جديد
- **Theme Guard:** إزالة كل hex مضمّن من XAML الجديدة واستخدام موارد الثيم فقط + تحديث baseline
- **Stock:** المساعد من WAUX (ProductId) بلا Lot/Shipment، الخام من WRM مع Lot/Shipment — فصل كامل
- **Balance:** يستخدم QtyKg للكجم و PackageCount للوحدات — تبسيط يناسب الواقع
- **DocumentNumber:** حركة مساعدة تحمل `#AUX-{ProductId}-{Timestamp}` و `#RET-...` للإرجاع

---

**جاهز للدفع والبناء.**

---

## تحديث 1.50.64 — ربط كراتين العملاء بالماركات الخاصة

**طلب المستخدم:** ربط الكراتين بالعميل — صنف كراتين فارغ السلطان ٨كجم عندما يكون هناك انتاج للعميل سلطان يقوم بخصم كمية الكراتين اليا من صنف كراتين السلطان. كل تاجر او عميل له ماركته الخاصة بالكراتين.

### ما تم تنفيذه:

1. **توسيع كيان `AuxCustomerSpec`:**
   - إضافة `AuxiliaryProductId` (كرتون العميل الماركة) و `GenericAuxiliaryProductId` (الكرتون العام الذي يُستبدل)
   - يدعم النظامين القديم (MaterialId) والجديد (ProductId)

2. **منطق الاستبدال التلقائي:**
   - `ProductionOrderService.ResolveAuxProductForCustomer()` — عند حساب احتياجات أمر إنتاج، إذا كان للعميل ماركة خاصة بالكرتون، يستبدل العام بالخاص
   - `AuxiliaryManagementService.ResolveAuxProductForCustomerInternal()` — نفس المنطق في شاشة احتياجات الأمر وصرفه
   - الأولوية: ProductId محدد > عام، PackagingTypeId محدد > عام، Priority أعلى أولاً
   - مثال: أمر للسلطان 3500 كرتون سكري 8كجم → كان يحتاج 3500 كرتون عام 8كجم → يستبدل تلقائياً بـ 3500 كرتون السلطان 8كجم ويخصم من WAUX

3. **شاشة جديدة `CustomerCartonMappingView` (MRPAUX1004):**
   - المسار: الإنتاج → 🏷️ كراتين العملاء (ماركات)
   - حقول: العميل (التاجر)، الصنف التام (اختياري — فارغ = كل الأصناف)، العبوة (اختياري)، الكرتون العام (يُستبدل)، كرتون العميل (ماركة)، اسم الماركة، الأولوية
   - Grid: عرض الروابط الحالية مع إمكانية إيقاف
   - حفظ عبر `AuxiliaryManagementService.SaveCustomerCartonMapping()` — لا SaveChanges مباشر في UI (يلتزم بـ CI guard)

4. **تكامل:**
   - `ScreenCatalog`: إضافة `customer-cartons` (🏷️ كراتين العملاء، MRPAUX1004)
   - `ScreenFactory`: إضافة مصنع للشاشة
   - `AppContainer`: الخدمة مسجلة مسبقاً
   - `screen_hex_baseline.json`: إضافة الشاشة الجديدة بـ 0 hex

### مثال عملي:

1. عرّف في بطاقة الأصناف:
   - صنف مساعد: `كراتين فارغ 8كجم عام` (ProductCode CART-8-GEN)
   - صنف مساعد: `كراتين السلطان 8كجم` (CART-8-SULTAN)
   - صنف مساعد: `كراتين العمري 8كجم` (CART-8-OMARI)

2. في شاشة BOM: سكري 8كجم → كرتون عام 8كجم 1/كرتون

3. في شاشة كراتين العملاء:
   - العميل=السلطان، الكرتون العام=فارغ عام 8كجم، كرتون العميل=السلطان 8كجم، الماركة=السلطان، أولوية=10 → حفظ
   - العميل=العمري، الكرتون العام=فارغ عام، كرتون العميل=العمري 8كجم، الماركة=العمري → حفظ

4. عند إصدار أمر إنتاج للسلطان 3500 كرتون سكري 8كجم:
   - النظام يحسب: 3500 × 1 = 3500 كرتون عام
   - يكتشف ربط السلطان → يستبدل بـ 3500 كرتون السلطان
   - يخصم من مخزون WAUX لصنف كراتين السلطان 8كجم (رصيد قبل/بعد مسجل)
   - في شاشة الصرف يظهر: `كراتين السلطان 8كجم` المطلوب 3500، التفاصيل: `(عام: كراتين فارغ 8كجم عام → ماركة عميل: كراتين السلطان 8كجم)`

### اختبار:

- structural_checks: PASS
- لا SaveChanges مباشر في UI
- يدعم PerCarton/PerKg/PerProduction + ماركة العميل
