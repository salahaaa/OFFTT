# تقرير فحص دورة الإنتاج الكاملة — أوامر الإنتاج → التنفيذ → الجودة → التام → التسليم → إقفال الخطة
**التاريخ:** 2026-09-11 · **المنهج:** قراءة تتبعيّة كاملة لخدمات الدورة في الكود المصدري (فحص ساكن معمّق — بلا تنفيذ)
**النطاق:** `ProductionOrderService`, `ExecutionService`+`QualityService`, `FinishedGoodsService`, `ProductionDeliveryService`, `CustomerDeliveryService`, `PlanClosureService`, `QualityGate`, `PlanSync`, `ServiceBase.ConsumeLot/PostStockMovement`

---

## 1) خريطة الدورة كما هي مُنفّذة فعلياً في الكود

```
الخطة المعتمدة ──إصدار──▶ أمر إنتاج (مسودة)
                             │ اعتماد (production/Approve)
                             ▼
              ┌──── معتمد/مجدول ────────────────────────────────┐
              │ • صرف المواد المساعدة من WAUX (فعلي)            │
              │ • الخام: لا يُخصم هنا (قاعدة توازن الإنتاج)     │
              ▼                                                  │
         بدء التنفيذ ──▶ قيد التنفيذ ⇄ متوقف                    │ إلغاء: مسموح فقط
              │                                                  │ قبل أي جلسة تنفيذ
              ▼ إقفال يوم الإنتاج (execution/Edit)               │ (وعكس المواد المساعدة)
   ┌──────────────────────────────────────────────┐              │
   │ • خصم الخام الفعلي من الدفعات (ConsumeLot)  │              │
   │ • مرتجع متبقي الصالة للدفعات (حركة Return)  │              │
   │ • تسوية المواد المساعدة آلياً (صرف/مرتجع)   │              │
   │ • توريد الكرتون الفارغ • مزامنة الخطة       │              │
   │ • إن «إرسال للفحص» ⟵ محضر جودة معلّق (+2يوم)│              │
   └──────────────────────────────────────────────┘              │
              │                                                  │
              ▼ الفحص والجودة (بعد يومَي تبريد)                  │
   استكمال المحضر ← معادلة (مفحوص=مقبول+مرفوض) ← اكتمال التغطية │
              │ اعتماد (quality/Approve) ← مزامنة المقبول للخطة  │
              ▼                                                  │
   استلام التام WFG (مسموح فور الإقفال — قبل اعتماد الفحص)      │
   مسودة ← إصدار ← استلام فعلي (الوحيد المؤثر على الرصيد)        │
              │                                                  │
              ▼ أمر تسليم الإنتاج (B96): من محضر معتمد،          │
                أو من خطة/إقفال بصلاحية تجاوز + سبب مكتوب        │
              ▼ تسليم العميل (بوابة الجودة المزدوجة:             │
                القرار + سقف المقبول + رصيد العميل)               │
              ▼ إقفال الخطة: كل أوامرهم مقفلة بلا موانع          │
                (أو استثنائي بسبب مكتوب) — وإلغاء الإقفال ممكن   │
```

**آلات الحالة:**
- **الأمر:** مسودة → معتمد/مجدول → قيد التنفيذ ⇄ متوقف → مكتمل (اكتمال كل البنود) → مقفل. الإلغاء متاح فقط قبل أي جلسة تنفيذ؛ لا يوجد «إقفال بعجز» بلا سبب تسوية مكتوب.
- **الفحص:** مُرسل(عند الإقفال)/مسودة → قيد الفحص → مكتمل (تغطية كامل المنتَج) → معتمد. التعديل بعد الاعتماد عبر «تصحيح معتمد» بصلاحية خاصة وسبب مسجل.
- **سند التام:** مسودة → مُصدر → مستلم (كلي/جزئي — كل استلام بسند متابعة مرقّم).
- **الخطة:** معتمدة → **مكتملة ≠ مقفلة** (قرار صريح) → مقفلة، مع إعادة فتح بصلاحية `Reopen` وسبب.

---

## 2) كيف يُخصم الخام ومتى — أهم قرار تصميمي في الدورة

**القاعدة المُنفذة:** الاعتماد يخصم **المواد المساعدة فقط**؛ **الخام يُخصم عند إقفال يوم الإنتاج بالكمية المستهلكة فعلياً** — والسبب موثق في الكود: في تصنيع التمور يزيد وزن الخارج عن الداخل لإضافة الماء أثناء التشغيل، فخصم الخام عند الاعتماد بوزن المنتج التام معادلة ثابتة خاطئة.

تفاصيل الخصم عند الإقفال (`CloseProductionDay`):
1. المستهلك = إدخال المستخدم الفعلي، وإن تركه فارغاً اعتُمد المخطط تقريباً (موثق).
2. التوزيع على الدفعات بنسبة المخطط لكل بند، مع سد كسور التقريب في آخر دفعة.
3. `ConsumeLot`: حارس رصيد (لا سالب) + **شبكة أمان المعالجة** — لا يُصرف خام لم تكتمل معالجته/تعقيمه مهما كان المسار.
4. حركة `Outbound` من مخزن الاستلام الفعلي للدفعة (البند المعالَج يعود لمخزن استلامه — لا يُفترض WRM دائماً).
5. **عميل حركة الصرف = مالك الدفعة** لا عميل البند التام (إصلاح B102 للأوامر متعددة العملاء).
6. المتبقي في الصالة يُعاد للدفعات بحركة `Return` موثقة بنسبة المصروف الفعلي، ويُعاد تخطيطه يدوياً في خطة الغد (لا ربط تلقائي).
7. تسوية المواد المساعدة: (المصروف عند الاعتماد) مقابل (فعلي × معادلة لكل كرتون منتج + الإدخال اليدوي + الهالك) — الفرق يُرتجع أو يُصرف آلياً بحركة موثقة.

**العكس:** `ReverseConsumption` (إلغاء الاعتماد/الإلغاء) يعكس المواد المساعدة فقط — لأن الخام لم يُصرف أصلاً. التعليق يوثق إصلاح علة «خلق مخزون من العدم» التي كانت تضيف المخطط كاملاً عند الإلغاء.

---

## 3) الانتقال إلى الفحص والجودة

- الإقفال اليومي مع «إرسال للفحص» ينشئ محضراً معلّقاً مرتبطاً بجلسة التنفيذ، بتاريخ متوقع **+يومي تبريد** (العيب لا يظهر إلا بعد أن يبرد المنتج — قاعدة مجال موثقة).
- `SaveCheck` يستكمل المحضر المعلّق بدل تكراره، ويفرض: معادلة الفحص (المفحوص = مقبول + مرفوض، كجم وكراتين)، بنود منتجات تامة فقط، **لا صنف خارج الأمر**، مطابقة هوية الصنف/الدفعة (`ProductIdentityGuard` — لا يُفحص خلاص مرتبط بدفعة سكري)، وربط الكراتين بالكيلو عبر تعريف العبوة (كانت الجودة المرحلة الوحيدة التي لا تربط الرقمين).
- **الاعتماد يتطلب تغطية كامل المنتَج** (مطابق + غير مطابق + مرفوض = المنتَج) مع إعادة احتساب حية لفحص ثانٍ لاحق أكمل التغطية.
- بعد الاعتماد: مزامنة المقبول إلى بنود الخطة بمطابقة **الدفعة** (لا الصنف وحده — إصلاح تعدد العملاء).
- التسليم لمخزن التام **مسموح فور الإقفال** (فترة التبريد)، لكن تسليم العميل **موقوف على اعتماد الفحص** — التعويض محكم في بوابة التسليم.

---

## 4) تسليم الإنتاج والتام وتسليم العميل

**استلام التام (`FinishedGoodsService`):**
- لا تسليم قبل إقفال يوم إنتاج مُرسل للجودة (أو فحص مستقل)، وسقف الاستلام = المنتَج الفعلي للصنف عبر كل السندات (يُعاد فحصه عند كل استلام — مسودتان معاً لا تتجاوزان).
- الإصدار لا يمس الأرصدة؛ **الاستلام الفعلي وحده يؤثر على الرصيد** (وارد WFG بسند متابعة مرقم، جزئي مسموح).

**أمر تسليم الإنتاج (B96):** المصدر محضر فحص معتمد، أو خطة/إقفال **بتجاوز يتطلب صلاحية `BypassInspection` + سبب مكتوب** (لا تجاوز صامت). مطابقة أسطر المصدر بالصنف+الدفعة+العميل، والعميل إلزامي عند الالتباس.

**تسليم العميل (`CustomerDeliveryService.Approve`) — ثلاث بوابات متتالية:**
1. `QualityGate.CustomerDeliveryAllowed` لكل بند: وجود فحص ← اعتماده ← قراره (مرفوض/محجوز يمنع). البوابة تشتق الأوامر من الدفعة إن مُرّر `orderId = null` — سد ثغرة التجاوز التاريخية.
2. رصيد العميل المتاح في مخزن التام يغطي الكمية، والدفعة إلزامية إن تعددت أرصدة الصنف.
3. `CustomerDeliveryQtyAllowed`: سقف المقبول المعتمد من الفحص.

**الإلغاء بقيد عكسي** (وارد `#REV1/2…`) لا بحذف حركات الدفتر، مع عكس توزيع الخطة وتصفير الفوترة — فلسفة دفتر أستاذ سليمة وموثقة.

---

## 5) إقفال الخطة

- `GetInfo` يحسب الموانع: أمر مفتوح/قيد الإنتاج/مكتمل بلا إقفال رسمي، بند خطة لم يُصدر كاملاً بأمر، إنتاج فعلي أكبر من المخطط.
- `ClosePlanFinal` (عزل `Serializable`): الإقفال العادي يتطلب صفراً من الموانع؛ **الاستثنائي يتطلب سبباً مكتوباً** ويُوثق في التدقيق مع قائمة الموانع. يحرر حجوزات الدفعات.
- `ReopenPlan` بصلاحية مستقلة وسبب مكتوب، ويعيد احتساب الحالة.
- «مكتملة ≠ مقفلة»: الإقفال الآلي مجرد تنبيه جاهزية — القرار صريح من شاشة الإقفال.

---

## 6) نتائج الفحص — ما ثبتت سلامته

| # | النقطة | الحكم |
|---|---|---|
| 1 | خصم الخام الفعلي عند الإقفال لا الاعتماد | سليم وموثق بقاعدة توازن الإنتاج |
| 2 | منع الإلغاء/إلغاء الاعتماد بعد وجود تنفيذ | سليم (حارس مزدوج: حالة + وجود جلسات) |
| 3 | عدم خلق مخزون من العدم عند العكس | سليم — العكس يخص المساعدة فقط، والعلة السابقة موثقة |
| 4 | بوابة الجودة المزدوجة عند تسليم العميل | سليمة — قرار + كمية، وسد تجاوز `null` |
| 5 | سقوف التراكم (خطة←أمر←تنفيذ←فحص←استلام←تسليم) | كل سقف يُعاد فحصه عند نقطة الأثر، بلا اعتماد على فحص سابق |
| 6 | هوية الدفعة/العميل عبر الدورة كاملة | سليمة (صرف بمالك الدفعة، تمييز بالدفعة في المزامنة والتسليم) |
| 7 | الإقفال الاستثنائي وإعادة الفتح | مشروطان بسبب مكتوب وتدقيق كامل |
| 8 | المعاملات الذرية | كل عملية في `RunOp` بمعاملة واحدة؛ الإقفال `Serializable` |

---

## 7) الملاحظات المكتشفة في هذا الفحص (جديدة)

### 🟠 متوسطة

**P1 — الإقفال يمر على بند كل أوامره ملغاة ولو بلا توثيق.**
`PlanClosureService.GetInfo`: `var coverage = active.Count > 0 ? active : matches;` — إن لم يوجد أمر نشط لبند خطة، تُحتسب الأوامر **الملغاة** تغطيةً للبند. التعليق يقول «الأمر الملغي **الموثق** يُعتبر مسوّى»، لكن لا يوجد أي فحص لوجود سبب الإلغاء في الأمر الملغي.
**الأثر:** خطة أُلغي أمرها الوحيد على بند ما (بلا سبب) تُقفل رسمياً بلا مانع وبصفر إنتاج لذلك البند — الظاهر في التدقيق لكن بلا منع.
**التوصية:** في حساب التغطية، استبعاد الأوامر الملغاة بلا `Notes`/سبب إلغاء من `matches`، أو إضافة مانع «بند بلا إنتاج ولا تسوية موثقة».

**P2 — `ReverseConsumption` يحذف حركات الدفتر بدل القيد العكسي.**
عند إلغاء الاعتماد/الإلغاء تُحذف حركات صرف المواد المساعدة (`RemoveRange`) ويُزاد الرصيد يدوياً — بينما `CustomerDeliveryService.Unapprove` يوثق فلسفة معاكسة صريحة: «الإلغاء بقيد عكسي (وارد) لا بحذف حركات دفتر الأستاذ».
**الأثر:** فقدان أثر الصرف والعكس من دفتر الحركة لمسار الإلغاء (التأثير المحاسبي صحيح، الأثر التاريخي لا). النطاق محدود: قبل أي تنفيذ فقط.
**التوصية:** توحيد الفلسفة — قيد عكسي بمرجع مميز (`#REV`) عبر `PostStockMovement`.

### 🟡 منخفضة

**P3 — بوابة القرار خارج المعاملة:** `CustomerDeliveryAllowed` تُفحص قبل `RunOp` بينما بوابة الكمية داخلها — نافذة زمنية ضيقة بين الفحص والتنفيذ (أسوأ الحالات قرار متقادم). النقل داخل المعاملة تعديل سطر واحد.

**P4 — المستهلك الافتراضي = المخطط** إن لم يُدخله المستخدم عند الإقفال. موثق ومقصود («تقريباً») وتظهر أثره في تنبيه معامل الإنتاجية — يُستحسن إضافة تنبيه صريح في رسالة الإقفال أنه تقدير لا قياس.

**P5 — يوما التبريد ثابتان في الكود** (`AddDays(2)` في الإقفال وفي `SaveCheck`). إن تغيّر ظرف المصنع يلزم تعديل برمجي — الأفضل إعداد في `SystemSettings`.

**P6 — المسار الإجمالي القديم** (بلا تفصيل بنود) يفحص اتساق الكرتون/الكيلو على البند الأول فقط (`break`) — مسار توافق، والشاشات تمرر تفصيل البنود دائماً؛ يُستحسن حذفه بعد التأكد من عدم وجود مستدعٍ خارجي.

### ℹ️ قرارات تصميم معلومة (ليست عيوباً)
- **إقفال يوم واحد لكل أمر:** المتبقي يعود لخام دفعته ويُعاد تخطيطه في خطة الغد — الاستمرار بأمر جديد لا بإقفال ثانٍ.
- **وضع `RecordingActualDelivery`:** يبسط الإقفال (متبقي الصالة صفر، لا ترحيل) — نمط شاشة مبسطة بمقاسات فعلية صافية.
- **تسوية المواد المساعدة تكتب حركاتها مباشرة** بكميات موقعة (اصطلاح دفتر موثق) بدل `PostStockMovement`.

---

## 8) التغطية الاختبارية القائمة لهذه الدورة

الدورة مغطاة بكثافة في حزمة الاختبارات: `FullWorkflowTests`, `FullCycleMultiCustomerTests`, `QualityGateTests`, `QualityDeliveryFlowTests`, `PlanClosureTests`/`PlanLevelClosingTests`/`ClosingParityTests`/`ClosingPreservesPlanBaselineTests`, `OrderReversalAuditTests`, `SingleCustomerEnforcementTests`, `DeliveryLineMatchingTests`, `ExecutionB57Tests`, `ProductionBalanceTests`, `PlanCapacityAccumulationTests`. **الملاحظة P1 تحديداً غير مغطاة باختبار** (لا اختبار يقفل خطة كل أوامر بندها ملغاة بلا سبب).

---

## 9) الخلاصة

الدورة الإنتاجية في هذا النظام **متماسكة ومحصنة بطبقات متراكبة** من السقوف والحراس، وأصعب القرارات فيها (خصم الخام الفعلي لا المخطط، مكتملة≠مقفلة، الإلغاء بقيد عكسي، بوابة الجودة المزدوجة) قرارات واعية وموثقة بأسبابها وسجل إصلاحاتها. الملاحظات الجديدة المكتشفة هنا (P1–P6) كلها في مستوى **التحصين لا الترميم**: أخطرها (P1) ثغرة سياسات في إقفال الخطط وليست خلل بيانات، و(P2) اتساق فلسفة دفتر. إصلاحهما مع اختبار لكل منهما يرفع اعتمادية الدورة إلى مستوى مرجعي.
