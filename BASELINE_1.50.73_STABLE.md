# Baseline Stable 1.50.73 — OFFTT / DatesErp

تاريخ تثبيت النسخة: 2026-09-19

## النسخة المستقرة

تم تثبيت النسخة التالية كمرجع Stable/Baseline قبل بدء أي تطوير جديد:

- `UPDATE_1.50.73_FULL_SOURCE_BUILDABLE.zip`
- النسخة الاحتياطية المطابقة: `BASELINE_1.50.73_STABLE_FULL_SOURCE_BUILDABLE.zip`
- عدد ملفات المصدر في الحزمة: 566
- SHA-256 للحزمتين:

```text
0674a4ce739c8b823f6a5d6b45c76a361d2b4a3493eb8df8f728ca07f94dee90
```

## تأكيد اختبار Windows

بحسب الاختبار الفعلي الذي تم تأكيده على Windows:

- تم فك الحزمة بنجاح.
- تم تنفيذ `dotnet publish` بنجاح.
- اشتغل البرنامج فعلياً.
- ظهرت شاشة خطط الإنتاج.
- ظهر زر «حفظ الخطة».
- تم إنشاء خطة وحفظها بنجاح.

وبذلك يُعد إصلاح زر حفظ خطة الإنتاج **مُختبراً بنجاح على Windows** في Baseline 1.50.73.

## حدود النسخة المستقرة

- لا تُعدّل أو تُحذف ملفات المصدر الخاصة بهذه النسخة.
- لا تُغيّر قاعدة البيانات أو إعدادات الاتصال الخاصة بها.
- لا تُستبدل الحزمة المستقرة بمخرجات تطوير لاحقة.
- أي تطوير لاحق يجب أن يكون إصداراً مستقلاً مع الرجوع إلى هذا الـ Baseline عند الحاجة.
- قبل تسليم أي تحديث لاحق يجب إعادة اختبار `dotnet publish` والتشغيل على Windows، بما في ذلك شاشة خطط الإنتاج والحفظ.

## Git reference

تم تثبيت مرجع Git باسم:

```text
stable-1.50.73
```

ويجب استخدام هذا المرجع لاستعادة النسخة المستقرة دون الاعتماد على ملفات `bin` أو `obj` أو EXE.

## البناء من المصدر

من جذر الحزمة:

```text
dotnet restore DatesErp.sln
dotnet publish DatesErp.sln -c Release -r win-x64 --self-contained true
```
