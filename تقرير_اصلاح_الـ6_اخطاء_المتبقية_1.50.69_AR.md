# تقرير إصلاح الـ 6 أخطاء المتبقية 1.50.69 — 0 Errors

**التاريخ:** 2026-09-17
**الفرع:** arena/01a091dd-offtt
**الالتزام:** 7999a70 + da59310 (السابق)
**الحالة:** تم الإصلاح، Push ناجح، Windows Installation NOT TESTED (الـ AI لا يستطيع تشغيل dotnet build في sandbox)

---

## الأخطاء الستة التي كانت موجودة في UPDATE_WORK

بعد إصلاحات 1.50.69 السابقة (CS1503 int?→int، CS1061 .Value على int، CS0234 Mvvm.PlanRowUi) وصل البناء إلى 6 أخطاء فقط:

### 1-3) CS0019 في 3 ملفات AutoSavePath
- **الموقع:**
  - `DeliveryView.xaml.cs:70`
  - `PlanningView.xaml.cs:54`
  - `ReceivingView.xaml.cs:38`
- **الكود الخاطئ:**
  ```csharp
  $"...{(cs ? cs.UserId ?? 0 : 0)}.json"
  ```
- **السبب:** `ICurrentSession.UserId` معرف في `ISession.cs:6` كـ `int UserId { get; }` وليس `int?`، بالتالي `??` غير صالح على int/int.
- **الحل:**
  ```csharp
  $"...{(cs ? cs.UserId : 0)}.json"
  ```
  إزالة `?? 0` والاكتفاء بـ `cs.UserId`.

### 4-5) CS0426 + CS7036 في ReceivingView:131 DocSearchWindow
- **الكود القديم:**
  ```csharp
  var w = new DocSearchWindow("بحث سندات الاستلام", _ship_all.Cast<dynamic>().Select(s => new DocSearchWindow.DocRow { ... }).ToList());
  ```
- **السبب:**
  - `DocSearchWindow.DocRow` لم يعد موجود — الواجهة الحالية تستخدم `SearchFieldDef` + `SearchResult`
  - الـ constructor الحالي يتطلب 3 باراميترات: `title, List<SearchFieldDef> filters, Func<Dictionary<string,string>, SearchResult> search`
- **الحل (متوافق مع PlanClosureView.xaml.cs:129):**
  ```csharp
  var win = new DocSearchWindow("بحث سندات الاستلام",
      new List<SearchFieldDef> {
          new() { Key = "doc", LabelAr = "رقم السند" },
          new() { Key = "customer", LabelAr = "العميل" },
          new() { Key = "container", LabelAr = "الحاوية" }
      },
      cond => {
          using var scope = AppContainer.NewScope();
          var db = scope.ServiceProvider.GetRequiredService<DatesErpDbContext>();
          var q = db.Shipments.AsNoTracking().AsQueryable();
          if (!string.IsNullOrWhiteSpace(cond.GetValueOrDefault("doc")))
              q = q.Where(s => s.DocumentNumber.Contains(cond["doc"].Trim()));
          // ... فلترة الحاوية والعميل
          var res = new SearchResult { Columns = new List<string> { "رقم السند", "العميل", "التاريخ", "الوزن كجم", "الحالة" } };
          foreach (var s in list) res.Rows.Add((s.Id, new object[] { ... }));
          return res;
      });
  win.Owner = Window.GetWindow(this);
  if (win.ShowDialog() == true && win.SelectedId is int id) OpenShipment(id);
  ```
  - يحافظ على وظيفة البحث، يعرض كل المستندات عند عدم إدخال شروط، ويدعم البحث بالعميل والحاوية.

### 6) CS0234 في ReceivingView:618 Views.PrintRenderer غير موجود
- **الكود القديم:**
  ```csharp
  var doc = new Views.PrintRenderer().Render(model);
  ```
- **السبب:** `PrintRenderer` الآن في `DatesErp.Desktop.Printing` كـ static class وليس في `Views`، وطريقة الطباعة الصحيحة لـ Receiving هي `ReceivingPrintDocument.Build(model)` كما في `ReceivingPrintDocument.cs:6`
- **الحل:**
  ```csharp
  var model = Views.ReceivingPrintModel.Load(db, _currentId);
  if (model == null) { Error("تعذر تحميل بيانات السند"); return; }
  var doc = Views.ReceivingPrintDocument.Build(model);
  var preview = new Views.PrintPreviewWindow(doc, $"سند استلام {model.DocumentNumber}") { Owner = Window.GetWindow(this) };
  preview.ShowDialog();
  ```
  - متوافق مع نمط DeliveryView (`StoredPrintModels.CustomerDelivery`) و PlanningView (`PlanningPrintModel.Load` + `PlanningPrintDocument.Build` + `PrintPreviewWindow`)

---

## الملفات التي تم تعديلها

- `src/DatesErp.Desktop/Views/Screens/PlanningView.xaml.cs` — سطر 54
- `src/DatesErp.Desktop/Views/Screens/DeliveryView.xaml.cs` — سطر 70
- `src/DatesErp.Desktop/Views/Screens/ReceivingView.xaml.cs` — سطر 38 + 131 (OpenSearchWindow كامل) + 618 (Print)
- `publish_1.50.67_READY/Source/...` — نفس الثلاثة (للحزمة الجاهزة)

---

## التحقق

- `grep -rn "UserId ?? 0"` بعد الإصلاح = 0 نتائج
- `grep -rn "DocSearchWindow.DocRow"` = 0
- `grep -rn "Views.PrintRenderer"` = 0 (فقط `Printing.PrintRenderer` الصحيح باقٍ)
- البناء في sandbox غير ممكن (dotnet غير مثبت، curl/wget محظور SSL_ERROR_SYSCALL — موثق في الجلسات السابقة)
- المستخدم يجب أن ينفذ في جهازه:
  ```
  cd C:\Users\LENOVO\.gemini\antigravity-ide\scratch\DateERP\UPDATE_WORK
  dotnet build DatesErp.sln -c Release
  ```
  المتوقع: `0 Error(s)`

---

## الحزمة التالية

بعد تأكيد 0 Errors:
- تشغيل `UPDATE-EXISTING-INSTALL-1.50.70.bat` (أو .69 حسب ترقيم المستخدم) الذي يبني وينسخ فوق `...\publish`
- إنشاء `DateERP_1.50.70_ONE_CLICK_INSTALL.zip` مع SHA256/size/commit/build result
- تقرير يذكر `Windows Installation NOT TESTED / Runtime NOT TESTED` لأن الـ AI لا يستطيع اختبار Windows

---

## القيود المحفوظة

- لا حذف وظيفة — البحث والطباعة والحفظ التلقائي باقٍ
- توافق مع `ICurrentSession.UserId int` حسب التعريف الفعلي
- توافق مع واجهة `DocSearchWindow` الحالية (title, filters, search Func)
- طباعة Receiving تستخدم `ReceivingPrintModel.Load` + `ReceivingPrintDocument.Build` + `PrintPreviewWindow` (نفس نمط النظام)
