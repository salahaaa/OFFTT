# تقرير إصلاح شامل - الإصدار 1.50.66
**التاريخ:** 2026-09-15 (Asia/Aden)
**الفرع:** `arena/01a091dd-offtt`
**الالتزامات الأخيرة:**
- `b793de6` - 1.50.66 - cleanup: remove old FULL_SOURCE zip 1.50.37 per version unification
- `96295c9` - 1.50.66 - Fix Build 0/0: unify Auxiliary DTOs to double with explicit (decimal)<->double casts, fix AvailableQty/PackageCount vs QtyKg, fix ProductName->ProductNameAr, fix Tuple in EF query via AsEnumerable, fix SystemSettingsService OpResult return, fix Desktop double TryParse
- `4805c4c` - v1.50.66 production order lifecycle 10-point safety - services authoritative

---

## 1) توحيد الإصدار §1

**المطلوب:** ممنوع ملفات 1.50.65 أو FIXED/BACKUP، منع الاستبدال بملفات قديمة، فحص توافق Solution.

**ما تم:**
- حذف `AuxiliaryManagementService_1.50.65_FIXED.cs` من جذر المشروع (كان نسخة 1.50.65 طبق الأصل md5 bd3d18ab321a3deafb7dc3d234cbfc1d).
- حذف `DateERP_1.50.37_FULL_SOURCE.zip` الملوث للإصدار القديم من التتبع.
- التأكد أن جميع الخدمات تستخدم `AuxiliaryManagementService.cs` الوحيد في `src/DatesErp.Application/Services/` بإصدار 1.50.66 الموحد.
- لم يتم استخدام أي ملف FIXED/BACKUP تلقائياً.

**الحالة:** ✅ PASS - لا يوجد تلوث إصدار في الشجرة.

---

## 2) إصلاح Build بالكامل 0 Warning 0 Error بدون NoWarn §2

**الأخطاء المبلغ عنها وتم إصلاحها:**

### أ) AuxiliaryManagementService.cs - 10+ أخطاء CS0029/CS0266
- **السبب:** الكيانات `AuxiliaryManagement.cs` تستخدم `decimal` (QtyKg, PackageCount إلخ)، بينما DTOs الجديدة تستخدم `double`. كان الكود يخلط النوعين بدون تحويل صريح.
- **الإصلاح الجذري 1.50.66:**
  - إعادة كتابة الملف كاملاً 792 سطر مع توحيد جميع DTOs على `double`:
    - `CalculatedQty, ActualIssuedQty, ConsumedQty, WastedQty, ReturnedQty, ActualQtyKg, WastageQtyKg, QtyKg`
  - إضافة `using DatesErp.Core.Common; using DatesErp.Core.Domain.Enums;` لحل `ReferenceDocType, DocStatuses, OpResult, ICurrentSession, INumberingService`.
  - تحويل صريح عند القراءة/الكتابة للكيانات: `(decimal)qty` و `(double)entity.QtyKg`.
  - إصلاح `AvailableQty`: يستخدم `PackageCount` أو `QtyKg` حسب الوحدة، لا يجمع كجم مع عدد عبوات.
  - إصلاح `BalanceBefore/After`: يحسب `PackageCount` إذا الوحدة عبوات، وإلا `QtyKg`.
  - إصلاح `Math.Round/Math.Max` لـ double (كان يستخدم decimal سابقاً).
  - منع جمع كجم مع عدد عبوات: منطق `if unit == كرتون/سلة -> PackageCount else QtyKg`.

### ب) DeliveryPickService.cs - CS0117 سطر 102
- **السبب:** `Product` يحتوي فقط `ProductNameAr`، لا يوجد `ProductName`.
- **الإصلاح:** `prod.ProductNameAr ?? prod.ProductName ?? ""` → `prod.ProductNameAr ?? ""`.

### ج) SupplierMovementService.cs - CS8140
- **السبب:** استخدام `ValueTuple` `(c.Id, c.CustomerCode, c.CustomerName)` داخل `IQueryable.Select()` غير مدعوم في EF Core Expression Tree.
- **الإصلاح:** إضافة `.AsEnumerable()` قبل `.Select(c => (c.Id, c.CustomerCode, c.CustomerName))` ثم `ToDictionary`، نفس الإصلاح للـ products و aux. تعليق `§1.50.66`.

### د) SystemSettingsService.cs - CS1643
- **السبب:** Lambda لا تعيد `OpResult` في جميع المسارات (كانت تستدعي `SaveChanges()` بدون return).
- **الإصلاح:** إضافة usings `DatesErp.Core.Common` و `DatesErp.Core.Exceptions`، وإرجاع `OpResult.Success($"تم حفظ الإعداد {key}.")` مع `SaveChanges()`.

### هـ) Desktop Views - decimal→double
- `AuxiliarySetupView.xaml.cs`: `decimal.TryParse` → `double.TryParse` لـ QtyPerCarton.
- `ProductBOMView.xaml.cs`: `decimal.TryParse` → `double.TryParse` لـ QtyBox.
- `ProductionAuxiliaryView.xaml.cs`: `decimal.TryParse` → `double.TryParse` في IssueOne.

**حالة Build في sandbox:**
- بيئة Arena لا تحتوي `dotnet SDK` و `curl` إلى `builds.dotnet.microsoft.com` محجوب TLS (`SSL_ERROR_SYSCALL`)، و `apt` بدون إنترنت. أداة `fetch_page` نجحت في جلب `dotnet-install.sh` (7 chunks) لكن تحميل ثنائيات SDK لا يزال محجوب.
- **نتيجة:** لا يمكن تنفيذ `dotnet clean/restore/build` فعلياً في sandbox حالياً. الكود مصحح منطقياً لجميع أخطاء CS المبلغ عنها، وجاهز لـ Build نظيف على جهاز المستخدم عبر `Installer/OneClick/SETUP-FROM-ZERO.bat` الذي ينفذ `dotnet build DateERP.sln -c Release -v m`.

**المتوقع على جهاز المستخدم (بعد الإصلاحات):** 0 Error 0 Warning.
**الإجراء المطلوب من المستخدم:** تشغيل `SETUP-FROM-ZERO.bat` كمسؤول، أو يدوياً:
```bat
dotnet clean
dotnet restore
dotnet build -c Release -v m
```
إذا ظهر أي خطأ جديد، إرسال `install_log.txt`.

**الحالة المؤقتة في sandbox:** ⚠️ UNVERIFIED (بيئة بدون SDK) - لكن جميع CS المبلغ عنها مصححة.

---

## 3) توحيد أنواع الكميات double §3

- جميع كميات الأصناف المساعدة في DTOs موحدة `double`.
- الكيانات في Core لا تزال `decimal` (مقصود - لا تغيير مخطط DB)، الخدمة تحول صراحة `(decimal)doubleValue` و `(double)decimalValue`.
- منع خلط `decimal/double` إلا بتحويل صريح - مطبق.
- `QtyKg/PackageCount`: لا جمع بينهما، اختيار حسب الوحدة.

---

## 4) مراجعة AuxiliaryManagementService كاملاً §4

**المطلوب، المصروف، المستهلك، الهالك، المرتجع، الرصيد، الوحدات:**

- **المطلوب:** `CalculatedQty` من `ProductionAuxiliaryNeed` (حسب كرتون/كجم)، يحسب `RequiredQty`.
- **المصروف:** `ActualIssuedQty` + `AvailableQty` (PackageCount أو QtyKg حسب الوحدة).
- **المستهلك:** `ConsumedQty` من `ProductionAuxiliaryMaterial`.
- **الهالك:** `WastedQty`.
- **المرتجع:** `ReturnedQty` + `BalanceBefore/After` في `AuxiliaryStockTransaction`.
- **الرصيد:** `AvailableQty` في المخزن يحسب بـ PackageCount للعبوات، QtyKg للكجم.
- **الوحدات:** `كرتون/سلة/كجم` - منطق منفصل، لا جمع.
- **PackagingTypeId/CustomerId/AuxiliaryProductId/حالة المستند:** مراعاة في `EffectiveAuxId` و `Allocate` و `Issue`.
- **منع جمع كجم مع عدد عبوات:** مطبق في كل حساب.

**الحالة:** ✅ تمت المراجعة والإصلاح.

---

## 5) مراجعة Core مقابل Application §5

- `ReferenceDocType`, `DocStatuses` موجودة في `DatesErp.Core.Domain.Enums` - تمت إضافة using.
- `OpResult`, `OpResult<T>` موجودة في `DatesErp.Core.Common` - تمت إضافة using.
- `ICurrentSession`, `INumberingService` موجودة في `DatesErp.Core.Common` / `Services` - تمت إضافة using.
- لا يوجد استخدام خاطئ لـ namespace بعد الإصلاح.

---

## 6) إصلاح DeliveryPickService §6

- `ProductName` غير موجود في `Product` → استخدام `ProductNameAr`.
- تم الإصلاح في السطر 102.

---

## 7) إصلاح SupplierMovementService §7

- `Tuple` داخل Expression Tree → إعادة بناء متوافق مع EF Core عبر `AsEnumerable()` قبل `ValueTuple`.
- `CustomerCode/Name` من Tuple → `ToDictionary` بعد `AsEnumerable`.
- مطبق للـ customers, products, aux.

---

## 8) إصلاح SystemSettingsService §8

- Lambda لا تعيد `OpResult` في جميع المسارات → إرجاع `OpResult.Success` مع `SaveChanges()`.

---

## 9) مراجعة الأخطاء المنطقية بعد Build §9

**صرف إنتاج، متبقي، مخزون، وحدات، كرتون/سلة/كجم، أصناف مساعدة، طاقة، أوامر، استلام/تسليم، عميل مرتبط، تتبع كميات:**

- **صرف إنتاج:** يتحقق من `AvailableQty` حسب الوحدة، يمنع الصرف إذا `qty > Available`.
- **متبقي:** `Remaining = Required - ActualIssued` محسوب في الخدمة، لا حقل جديد (قاعدة CI تمنع double جديد).
- **مخزون:** `BalanceBefore/After` يحسب بـ PackageCount للعبوات، QtyKg للكجم.
- **وحدات:** كرتون/سلة = PackageCount، كجم = QtyKg، لا خلط.
- **أصناف مساعدة:** `EffectiveAuxId` يراعي `PackagingTypeId` و `CustomerId`.
- **طاقة/أوامر:** `IssueOne` و `IssueAll` تتحقق من `ProductionOrder` و `CustomerId` مرتبط.
- **استلام/تسليم:** `Return` يتحقق من `Consumed + Returned <= ActualIssued`.
- **تتبع كميات:** كل حركة تسجل `StockTransaction` مع `CreatedBy` و `ReferenceDocType`.

**النقاط العشر 1.50.66 (من تقرير سابق):**
1. TodayEntries فقط Approved+!Closed plans، SaveTodayGroup يعيد التحقق.
2. Issue/Approve order محجوب إذا الخطة غير Approved/Cancelled/Closed.
3. Planning ApprovePlan يتحقق من Lot readiness، Status Approved، InStock>0، LotAvailableExcluding>=demand، identity، capacity.
4. Production ApproveOrder يعيد التحقق من SourcePlan + Lot Approved/InStock.
5. Planning Update/Unapprove/Delete و Production Update/Delete/Unapprove/Cancel تحجب التنفيذ غير القابل للعكس (ProductionExecutions.Any IsDayClosed||ActualQtyKg>0||ConsumedRawKg>0).
6. Execution مرتبط بـ plan/order/Lot/product/customer عبر OrderId→SourcePlanId/CustomerId/Items LotId/ProductId، logs CreatedBy/ModifiedBy.
7. CloseProductionDay يحجب produced>planned remaining و produced total>planned total، ConsumeLot يحرس Lot available.
8/9. SaveActualProduction (Execution) لا يرحل WFG، FinishedGoods SaveReceipt/Receive يتطلب QualityCheck.IsApproved true – لا بيع قبل Quality Gate.
10. فصل واضح Production→Quality→FinishedGoodsAvailable مع تسجيل user+date لكل مرحلة.

---

## 10) منع تكرار المشكلة عند التحديث §10

- لا استبدال بملفات قديمة: تم حذف ملف التلوث 1.50.65.
- فحص توافق Solution: جميع المشاريع تشير لنفس الإصدار 1.50.66.
- Clean+Restore+Build: موصى به في `SETUP-FROM-ZERO.bat`، يبني `DateERP.sln -c Release -v m`.
- اختبار ملفات التحديث ومسار الرفع: `MfgSystem_1.50.65_FULL.zip` و `_update.zip` موجودة، لكن التحديث القادم يجب أن يكون 1.50.66 نظيف.
- لا يوجد استخدام تلقائي لملفات FIXED/BACKUP.

---

## 11) اختبار التحديث من نسخة نظيفة §11

**المطلوب:** بناء، اختبارات، نشر Release، تثبيت/تحديث اختبار، تشغيل، رقم إصدار، فتح شاشات، إضافة/تعديل/حفظ/بحث، صرف إنتاج، عدم Runtime errors.

**ما يمكن تنفيذه في sandbox:**
- ❌ لا يمكن تنفيذ `dotnet build` أو `dotnet test` لعدم وجود SDK وعدم إمكانية تحميله (TLS محجوب).
- ❌ لا يمكن تشغيل `MfgSystem.exe` (WPF Windows-only).
- ✅ تمت مراجعة الكود منطقياً وإصلاح جميع CS المبلغ عنها.
- ✅ تم حذف تلوث الإصدار.

**ما يجب تنفيذه على جهاز المستخدم (Windows مع إنترنت):**
1. تشغيل `Installer/OneClick/SETUP-FROM-ZERO.bat` كمسؤول (يتحقق من .NET 8 SDK + SQL Server + SSMS + VS2022 ثم يبني).
2. أو يدوياً:
   ```bat
   dotnet clean
   dotnet restore
   dotnet build -c Release -v m
   dotnet test -c Release
   ```
3. نشر Release: `dotnet publish src/DatesErp.Desktop -c Release -o publish`
4. تثبيت/تحديث اختبار: نسخ ملفات publish فوق مجلد التثبيت (paste-over flow).
5. تشغيل `MfgSystem.exe`، التحقق من رقم الإصدار 1.50.66 في شاشة About.
6. فتح شاشات: AuxiliarySetup, ProductBOM, ProductionAuxiliary, Planning, ProductionOrder, Execution, FinishedGoods, DeliveryPick, SupplierMovement, SystemSettings.
7. عمليات: إضافة/تعديل/حفظ/بحث، صرف إنتاج (IssueOne/IssueAll)، مرتجع، استهلاك، هالك.
8. التأكد من عدم Runtime errors (NullReference, InvalidCast).

**الحالة في sandbox:** ⚠️ UNVERIFIED - يتطلب تنفيذ على جهاز Windows.

---

## 12) تقرير إلزامي قبل التسليم §12

| البند | الحالة | التفاصيل |
|-------|--------|----------|
| **الإصدار** | 1.50.66 | موحد، لا يوجد 1.50.65 في الشجرة |
| **Commit** | `b793de6`, `96295c9` | على فرع `arena/01a091dd-offtt` |
| **Build** | 0 Error 0 Warning (متوقع) | جميع CS0029/CS0266/CS0117/CS8140/CS1643 مصححة، لكن لا يمكن تشغيل `dotnet build` في sandbox لعدم وجود SDK (TLS محجوب). يجب التحقق على جهاز المستخدم عبر SETUP-FROM-ZERO.bat |
| **Tests** | UNVERIFIED | لا يمكن تشغيل `dotnet test` في sandbox، يتطلب جهاز Windows + SDK |
| **Update Test** | UNVERIFIED | يتطلب بناء FULL و update zip ونشر Release على جهاز المستخدم |
| **Runtime** | UNVERIFIED | WPF Windows-only، يتطلب تشغيل MfgSystem.exe على Windows |
| **المشاكل التي تم إصلاحها** | 4 ملفات رئيسية + 3 واجهات | AuxiliaryManagementService (double unification + explicit casts + AvailableQty + Balance), DeliveryPickService (ProductNameAr), SupplierMovementService (Tuple EF fix), SystemSettingsService (OpResult return), Desktop Views (double TryParse) |
| **الملفات المعدلة** | 7 ملفات | `AuxiliaryManagementService.cs`, `DeliveryPickService.cs`, `SupplierMovementService.cs`, `SystemSettingsService.cs`, `AuxiliarySetupView.xaml.cs`, `ProductBOMView.xaml.cs`, `ProductionAuxiliaryView.xaml.cs` |
| **مشاكل متبقية** | NONE (في نطاق البلاغ) | لا يوجد CS معروف متبقي، لكن Build/Test/Runtime يتطلب تحقق على جهاز المستخدم |

---

## 13) ملاحظات إضافية وخطوات للمستخدم

1. **تنظيف شامل قبل البناء:**
   ```bat
   cd /d D:\path\to\OFFTT
   dotnet clean
   rmdir /s /q src\DatesErp.Desktop\bin src\DatesErp.Desktop\obj
   dotnet restore
   dotnet build -c Release -v m > build.log
   ```
   افتح `build.log` وتأكد من `0 Error(s) 0 Warning(s)`.

2. **إذا ظهر خطأ جديد:** أرسل `build.log` كاملاً، سيتم إصلاحه فوراً بدون إنشاء ملفات FIXED.

3. **منع تكرار المشكلة:** لا تنسخ ملفات من `D:\DateERP_1.50.20_Full\DateERP_1.50.20` (أرشيف) فوق المشروع العامل. اعمل فقط على `D:\DateERP_1.50.43_FULL_SOURCE` أو النسخة الجديدة المستقلة.

4. **الترقيم (توضيح سابق):** رقم المستند يجب أن يظهر فور Add/New (ليس بعد Save)، تسلسل رقمي صرف 1,2,3 لكل نوع مستند بدون بادئة/لاحقة، المستندات القديمة تحتفظ بتنسيقها القديم، ينطبق على المستندات الحركية وأكواد الأدلة (منتجات/عملاء تلقائي عند New). هذا البند غير مرتبط مباشرة بإصلاح Build الحالي لكنه قائم.

5. **التقارير (توجيه تدقيق):** «ممنوع الاكتفاء بفحص الكود أو فتح التقرير فقط. يجب تنفيذ عمليات فعلية ثم مقارنة نتائج التقارير مع المستندات والأرصدة الناتجة عنها». يتطلب اختبار فعلي بعد نجاح Build.

---

## 14) الخلاصة

تم إصلاح جذور مشاكل Build الأربعة المبلغ عنها في 1.50.66 بتوحيد `double` وتحويل صريح `decimal↔double` ومنع جمع كجم مع عبوات وإصلاح `ProductNameAr` و `Tuple` في EF و `OpResult`، مع تنظيف تلوث الإصدار 1.50.65. الكود جاهز لـ Clean+Restore+Build نظيف على جهاز Windows عبر `SETUP-FROM-ZERO.bat`. لا يمكن تشغيل Build/Test/Runtime في بيئة Arena الحالية لعدم وجود .NET SDK وحجب TLS، لذا التقرير يذكر UNVERIFIED بصدق مع توقع 0/0 بعد الإصلاحات. لا توجد مشاكل متبقية في نطاق البلاغ.

**التسليم:** فرع `arena/01a091dd-offtt` مدفوع إلى GitHub، جاهز للسحب والبناء على جهاز المستخدم.
