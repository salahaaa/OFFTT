# تقرير السلامة v1.50.66 — دورة حياة أمر الإنتاج (10 نقاط) — تنفيذ فعلي

**التاريخ:** 2026-09-15
**الفرع:** arena/01a091dd-offtt
**الحالة:** تم التنفيذ — الخدمات هي المرجع (Services authoritative) لا الواجهة

---

## 1) أمر الإنتاج ينشأ فقط من خطة معتمدة Approved Production Plan

- **الملف:** `src/DatesErp.Application/Services/ProductionOrderService.Today.cs`
- **التعديل:**
  - `TodayEntries()` كانت تسمح `Approved || Closed` — تم تعديلها إلى `IsApproved && Status == Approved && !IsClosed` فقط.
  - `SaveTodayGroup()` أضيف فحص صريح: `plan.IsApproved && Status==Approved && !IsClosed` وإلا رفض `PLAN_NOT_APPROVED`.
  - رسالة: "أمر الإنتاج لا ينشأ يدوياً؛ يلزم مرجع خطة اليوم المعتمدة (Approved Production Plan فقط)."

## 2) منع إصدار أمر إذا الخطة غير معتمدة أو ملغاة أو مقفلة

- نفس الملف + `GetTodayPendingGroups()` و `IssueTodayGroup()` و `ValidateTodayOrder()` — جميعها تمر عبر `TodayEntries()` التي أصبحت ترفض المقفلة/الملغاة.
- في `SaveTodayGroup()` فحص إضافي قبل أي إنشاء.

## 3) عند اعتماد الخطة: تحقق من جاهزية الدفعة والطاقة والكمية المتاحة

- **الملف:** `src/DatesErp.Application/Services/PlanningService.cs`
- **إضافات:**
  - `CheckLotAvailabilityForApproval(plan)` — لكل دفعة في الخطة:
    - الدفعة موجودة، حالتها Approved، غير مقفلة، رصيدها >0.
    - المتاح الفعلي = `LotAvailableExcluding(lotId, planId)` (يطرح حجوزات الخطط الأخرى والأوامر) — يجب أن يكون ≥ المطلوب في هذه الخطة.
  - `ApprovePlan()` الآن يستدعي:
    1. `CheckTreatmentReadiness()` (كان موجوداً)
    2. `CheckLotAvailabilityForApproval()` (جديد)
    3. داخل المعاملة: إعادة فحص الاثنين + `ValidateCapacity()`
  - يمنع اعتماد خطة على دفعة صفر أو غير معتمدة أو كميتها غير كافية.

## 4) عند اعتماد أمر الإنتاج: إعادة التحقق من الخطة/الدفعة/الكمية قبل السماح بالتنفيذ

- **الملف:** `src/DatesErp.Application/Services/ProductionOrderService.cs`
- **إضافات:**
  - `ReverifyPlanAndLotForApproval(order)` — يتحقق:
    - `SourcePlan` ما زالت Approved وغير مقفلة.
    - كل دفعة في الأمر معتمدة ورصيدها >0.
  - `ApproveOrder()` الآن:
    - `ReverifyPlanAndLotForApproval()` أولاً
    - ثم `ValidateTodayOrder()` + `GuardReceivingOrderReadiness()` + `CheckPlanRemaining()` + `ValidateSourceCapacity()`
  - يمنع اعتماد أمر إذا تغيرت حالة الخطة بعد الإنشاء.

## 5) منع تعديل الخطة أو الأمر بعد دخول مرحلة التنفيذ غير القابلة للرجوع

- **الملفات:** `PlanningService.cs` + `ProductionOrderService.cs`
- **تعريف المرحلة غير القابلة للرجوع:** وجود `ProductionExecutions` بـ `IsDayClosed || ActualQtyKg>0 || ConsumedRawKg>0`
- **PlanningService:**
  - `PlanHasIrreversibleExecution(planId)` جديد — يبحث عن أوامر الخطة ثم تنفيذاتها.
  - `UpdatePlan()` — يرفض إذا `PlanHasIrreversibleExecution()`
  - `UnapprovePlan()` — يرفض فك الاعتماد إذا دخلت التنفيذ
  - `DeletePlan()` — يرفض الحذف إذا دخلت التنفيذ
- **ProductionOrderService:**
  - `OrderHasIrreversibleExecution(orderId)` جديد
  - `UpdateOrderHeader()` — يرفض إذا تنفيذ غير قابل للرجوع
  - `DeleteOrder()` — يرفض الحذف
  - `UnapproveOrder()` و `CancelOrder()` — يرفضان إذا تنفيذ موجود (رسالة "مرحلة غير قابلة للرجوع")

## 6) ربط التنفيذ الفعلي بالخطة والأمر والدفعة والمنتج والعميل

- **الملف:** `src/DatesErp.Application/Services/ExecutionService.cs`
- **التنفيذ:**
  - `ProductionExecution.OrderId` يربط بـ `ProductionOrder` الذي يملك `SourcePlanId` + `CustomerId` + `Items` (كل Item يملك `LotId/ProductId/CustomerId/PackagingTypeId`)
  - عند الإقفال: `exe.CreatedBy = Session.UserId`, `ModifiedBy`, `CreatedDate`, `ModifiedDate`, `EndDateTime` — سجل مستخدم وتاريخ لكل مرحلة.
  - التعليق: "§1.50.66.6 — ربط التنفيذ بالخطة والأمر والدفعة والمنتج والعميل + سجل المستخدم والتاريخ"

## 7) عند إقفال يوم الإنتاج: منع المنتَج > المخطط أو > المتاح

- **الملف:** `ExecutionService.cs`
- **إضافات في `CloseProductionDay()`:**
  - إعادة التحقق أن `SourcePlan` ما زالت معتمدة (قبل أي صرف).
  - فحص إجمالي: `producedSoFar + producedKg <= plannedTotal` وإلا `OVER_PLAN`.
  - فحص تفصيلي لكل بند (كان موجوداً) — يبقى: `ProducedQtyKg + kg > PlannedQtyKg` مرفوض.
  - `ConsumeLot()` يفحص `InStockQtyKg` و `GuardTreatedStock()` — يمنع صرف أكثر من المتاح أو خام تحت المعالجة.
  - `remainingInHall` يُعاد لخام دفعته — لا يختفي.

## 8) الإنتاج غير صالح للبيع عند SaveActualProduction — يجب أن يمر عبر Quality Gate

- **الملف:** `ExecutionService.cs` + `FinishedGoodsService.cs`
- **التنفيذ:**
  - `CloseProductionDay()` لا ينشر أي حركة إلى `WFG` (مخزن التام) — فقط `WRM` صرف ومرتجع. تعليق صريح: "§1.50.66.9 — لا يُمنح رصيد بيع (WFG) في هذه المرحلة"
  - `FinishedGoodsService.SaveReceipt()` الآن يرفض إذا لا يوجد `QualityCheck.IsApproved == true` — رسالة "لا يمكن تسليم الإنتاج قبل ... واعتماد الفحص — Quality Gate"
  - `Receive()` أيضاً يفحص `qcApproved` قبل `PostStockMovement` إلى WFG — يرمي `QUALITY_GATE` إذا لم يعتمد.

## 9) إصلاح مسار SaveActualProduction ليمنع منح رصيد بيع قبل اعتماد الجودة

- **الملف:** `FinishedGoodsService.cs`
- **قبل:** كان يسمح `coolingPending = !IsApproved` وينشئ سند استلام مع ملاحظة تبريد — يمنح رصيد بيع قبل الجودة.
- **بعد:**
  - `SaveReceipt()` يرفض إذا الفحص غير معتمد.
  - مسار `delivery` المربوط أيضاً يتحقق من اعتماد فحصه.
  - `Receive()` يتحقق مرة ثانية قبل `PostStockMovement` إلى WFG.
  - لا يوجد أي `PostStockMovement` إلى WFG في `ExecutionService` — الفصل صارم.

## 10) فصل واضح Production → Quality → Finished Goods Available مع سجل المستخدم والتاريخ لكل مرحلة

- **Production:** `ExecutionService.CloseProductionDay()` — يسجل `CreatedBy/ModifiedBy/EndDateTime` + يصرف خام من `WRM` + ينشئ `QualityCheck` مسودة `ExpectedCheckDate = Today+2` + لا يمنح WFG.
- **Quality:** `QualityService.SaveCheck()` + `ApproveCheck()` — يسجل `InspectorName`, `ApprovedBy`, `ApprovedDate`, `Decision`, `MoisturePct`... + يزامن `PlanSync.SyncAccepted()`.
- **Finished Goods:** `FinishedGoodsService.SaveReceipt()` + `Receive()` — يسجل `CreatedBy`, `ApprovedBy`, `ApprovedDate`, `ReceiveCount`, `ReceiptNumber` + ينشر إلى `WFG` فقط بعد `Quality.IsApproved`.
- كل مرحلة لها مستخدم وتاريخ وتُسجل في `AuditLogs` و `OrderEvents` (مركز المهام).

---

## الصلاحيات منفصلة (Approval permissions separate)

- `Require("planning","Approve")` لاعتماد الخطة فقط.
- `Require("production","Approve")` لاعتماد أمر الإنتاج فقط.
- `Require("quality","Approve")` لاعتماد الفحص فقط.
- `Require("execution","Edit")` لإقفال اليوم فقط.
- `Require("finishedgoods","Approve")` لاستلام التام فقط.
- لا يوجد تجاوز — كل خدمة تتحقق من صلاحيتها.

---

## الملفات المعدلة

1. `src/DatesErp.Application/Services/ProductionOrderService.Today.cs` — TodayEntries + SaveTodayGroup
2. `src/DatesErp.Application/Services/PlanningService.cs` — CheckLotAvailability + PlanHasIrreversibleExecution + Approve/Update/Unapprove/Delete
3. `src/DatesErp.Application/Services/ProductionOrderService.cs` — Reverify + OrderHasIrreversibleExecution + Approve/Update/Delete/Cancel/Unapprove
4. `src/DatesErp.Application/Services/ExecutionService.cs` — guards plan still approved + produced <= planned + no WFG + user/date logging
5. `src/DatesErp.Application/Services/FinishedGoodsService.cs` — Quality Gate إجباري قبل WFG

---

## اختبار يدوي مقترح (كما يطلب المستخدم: عمليات فعلية ثم مقارنة التقارير)

1. أنشئ خطة ببند دفعة رصيدها صفر → اعتماد يجب أن يرفض "رصيدها صفر".
2. اعتمد خطة → أنشئ أمر إنتاج اليوم → ألغِ اعتماد الخطة → حاول اعتماد الأمر → يجب أن يرفض "الخطة ليست معتمدة حالياً".
3. اعتمد أمر → سجل إقفال يوم بكمية > المخطط → يجب أن يرفض OVER_PLAN.
4. أقفل يوم إنتاج (Production) → حاول استلام في التام (WFG) قبل اعتماد الجودة → يجب أن يرفض Quality Gate.
5. اعتمد فحص الجودة → استلم في التام → يجب أن ينجح ويمنح رصيد بيع.
6. بعد إقفال يوم (Execution) → حاول تعديل الخطة أو الأمر → يجب أن يرفض "مرحلة غير قابلة للرجوع".
7. تحقق من `OrderEvents` — يجب أن يظهر: إنشاء → اعتماد خطة → إنشاء أمر → اعتماد أمر → بدء → إقفال (مع مستخدم وتاريخ) → فحص جودة → اعتماد جودة → استلام تام.

---

## ملاحظات التوافق

- لا حذف لوظائف قائمة — فقط إضافة حراس.
- رقم المستند يظهر عند الإنشاء (Numbering.Next) — متوافق مع مطلب المستخدم.
- لا تغيير DB Schema — فقط منطق خدمات.

