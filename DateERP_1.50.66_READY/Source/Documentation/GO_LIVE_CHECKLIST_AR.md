# 🛫 قائمة إقلاع النظام — تشغيل MfgSystem لأول مرة بـ10 مستخدمين

**الإصدار:** 1.50.60 — **التاريخ:** 13/09/2026
**الاستخدام:** اطبع هذه القائمة وعلّقها بجانب جهاز الخادم. اشطب كل بند ✓ بالترتيب — لا تنتقل لبند قبل إتمام ما قبله. مثل قائمة إقلاع الطيار: لا إقلاع بذاكرة، بل بقائمة.

> ⏱ الزمن التقديري الكامل: يوم عمل واحد (المرحلة أ+ب+ج) ثم يوم تشغيل أول (د) وأسبوع مراقبة (هـ).

---

## المرحلة (أ) — الخادم وقاعدة البيانات ⚙️

| ✓ | البند | خطوات النقر | كيف تتأكد أنه تم؟ |
|---|---|---|---|
| ☐ | A1. تثبيت SQL Server على جهاز الخادم | شغّل `SQLEXPR_x64_ENU.exe` ← اختر **Basic** ← وافق على الترخيص ← Install ← عند النهاية اضغط **Install SSMS** (أو شغّل ملف SSMS 20.2.1 منفصلاً) | تظهر أيقونة SSMS في قائمة ابدأ |
| ☐ | A2. تفعيل اتصال الشبكة للخادم | افتح **SQL Server Configuration Manager** ← SQL Server Network Configuration ← Protocols ← انقر يمين **TCP/IP** ← Enable ← أعد تشغيل خدمة SQL Server (كليك يمين ← Restart) | الحالة Running بعد Restart |
| ☐ | A3. إنشاء قاعدة النظام | افتح SSMS ← اتصل بـ `localhost` (Windows Authentication) ← كليك يمين Databases ← **New Database** ← الاسم حسب دليل `Server_Setup.md` | القاعدة ظاهرة في Object Explorer |
| ☐ | A4. مجلد النظام على الخادم | انسخ محتوى حزمة **FULL** إلى `D:\MfgSystem` على الخادم | المجلد موجود وبداخله ملفات البرنامج |
| ☐ | A5. البناء الأول | شغّل `update.bat` من مجلد النظام ← انتظر تقرير **BUILD SUCCESS** ثم **COPY SUCCESS** | الرسالة الخضراء/النص `[SUCCESS]` في آخر النافذة |
| ☐ | A6. فحص الاتصال من البرنامج | افتح النظام من الخادم ← شاشة إعداد الاتصال ← **فحص الاتصال** | الخطوات الخمس كلها ✓ (الخادم/الخدمة/القاعدة/الدخول/الاتصال) |

**⛔ توقف هنا إن فشل أي بند — لا تكمل والقاعدة غير موصولة.**

---

## المرحلة (ب) — الاختبارات الآلية والفحص الذاتي 🧪

| ✓ | البند | خطوات النقر | كيف تتأكد؟ |
|---|---|---|---|
| ☐ | B1. تشغيل حزمة الاختبارات الكاملة | شغّل `فحص_التقارير_تشغيل_الاختبارات.bat all` | السطر الأخير `[SUCCESS] All executed audit tests passed` — **الصورة الكاملة (~800 اختبار)** |
| ☐ | B2. اختبارات مسارات التصحيح الجديدة | نفس الملف بالحجة `corr` | `[SUCCESS]` |
| ☐ | B3. الفحص الذاتي من داخل النظام | افتح النظام ← شاشة **«معلومات النظام»** ← زر **الفحص الذاتي** | الخلاصة «✅ لا خلل بنيوي» — والأقسام الستة: الاتصال/الأولية/التشغيلي/الاتساق/**الفحص العميق**/الشاشات |
| ☐ | B4. حفظ تقرير الفحص | الملف يُكتب تلقائياً في `%\LocalAppData%\DateERP\logs\selftest.txt` — انسخه لمجلد النظام كأرشيف اليوم الأول | الملف موجود وتاريخه اليوم |
| ☐ | B5. فحص مطابقة المخزون | شاشة «معلومات النظام» ← زر **فحص مطابقة المخزون** | «لا فروق» (طبيعي قبل أي إدخال) |

> **ملاحظة:** الفحص الذاتي يعمل **تلقائياً عند كل إقلاع** بلا نوافذ، والفحص العميق (الجديد) يكشف آلياً: فرق الأرصدة عن دفتر الحركات، القيود المكررة، رايات الاعتماد المتناقضة، السلاسل الممزقة (مستند حي على دفعة ملغاة)، الحسابات بلا مدير فعّال، ونقص صفوف مصفوفة الصلاحيات.

---

## المرحلة (ج) — المستخدمون والصلاحيات والبيانات الأولية 👥

| ✓ | البند | خطوات النقر | كيف تتأكد؟ |
|---|---|---|---|
| ☐ | C1. إنشاء الموظفين | شاشة **الموظفين** ← جديد ← أدخل العشرة ← حفظ | القائمة تعرض 10 |
| ☐ | C2. إنشاء حساب دخول لكل موظف | شاشة **المستخدمين** ← جديد ← اسم مستخدم + كلمة أولية ← اربطه بالموظف ← حفظ | 10 حسابات فعّالة |
| ☐ | C3. توزيع الأدوار | لكل حساب: دور **مخازن** (من يستلم/يصرف)، **إنتاج** (من يخطط/ينفذ)، **جودة** (من يفحص)، **تقارير** (من يقرأ فقط) — لا تعطِ أحداً «مدير النظام» إلا أنت | مصفوفة الأدوار معبأة |
| ☐ | C4. العمليات الحساسة — **للخارج عن الدور فقط** | شاشة **الصلاحيات والأدوار** ← الدور الموثوق ← المورد «الاستلام» ← فعّل «تصحيح السلسلة» و«تصحيح كمية/عميل مستند معتمد» لمن تثق به **فقط** (افتراضياً: المدير وحده) | الموظف العادي **لا يرى** زرّي 🛠 و🔗 في الاستلام — جرّب بحساب مخازن عادي وتأكد بعينك |
| ☐ | C5. تجربة دخول لكل حساب | اخرج ← ادخل بكل مستخدم مرة ← تأكد أن شاشاته تظهر حسب دوره | 10/10 دخول ناجح |
| ☐ | C6. البيانات الأساسية | الأصناف ← وحدات القياس (القاعدة الذهبية: وحدة الصنف الرسمية) ← العملاء ← المخازن (WRM/WFG/WAUX/WTRT موجودة افتراضياً — تحقق) ← الورديات ← خطوط الإنتاج | القوائم المنسدلة كلها مملوءة |
| ☐ | C7. الأرصدة الافتتاحية (إن وُجد مخزون سابق) | سند استلام افتتاحي لكل دفعة قديمة ← اعتمده ← تحقق أن تقرير المخزون يطابق جردك الورقي | تقرير المخزون = الجرد |
| ☐ | C8. النسخ الاحتياطي | فعّل النسخ الاحتياطي التلقائي (AutoBackup) ← حدد مجلداً على **جهاز غير الخادم** إن أمكن ← نفّذ نسخة يدوية أولى | ملف نسخة بتاريخ اليوم موجود |
| ☐ | C9. اختبار استرداد (مرة واحدة) | استعد النسخة على قاعدة تجريبية وتأكد أن النظام يفتح عليها | النظام فتح وعرض البيانات |

---

## المرحلة (د) — يوم التشغيل الأول 🚀

| ✓ | البند | خطوات النقر |
|---|---|---|
| ☐ | D1. أول سند استلام **حقيقي** | شاشة الاستلام ← جديد ← العميل/الدفعة/الوزن ← حفظ ← **اعتماد** ← تحقق: الدفعة ظهرت، رصيد المخزون زاد، قيد وارد في تقرير الحركة |
| ☐ | D2. أول خطة إنتاج من الشحنة | التخطيط ← من الاستلام ← اعتمد (مستخدم **غير المنشئ** — Four-Eyes) |
| ☐ | D3. أول أمر إنتاج ← تنفيذ ← جودة ← تام | أمر من الخطة ← اعتماد ← بدء ← إنتاج ← إيقاف/إقفال اليوم بالتسوية ← محضر جودة ← استلام تام |
| ☐ | D4. أول تسليم عميل | تسليم ← اختيار من التام المتاح ← اعتماد ← تحقق نقص رصيد التام وزيادة المسلَّم |
| ☐ | D5. **تجربة تصحيح واحدة مقصودة** | افتح سند اليوم ← 🛠 تصحيح معتمد (أو إلغاء اعتماد) ← اكتب سبباً ← تحقق من القيد العكسي/قيد الفرق في تقرير الحركة وسجل التدقيق |
| ☐ | D6. مساءً: مراجعة سجل التدقيق | شاشة التدقيق ← راجع كل عمليات اليوم ← أي «محاولة مرفوضة» متكررة = موظف يحتاج تدريباً أو صلاحية |
| ☐ | D7. مساءً: فحص ذاتي + نسخة احتياطية | معلومات النظام ← فحص ذاتي (كل الأقسام ✓) ← تحقق من نسخة اليوم |

---

## المرحلة (هـ) — أسبوع المراقبة والتوسع إلى 10 📈

| ✓ | البند | التكرار |
|---|---|---|
| ☐ | E1. فحص ذاتي صباحي سريع (يشتغل تلقائياً مع الإقلاع — راجع `selftest.txt` إن ظهر خلل) | يومياً |
| ☐ | E2. تقرير حركة المخزون مقابل مستندات اليوم (عينات عشوائية) | يومياً |
| ☐ | E3. التحقق من ملف النسخة الاحتياطية | يومياً |
| ☐ | E4. سجل التدقيق: الأخطاء المتكررة والمحاولات المرفوضة | يومياً |
| ☐ | E5. تشغيل المستخدمين العشرة فعلياً على أجهزة الشبكة (كل جهاز: حزمة FULL + `update.bat` أو حزمة update إن كان مثبَّتاً سابقاً + توجيهه لخادم القاعدة) | اليوم 2–3 |
| ☐ | E6. قرار «جاهز إنتاجياً»: 5 أيام بلا إخفاق فحص عميق + بلا فرق مخزون | نهاية الأسبوع |

---

## 🆘 إن ظهر خلل — بروتوكول الإبلاغ (3 خطوات)

1. **شغّل الفحص الذاتي** (معلومات النظام ← فحص ذاتي) — هو يجيب سؤال «هل العطل من جهازي أم من البرنامج؟».
2. **انسخ الملف** `%\LocalAppData%\DateERP\logs\selftest.txt`.
3. أرسل الملف + اسم الشاشة + رقم المستند + لقطة الخطأ كما هي.

---

*هذه القائمة مبنية على الفحص الفعلي لكود الإصدار 1.50.60: الفحص الذاتي ستة أقسام (الاتصال، البيانات الأولية، الإعداد التشغيلي، اتساق البيانات، **الفحص العميق الجديد**، الشاشات والأزرار)، ويعمل تلقائياً عند كل إقلاع.*
