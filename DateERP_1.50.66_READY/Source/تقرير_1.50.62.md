# تقرير الإصدار 1.50.62 — الجودة المباشرة + قوالب ونسخ سندات

**التاريخ:** 2026-09-14
**الإصدار:** 1.50.62
**السابق:** 1.50.61

## ما تم تنفيذه

### 5) الجودة مباشرة من الجدول — تحقق فوري أحمر
- `QualityView.xaml.cs`:
  - `ItemRowUi.IsInvalid` = `|Total - ReceivedCartons| > 0.001`
  - `ValidationError` = "مجموع الصفات X لا يساوي المستلم Y"
  - `StandardUi.IsInvalid` = `Value < Min || Value > Max`
  - `CellEdited()` الآن ترفع `IsInvalid` و `ValidationError` مع `Total`
- `QualityView.xaml`:
  - `ResultsGrid` أضيف له `RowStyle` مع `DataTrigger IsInvalid=True → Background={StaticResource InvalidRowBrush}` + Tooltip
  - `CriteriaGrid` نفس الشيء
  - النتائج: أي صف مجموع صفاته لا يطابق المستلم يتحول أحمر فوراً (7-ز)
  - المعايير: أي قيمة خارج الحدود تتحول أحمر

### 8) قوالب ونسخ سندات سابقة
#### أ) قوالب خطط الإنتاج
- `PlanningService.cs`:
  - `GetTemplates()`: يرجع خطط `PlanType == "Template"`
  - `SaveAsTemplate(planId, templateName)`: ينسخ خطة كاملة كبنود قالب جديد برقم تلقائي
  - `CreateFromTemplate(templateId, newTitle)`: ينشئ خطة يومية جديدة من قالب مع تاريخ اليوم
- `PlanningView.xaml`: زرين جديدين:
  - "💾 حفظ كقالب" → يفتح `InputDialog` لاسم القالب → يحفظ
  - "📋 تحميل قالب" → يفتح `TemplatePickerWindow` يعرض القوالب → خيارين: إنشاء خطة جديدة من القالب أو استبدال بنود الخطة الحالية ببنود القالب
- `TemplatePickerWindow.xaml/.cs`: نافذة اختيار قالب مع جدول (رقم/اسم/بنود)

#### ب) نسخ من إيصال سابق
- `ReceivingView.xaml`: زر "📋 نسخ من إيصال سابق"
- `ReceivingView.xaml.cs`: `CopyFromPrevious_Click`:
  - يجلب آخر 20 إيصالاً
  - يفتح `ShipmentPickerWindow` لاختيار إيصال
  - ينسخ جميع بنوده إلى السند الحالي (مع المنتج/العبوة/العدد/الوزن)
- `ShipmentPickerWindow.xaml/.cs`: نافذة اختيار إيصال سابق

### تحسينات عامة
- `structural_checks.py` → PASS
- رقم الإصدار 1.50.61→1.50.62 في 5 مواقع

## الملفات المعدلة
- src/DatesErp.Desktop/Views/Screens/QualityView.xaml
- src/DatesErp.Desktop/Views/Screens/QualityView.xaml.cs
- src/DatesErp.Desktop/Views/Screens/PlanningView.xaml
- src/DatesErp.Desktop/Views/Screens/PlanningView.xaml.cs
- src/DatesErp.Application/Services/PlanningService.cs
- src/DatesErp.Desktop/Views/Screens/ReceivingView.xaml
- src/DatesErp.Desktop/Views/Screens/ReceivingView.xaml.cs
- src/DatesErp.Desktop/Views/TemplatePickerWindow.xaml/.cs (جديد)
- src/DatesErp.Desktop/Views/ShipmentPickerWindow.xaml/.cs (جديد)
- Version bump files

## الحزم
- `MfgSystem_1.50.62_FULL.zip`
- `MfgSystem_1.50.62_update.zip`

## خطوات الاختبار
1. الجودة: افتح فحص → عدل كمية صفة لتجعل المجموع ≠ المستلم → الصف أحمر + Tooltip يوضح الفرق.
2. المعايير: أدخل قيمة خارج [Min,Max] → الصف أحمر.
3. قوالب: افتح خطة → "حفظ كقالب" → اسم → حفظ → "تحميل قالب" → اختر → إنشاء جديدة → تفتح الخطة الجديدة بنفس البنود.
4. نسخ إيصال: استلام → جديد → "نسخ من إيصال سابق" → اختر إيصال → بنوده تنزل فوراً.

## روابط التنزيل (بعد الرفع)
- FULL: `https://github.com/salahaaa/OFFTT/raw/arena/01a091dd-offtt/MfgSystem_1.50.62_FULL.zip`
- UPDATE: `https://github.com/salahaaa/OFFTT/raw/arena/01a091dd-offtt/MfgSystem_1.50.62_update.zip`
