# لماذا المشاكل لا تزال نفسها؟ — الحل النهائي 1.50.67

**تاريخ:** 2026-09-17
**السبب الجذري:** أنت تختبر **البرنامج المنصب القديم** في `publish\MfgSystem.exe` الذي تاريخه قبل 2026-09-15، بينما كل إصلاحاتنا موجودة فقط في **السورس** على GitHub. نسخ ملفات `.cs` فقط لا يكفي — يجب **إعادة بناء** `dotnet publish` ثم نسخ الناتج فوق `publish`.

---

## الصورة 1: خطأ النسخ الاحتياطي قبل الترحيل

```
Cannot open backup device 'C:\Users\LENOVO\Documents\DateERP\Backups\DateERP_PreMigration_...bak'. Operating system error 5 (Access is denied)
```

**السبب:** SQL Server يعمل بحساب `NT Service\MSSQLSERVER` لا يملك صلاحية الكتابة في مجلد المستندات الخاص بك.

**الحل في الكود الجديد `AutoBackup.cs` (commit aa989be + e519dd4):**
- `ResolveWritableBackupFolder()` يجرب 5 مجلدات: `CommonDocuments`, `C:\SQLBackups\DateERP`, `C:\Temp\DateERP\Backups`, `Temp\DateERP\Backups`, ثم `Documents`
- يختبر الكتابة قبل النسخ
- إذا فشل، يعرض رسالة واضحة مع 3 حلول ويخيرك: **اضغط "نعم" للمتابعة بدون نسخة** أو "لا" للإلغاء

**ماذا تفعل الآن (حتى بدون تحديث):**
- في النافذة التي تظهر، اضغط **"نعم"** — سيتخطى النسخ الاحتياطي ويكمل الترحيل
- أو أنشئ مجلد `C:\SQLBackups\DateERP` وأعطه صلاحية Everyone

**بعد التحديث:** لن تظهر الرسالة إلا نادراً، وإن ظهرت ستجد مجلد بديل تلقائياً

---

## الصورة 2: خطط الإنتاج تظهر مشوهة (ط§طھط§ط·)

**السبب:** ملفات XAML القديمة بدون BOM + بناء قديم + خط `FlowDirection` أو خط لا يدعم العربية

**الحل في الكود الجديد:**
- إضافة **UTF-8 BOM** لكل 52 ملف XAML — يضمن قراءة العربية صحيحة
- `PlanningView.xaml` تم إرجاعه للتصميم السابق البسيط المتفق عليه + إصلاح الوحدات
- `UnitDisplay` الآن `كرتون (2.0 كجم)` بدل `كجم` فقط

**إذا لا تزال مشوهة بعد التحديث:**
1. تأكد أنك بنيت من السورس الجديد (تاريخ DLL اليوم)
2. احذف مجلد `publish` القديم كاملاً وانسخ الجديد
3. تأكد أن نظام Windows لديه خطوط عربية (Segoe UI)
4. شغل `MfgSystem.exe` مباشرة وليس من داخل ZIP

---

## الصورة 3: مشاكل التنصيب + تسميات XAML/C#

**السبب:** مراجع قديمة `SaveActionBtn`, `HoldRadio`, `ApproveRadio` في `PlanningView.xaml.cs` بينما XAML القديم لا يحتويها

**الحل:**
- `PlanningView.xaml` الحالي في GitHub يحتوي كل الأسماء: `SubmitBtn`, `ReturnBtn`, `ApproveRadio`, `HoldRadio`, `SaveActionBtn`, `EditRowBtn`
- `PlanningView.xaml.cs` تم تغيير `_currentPlanId` من `int` إلى `int?` لإصلاح CS0472
- أداة تحقق `tools/ci/verify_xaml_names.py` تمنع تكرار المشكلة — يجب 0 أخطاء قبل Publish

---

## لماذا لم تجد جديد؟

لأنك نسخت ملفات `.cs` فقط إلى `Source` لكنك **لم تنفذ `dotnet publish`** أو نسخت فوق `publish` القديم بدون حذف.

**الدليل:**
```bat
dir "C:\Users\LENOVO\.gemini\antigravity-ide\scratch\DateERP\publish\DatesErp.Desktop.dll"
→ تاريخه 2026-09-15 (قديم)
dir "C:\Users\LENOVO\.gemini\antigravity-ide\scratch\DateERP\FULL_EXTRACTED\...\Source\src\DatesErp.Desktop\Views\Screens\PlanningView.xaml.cs"
→ تاريخه اليوم (جديد)
```
DLL قديم + سورس جديد = المشاكل تبقى

---

## الحل النهائي — خطوة بخطوة (انسخ والصق):

### 1) حمّل الحزمة الكاملة الجديدة (آخر commit aa989be):

```
https://github.com/salahaaa/OFFTT/archive/refs/heads/arena/01a091dd-offtt.zip
```

### 2) فك الحزمة واستبدال السورس:

```bat
REM احذف السورس القديم
rmdir /s /q "C:\Users\LENOVO\.gemini\antigravity-ide\scratch\DateERP\FULL_EXTRACTED\DateERP_1.50.66_FULL_200MB\Source\src"

REM انسخ الجديد
xcopy "C:\Users\LENOVO\Downloads\OFFTT-arena-01a091dd-offtt\src\*" "C:\Users\LENOVO\.gemini\antigravity-ide\scratch\DateERP\FULL_EXTRACTED\DateERP_1.50.66_FULL_200MB\Source\src\" /E /Y /I
```

### 3) فحص قبل البناء (يجب 0 أخطاء):

```bat
cd /d "C:\Users\LENOVO\.gemini\antigravity-ide\scratch\DateERP\FULL_EXTRACTED\DateERP_1.50.66_FULL_200MB\Source"
python tools\ci\verify_xaml_names.py
REM يجب أن يطبع: ✅ فحص XAML/C# — لا يوجد مراجع لأسماء غير موجودة
```

### 4) بناء جديد:

```bat
dotnet publish src\DatesErp.Desktop\DatesErp.Desktop.csproj -c Release -r win-x64 --self-contained true -o "C:\Users\LENOVO\.gemini\antigravity-ide\scratch\DateERP\publish_NEW_1.50.67"
```

إذا ظهر خطأ Build، أرسل لي نص الخطأ كاملاً

### 5) نسخ فوق التشغيل:

```bat
REM أغلق النظام أولاً
rmdir /s /q "C:\Users\LENOVO\.gemini\antigravity-ide\scratch\DateERP\publish"
mkdir "C:\Users\LENOVO\.gemini\antigravity-ide\scratch\DateERP\publish"
xcopy "C:\Users\LENOVO\.gemini\antigravity-ide\scratch\DateERP\publish_NEW_1.50.67\*" "C:\Users\LENOVO\.gemini\antigravity-ide\scratch\DateERP\publish\" /E /Y /I
```

### 6) تشغيل:

```bat
"C:\Users\LENOVO\.gemini\antigravity-ide\scratch\DateERP\publish\MfgSystem.exe"
```

- عند رسالة النسخ الاحتياطي اضغط **نعم**
- افتح خطط الإنتاج → يجب أن تظهر عربية صحيحة + وحدة "كرتون (2 كجم)" + لا حفظ تلقائي وهمي

---

## إذا لا يزال لا يعمل:

1. احذف كل شيء في `publish` و `publish_NEW` وابدأ من جديد
2. تأكد أن `dotnet --version` يطبع 8.x
3. أرسل لي لقطة شاشة لـ `dir publish\*.dll` يظهر تاريخ الملفات
4. أرسل لي نص خطأ `dotnet publish` كاملاً

**روابط مباشرة لآخر ملفات:**
- https://raw.githubusercontent.com/salahaaa/OFFTT/arena/01a091dd-offtt/src/DatesErp.Desktop/Services/AutoBackup.cs
- https://raw.githubusercontent.com/salahaaa/OFFTT/arena/01a091dd-offtt/src/DatesErp.Desktop/Views/Screens/PlanningView.xaml
- https://raw.githubusercontent.com/salahaaa/OFFTT/arena/01a091dd-offtt/src/DatesErp.Desktop/Views/Screens/PlanningView.xaml.cs
- https://raw.githubusercontent.com/salahaaa/OFFTT/arena/01a091dd-offtt/src/DatesErp.Desktop/Mvvm/PlanningRows.cs
