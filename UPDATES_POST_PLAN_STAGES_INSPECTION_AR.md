# تقرير الفحص المعمق الشامل — المراحل بعد خطط الإنتاج (إصدار 1.50.71)

**تاريخ الفحص:** 18/09/2026
**الفرع:** `arena/01a0ac34-offtt` (HEAD = `94fa565`)
**نطاق الفحص:** كل مرحلة تالية لخطط الإنتاج فحصاً سطر‑ب‑سطر:

| المرحلة | الخدمات | الشاشات |
|---|---|---|
| أوامر الإنتاج (إصدار/تسجيل الفعلي) | `ProductionOrderService.cs` (1089) + `.Today.cs` (289) | `ProductionDeliveryView`، `OrderDetailWindow` |
| التنفيذ وإقفال اليوم + الجودة | `ExecutionService.cs` (986: CloseProductionDay + QualityService + PlanSync) | `QualityView`، `QCWindow` |
| بوابة الجودة | `QualityGate.cs` (155) | — |
| الإنتاج التام | `FinishedGoodsService.cs` (420) | `FinishedGoodsView`، `FGReceiveView` |
| تسليم العملاء | `CustomerDeliveryService.cs` (332)، `DeliveryPickService.cs` (167) | `DeliveryView`، `DeliveryReviewWindow` |
| إقفال الخطط + مزامنة التقدم | `PlanClosureService.cs` (270)، `PlanProgressService.cs` (416) | `PlanClosureView`، `PlanDetailWindow` |
| الأساس المشترك | `ServiceBase.cs` (400)، `UnitsPolicy.cs`، `CartonService.cs` (224)، `InspectionService.cs` (764) | `MyTasksView` |

**الإجمالي:** ~5,700 سطر خدمات + ~2,900 سطر واجهات — تمت القراءة الكاملة لخدمات: ProductionOrderService(+Today)، ExecutionService، ServiceBase، QualityGate، FinishedGoodsService، CustomerDeliveryService، ProductionDeliveryService(+Actual)، PlanClosureService، PlanProgressService، CartonService، DeliveryPickService، InspectionService. وقراءة شاملة للشاشات السبع والنوافذ الأربع.

---

## 🔴 P1 — حرج (خطر تجاوز السيطرة أو فقدان بيانات للمستخدم)

### P1-1 — تجاوز بوابة الجودة في سندات التسليم التي تجمع أوامر متعددة («تسليم كامل المتاح» مسار افتراضي)

**الموقع:** `CustomerDeliveryService.cs` L176 (البوابة)، L221 (سقف الكمية)، L258+ (مزامنة الخطة)
**الموقع المرتبط:** `DeliveryView.xaml.cs` L347–361 (اشتقاق `orderId` من أول دفعة)

```csharp
// L176 — داخل foreach لكل بند:
var (ok, reason) = QualityGate.CustomerDeliveryAllowed(Db, dlv.OrderId, item.LotId, item.ProductId);
```

**المشكلة:** بوابة الجودة المركزية تستقبل `dlv.OrderId` (أمر واحد في رأس السند، مشتق في الواجهة من **أول دفعة** فقط). في `QualityGate.CustomerDeliveryAllowed` (QualityGate.cs L28–40): إن وُجد `orderId` فيُفحص **أمر واحد فقط**، والاشتقاق من الدفعة (`lotId`) يُستخدم **عند غياب** `orderId` حصراً.

**السيناريو الواقعي (ليس حالة نادرة):** زر «تسليم كامل المتاح» في شاشة التسليم (`DeliverAll_Click`) يضيف **كل** أرصدة العميل في مخزن التام — أي كل الدفعات من **كل** أوامره (شائع جداً في الخطط الطويلة متعددة الأيام). عند الحفظ يُشتق `orderId` من أول دفعة فقط. النتيجة عند الاعتماد:

1. **بوابة القرار** تفحص فحوصات الأمر الأول فقط: بند تابع لأمر **فحصه مرفوض** أو **لا يوجد له فحص أصلاً** يمرّ إذا كان أمر الرأس معتمداً «مطابق».
2. **سقف «المطابق المعتمد»** (`CustomerDeliveryQtyAllowed` L100–109) يحسب نطاقاً من أمر الرأس فقط؛ للصنف المختلف عن بنود الفحص يتجاوز السقف **بصمت** (تعليق L98: «يُتجاوز بصمت عند غياب بنود فحص»).
3. **مزامنة الخطة** (L258+): `planId` يُشتق من `dlv.OrderId` ل**كل** البنود؛ ما أُنشئ من أمر آخر يُسجَّل على خطة الرأس إن وُجد صنف مطابق فيها (تلوث تقدم خطة)، أو يرفض الاعتماد كله بـ `PLAN_OVERFLOW` إن لم يوجد.

**الأثر:** البضاعة المرفوضة/غير المفحوصة تُسلَّم للعميل + أرصدة الخطط تُلوَّث — في المسار الافتراضي للشحنات متعددة الأوامر. هذا بالضبط الصنف من الثغرات التي وثّقتها التعليقات السابقة في الكود («أُثبت بالتشغيل أن دفعة مرفوضة تماماً سُلّمت للعميل» L172) لكن الإصلاح أغلق تجاوز الـ `null` دون غلق تعدد الأوامر.

**الإصلاح المقترح (دقيق وغير متسرع):**
- في L176: `QualityGate.CustomerDeliveryAllowed(Db, null, item.LotId, item.ProductId)` — البوابة صُممت أصلاً للاشتقاق من الدفعة عند غياب المعرّف، فيُفحص **أمر كل بند** على حدة.
- نفس المنطق لسقف الكمية: نطاق السقف = أمر الدفعة للبند لا أمر رأس السند.
- مزامنة الخطة: خطة كل بند من دفعته (`lot → order → SourcePlanId`)، وخطة رأس السند بديلاً فقط للbens المرتبطة فعلاً بها؛ الفائض يبقى `PLAN_OVERFLOW` صريحاً.

---

### P1-2 — «تسليم نصف/جزئي» للإنتاج التام يُستبدل بصمت بالتسليم الكامل

**المواقع:**
- `FinishedGoodsView.xaml.cs` L202 (`PackageCount = i.Packages`) + عمود «العبوات» `IsReadOnly=True` (XAML L47) + زر «نصف» L179
- `FGReceiveView.xaml.cs` L183 (`Packages = oi.ProducedCartons`) + L212 (`PackageCount = l.Packages`) مع عمود «كمية السند (كجم)` قابل للتحرير (XAML L54)
- `UnitsPolicy.cs` L155–174 (`EnsureCartonKgConsistency`)
- `FinishedGoodsService.cs` L91 (استدعاء EnsureCartonKgConsistency داخل SaveReceipt)

**سلسلة الأحداث:**
1. المستخدم يضغط «نصف» (أو يكتب كجم جزئياً) ← `DeliverQty = 3750 كجم` بينما `Packages = 1000` (كامل المنتَج — العمود للقراءة فقط).
2. الخدمة تستدعي `EnsureCartonKgConsistency(netWeightKg: 3750, cartons: 1000)` — وقاعدتها (§1.50.68): **عند التصادم، الكراتين هي الصحيحة** وتُصحَّح الكيلو تلقائياً:
   ```csharp
   double computed = Math.Round(cartons * weight, 1); // 1000 × 7.5 = 7500
   if (Math.Abs(qtyKg - computed) > tolerance) return computed; // ← 7500!
   ```
3. **النتيجة:** سند التسليم يصبح 1000 كرتون / **7500 كجم** — اختيار «نصف» المستخدم **يُلغى بصمت** دون خطأ ودون تنبيه (تنبيه B86/H7 للكراتين لا ينطلق لأن 1000 = المتبقي المنتَج تماماً).

**الأثر:** مستند مخزني/مالي يُوَرَّد كامل الإنتاج بينما المستخدم يعتقد أنه أمر نصفه. لا خطأ، لا تحذير — استبدال صريح لإدخال المستخدم.

**إصلاح إضافي مرتبط (نفس الجذر):** بعد استلام جزئي لسند كامل، حساب «المتاح» في `Order_Changed` (FinishedGoodsView L149–151) و`Download` (FGReceiveView L168–170) = `Σ NetWeightKg` عبر **كل** السندات (بلا فلتر حالة) ← يظهر متاح = 0، فيتعذّر إنشاء سند تابع من الواجهة (المسار الوحيد: مواصلة الاستلام على نفس السند — يعمل، لكن العرض خادع، والسند الملغى/المعكس يستمر بحجز كامل كميته).

**الإصلاح المقترح:**
- الواجهة تُرسل كراتين **تناسبية** مع الكمية المختارة: `PackageCount = round(DeliverQty / Weight × Packages)` (مع حارس `Weight > 0`) — فتتسق البند وتعمل القاعدة كما صُممت.
- حساب «المتاح» = `Σ ReceivedQtyKg` (ما دخل التام فعلاً — يتطابق مع سقف الخدمة) بدل `Σ NetWeightKg` الخام.

---

## 🟠 P2 — عالٍ

### P2-1 — عمودا «تاريخ استلام التام» و«مدة البقاء» في قائمة الالتقاط ميّتان + ترتيب FIFO مكسور

**الموقع:** `DeliveryPickService.cs` L56

```csharp
where r.WarehouseId == whFg && r.Status == DocStatuses.Approved
```

سندات الاستلام التام (`FinishedGoodsReceipt`) تمرّ بحالات: `Draft → Issued → Completed` (FinishedGoodsService: SaveReceipt/Issue/Receive) — **لا تصبح أبداً `Approved`** (هذه ثابتة مختلفة في `DocStatuses`، BaseEntity.cs L47/L54). لذلك:
- `fgIn` دائماً فارغ ← عمود «تاريخ الاستلام من التام» = «—» دائماً، «مدة البقاء» = 0 دائماً.
- ترتيب FIFO (`OrderBy(FgReceiptDate)` L152) يفشل مفتاحه الأول دائماً ويتدهور إلى «الأقدم إنتاجاً» فقط — مخالفة لقاعدة §2 «الأقدم استلاماً أولاً».

**الإصلاح:** `r.Status == DocStatuses.Completed` (أو `r.IsApproved == true`).

### P2-2 — تطابق نصي صارم `CheckType == "نهائي"` لا ينطبق على أي فحص فعلي

**المواقع:** `DeliveryPickService.cs` L82، `ReportService.cs` L369
**قيم الفحص الفعلية:** `ExecutionService.cs` L509 = `"نهائي — بعد التبريد (يومان)"` (الفحص التلقائي عند إقفال اليوم)، و`InspectionService.SaveDeliveryCheck` يمرر `"نهائي — بعد التبريد"`، و`QCWindow` يحفظ النوع الأصلي للفحص (أي من القيمتين السابقتين). `SaveCheck` (ExecutionService L641) يسقط إلى `"نهائي"` فقط عند `null` — لا يحدث عملياً.

**الأثر:** `decisions` دائماً فارغ في:
- قائمة الالتقاط: عمود «الصفة/الدرجة» = «بانتظار الفحص» **دائماً** حتى لو اعتمد الفحص النهائي (بينما نفس الشاشة تعرض «✔ معتمد» الصحيح في جدول الرصيد عبر `QualityGate.DeliveryReadiness` — تعارض عرض داخل شاشة واحدة).
- تقرير تسليمات العملاء: أعمدة الصفة/القرار لا تملأ.

**الإصلاح:** الفلترة بـ `c.IsApproved` (العلامة الفعلية التي يضعها `ApproveCheck`) مع شرط اختياري `CheckType` يحتوي «نهائي»، بدل التطابق الحرفي. (نفس نمط «الثوابت المثبتة» الذي سبب أزمة الإصدار 1.50.67.)

### P2-3 — تسويات الأصناف المساعدة (B95) تكتب الرصيد مباشرة: يمكن أن يسلب WAUX

**الموقع:** `ExecutionService.cs` — قسم موازنة الأصناف المساعدة في `CloseProductionDay`

المطابقة الفعلية (الفرق بين المصروف والمحسوب) تُقيَّد بـ:
```csharp
Db.InventoryTransactions.Add(...);
bal.QtyKg += diff;   // مباشر — لا PostStockMovement
```
أي **دون** حارس الرصيد السالب وحارس `DUPLICATE` اللذين يحميان كل الحركات الأخرى عبر `PostStockMovement` (ServiceBase.cs). فرق سالب كبير (تبديد/سرق/تسجيل خاطئ) يجعل رصيد WAUX سالباً بصمت.

**الإصلاح:** تمرير التسوية عبر `PostStockMovement` بحالة `Adjustment` (المعفاة من حارس السالب حسب تصميمه الحالي) **مع** فحص صريح يرفض السالب ويطلب تسوية مصرحة — أو إبقاء المسار المباشر مع حارس `bal.QtyKg + diff < 0 → رفض`.

### P2-4 — قرار الفحص («Passed») لا يُطابق مع النتائج: مرفوضات محفوظة تحت قرار «مطابق»

**المواقع:**
- `QualityView.xaml.cs` L287: بدون تحديد أزرار القرار ← `"Passed"`.
- `QCWindow.xaml` L135: عنصر «مطابق» مع `IsSelected="True"` مسبقاً.
- `InspectionService.cs` L671: أي قيمة غير صالحة ← `"Passed"` **بصمت**.
- `QCWindow.xaml.cs` L223: تعبئة تلقائية `AcceptedCtn = remainingCtn` (كل المتبقي مقبول).

**الأثر:** فحص فيه كراتين مرفوضة يُحفظ بقرار «مطابق — مقبول للإفراج» دون أي تحذير (المستخدم يتجاهل أزرار القرار ويحفظ). السجل يقول «Passed» بينما النتائج تفصيلياً تحتوي مرفوضاً. الأرصدة محمية (سقف التسليم يحسب «المقبول المعتمد» فقط، والمرفوض لا يدخل التام) لكن **المحضر القانوني مضلل** وبوابة القرار تعتمد على هذا الحقل.

**الإصلاح:** في `SaveDeliveryCheck`/`SaveCheck`: إن `RejectedCartons > 0` و`Decision == "Passed"` → طلب تأكيد صريح أو رفض، وعدم إسقاط القيم غير الصالبة إلى «Passed» بصمت (رفض مع رسالة).

---

## 🟡 P3 — متوسط

### P3-1 — صفوف «اختيار الدفعة» في شاشة التسليم: وزن الكرتون مفقود ← تعديل الكراتين يصفّر الوزن

**الموقع:** `DeliveryView.xaml.cs` L505 (`OpenBatchPicker`)

```csharp
row.UnitWeight = 0; // «سيُحدَّث من بطاقة الصنف» — لكن لا يوجد من يحدّثه!
// row.CartonWeight لا يُملأ إطلاقاً — بينما BatchRow.CartonWeight متوفر (FinishedBatchPickerWindow L35)
```
عند تعديل خلية «الكراتين *» في مثل هذا الصف (`CellEditEnding` L79–88 وsetter الخاصية `Packages`):
```csharp
double w2 = row.UnitWeight > 0 ? row.UnitWeight : (row.CartonWeight > 0 ? row.CartonWeight : 0);
row.Qty = w2 > 0 ? Math.Round(row.Packages * w2, 1) : 0; // ← 0
```
الوزن يصبح **0** بصمت → عند الحفظ: إما رفض السند كله برسالة «الكمية يجب أن تكون أكبر من صفر» لا تدل على السبب، أو استبعاد الصف (انظر P3-3).

**الإصلاح (سطر):** `row.CartonWeight = batch.CartonWeight; row.UnitWeight = batch.CartonWeight;`

### P3-2 — رسالة نجاح «تم اعتماد الجودة» في سندات التام تُقال حتى والفحص معلّق

**الموقع:** `FinishedGoodsService.cs` L181–185

التصميم (§التدفق: تسليم التام مسموح بعد الإقفال مع فحص معلّق — فترة التبريد يومان) يسمح بإنشاء السند مع `coolingPending = true`، لكن رسالة النجاح المباشرة تقول دائماً **«تم اعتماد الجودة، جاهز للاستلام»**. والمتغير `coolingNote` (L181) **ميت** — أُعلن ولم يُستخدم أبداً (قصد واضح لم يكتمل).

**الإصلاح:** بناء الرسالة من `coolingPending`: «الفحص قيد التبريد (يومان) — التسليم للتام مسموح، وتسليم العميل ينتظر الاعتماد».

### P3-3 — حفظ سند التسليم يستبعد الصفوف غير المكتملة **بصمت**

**الموقع:** `DeliveryView.xaml.cs` L347

```csharp
var validItems = _items.Where(r => !r.IsEmptyRow && r.ProductId != 0 && r.Packages > 0).ToList();
```
صفّ فيه صنف بلا كراتين (أو كراتين صفرية بعد P3-1) **يختفي من السند المحفوظ دون أي تنبيه** — المستخدم يرى 3 صفوف ويحفظ سندق بندين.

**الإصلاح:** إن استُبعد أي صف غير فارغ → رسالة تحدد الصنف/الدفعة وسبب الاستبعاد قبل الحفظ.

### P3-4 — مساران للصرف المساعد (قديم + جديد) يمكن أن يصرفا نفس الصنف مرتين

**الموقع:** `ProductionOrderService.cs` `CalculateMaterials` (~L210–270)

`aggOld` من `ConsumptionFormulas` (النظام القديم) و`aggNew` من `ProductAuxiliaryRequirements` (§1.50.63) يُحفظان **معاً** كبنود `ProductionOrderMaterial` بدون أي مطابقة/دمج بين المسارين. إن عرّف المستخدم الصنف المساعد نفسه في النظامين (حالة انتقال شائعة) ← بندان لنفس الصنف ← **صرف مزدوج** عند تحرير الأمر.

**الإصلاح:** عند بناء `aggNew`، استبعاد الأصناف الموجودة في `aggOld` (أو تحذير صريح عند الازدواج).

### P3-5 — فخ وسم «الكمية لكل كرتون» في شاشة الـ BOM مع طريقة «لكل كجم»

**الموقع:** `ProductBOMView.xaml` L32 (الوسم ثابت «الكمية لكل كرتون *») + L38–39 (الخيارات: لكل كرتون/لكل كجم)؛ الحساب في `ProductionOrderService` `case "PerKg": qty = PlannedQtyKg × r.QtyPerCarton`

الحساب نفسه صحيح (الحقل يحمل «لكل وحدة طريقة الحساب»)، لكن **الوسم لا يتغير** مع طريقة الحساب. مستخدم يتبع الوسم ويختار «لكل كجم» ويُدخل قيمة «لكل كرتون» (مثلاً 0.16 كجم لكل كرتون 8كجم بدل 0.020 لكل كجم) ← **صرف مفرط ×8** للأصناف المساعدة بصمت.

**الإصلاح:** تغيير الوسم ديناميكياً حسب الاختيار («الكمية لكل كجم») + مثال توضيحي، أو حقلان منفصلان (PerKg/PerCarton).

### P3-6 — إقفال اليوم: الخام المستهلك «بالمخطط» عند غياب الفعلي (مكتوب في الكود لكنه انحراف صامت)

**الموقع:** `ExecutionService.cs` `CloseProductionDay`

```csharp
consumed = consumedRawKg > 0 ? consumedRawKg : plannedRaw;
```
التعليق يوثق أنه مقصود، لكن عملياً: نسيان إدخال «الخام المستهلك فعلياً» يرحّل **المخطط** في دفتر الخام — انحراف تراكمي في إحصاءات الاستهلاك/التكلفة دون أي أثر على السند يميزه (لا وسم «مُقدَّر/مخطط»).

**الإصلاح المقترح (بدون تغيير السلوك):** وسم الحركة/الحقل عند الإسقاط (مثل `notes: "مُرحَّل بالقيمة المخططة"`) + تنبيه في شاشة التنفيذ عند الإقفال بدون فعلي.

---

## 🔵 P4 — منخفض / تجربة استخدام

| # | البند | الموقع | الوصف |
|---|---|---|---|
| P4-1 | **حفظ تلقائي متبقٍ في شاشة التسليم** | `DeliveryView.xaml.cs` `AutoSaveDraft/TryRestoreAutoSave` + `DeliveryDraft_*.json` | النمط نفسه الذي أزيل من شاشة الخطط (المسودات الوهمية): هنا لا يخلق مستندات في القاعدة (يستعيد صفوف الإدخال فقط) لكن الاستعادة بعد إغلاق/انقطاع تملأ الشاشة بصفوف «مجهولة المصدر». غير متسق مع قرار الإزالة السابق — يُفضّل حذفه أو وسمه بوضوح «مسودة غير محفوظة من …». |
| P4-2 | **استثناء عام بدل رسالة صديقة عند فتح سند** | `DeliveryView.xaml.cs` `OpenDelivery` (رمي `InvalidOperationException` عند وزن كرتون غير معرف) | يظهر في مربع الخطأ العام (HandleException) بدل رسالة «عرّف وزن الكرتون للصنف X». |
| P4-3 | **تعبئة تلقائية «كل المنتَج مقبول» في فحصين** | `QualityView.xaml.cs` L172، `QCWindow.xaml.cs` L223 | أبسط مسار = قبول كامل بلا ملاحظة (والقرار الافتراضي «Passed» أيضاً). مقصود كتبسيط، لكن يُنصح بتمييز القيم المعبّأة مسبقاً بصرياً حتى لا تُحفظ دون تدقيق. |
| P4-4 | **بلا سقف عميل في إدخال الفحص** | `QCWindow` `Save_Click` | يُقبل إدخال `AcceptedCtn` أكبر من `RemainingCtn`؛ الرفض يأتي من خدمة الفحص برسالة تغطية (صحيحة لكن غير بديهية). فحص محلي يسبق: `AcceptedCtn + RejectedCtn ≤ RemainingCtn`. |
| P4-5 | **`MarkInvoiced` بلا معاملة/قفل** | `PlanProgressService.cs` L402–413 | قراءة-ثم-كتابة بدون Transaction: تنافس نظري (سطح مكتب أحاد المستخدم → منخفض). كما أن إلغاء الاعتماد يصفّر `InvoicedQtyKg` بصمت — يُفضّل تدقيق (Audit) لعكس الفوترة لأغراض مالية. |
| P4-6 | **رصيد «كجم فقط» بلا وزن كرتون لا يظهر في التسليم** | `DeliveryPickService.cs` `cartons = … Round(CartonsOf(QtyKg, ctnW))` | رصيد بلا `PackageCount` وبلا وزن كرتون معرّف → صف بكراتين 0 → غير قابل للتوزيع (التسليم بالكرتون). حد نادر لكنه «رصيد خفي» — يُفضّل تنبيه في شاشة الرصيد. |
| P4-7 | **«مدة البقاء» و«الصفة» في نافذة مراجعة التسليم** | `DeliveryReviewWindow` (تغذية من DeliveryPickService) | ترث عيبَي P2-1/P2-2 حتى إصلاحهما. |

---

## ✅ ما فُحص ووجد **متيناً** (ملخص موجز لكل مرحلة)

**الأساس المشترك (ServiceBase):**
- `RunInTransaction`: معاملات ذرية مع إعادة محاولة لتصادم الفريد (2627/2601/sqlite-19) واختياري deadlock وrollback + `ChangeTracker.Clear`.
- `PostStockMovement`: حارس `DUPLICATE` (مستند+نوع+مخزن+صنف+خام+دفعة+عميل+عبوة)، حارس الرصيد السالب (Adj استثناء)، حارس `CUSTOMER_MIX`.
- `ConsumeLot`: حارس الدفعة السالبة + `GuardTreatedStock` (TreatmentReady − Produced) كشبكة أمان نهائية.

**أوامر الإنتاج:**
- `SaveTodayGroup`: إصدار صارم من الخطة فقط (هوية مطابقة + كمية دقيقة)، معاملة Serializable، فشل نظيف عند 1205.
- `CloseOrder`/`CancelOrder`/`StopOrder`/`DeleteOrder`: حراس دورة الحالة كاملة؛ `OrderHasIrreversibleExecution` يمنع الحذف/الإلغاء بعد التنفيذ؛ سبب إلزامي ≥10 أحرف للإيقاف.
- `ReverseConsumption`: إصلاح السابق **مؤكد** (عكس القيد لا الحذف).

**التنفيذ/إقفال اليوم (CloseProductionDay):**
- رفض الإقفال بدون إنتاج (B95)، أسقف M13 لكل بند (كجم **و** كراتين معاً)، تتبع مالك الدفعة (B102)، فحص انحراف الغلة (تنبيهي — مقصود لماء التشغيل)، إنشاء فحص تلقائي بتاريخ متوقع +2 أيام (فترة التبريد)، إغلاق تلقائي للخطة المكتملة، إرجاع «المتبقي في القاعة» للدفعة الأصلية (علامة `carryToNextDay` للعرض فقط — موثق).

**الجودة (QualityService + InspectionService + QualityGate):**
- `SaveDeliveryCheck`: **تساوي صارم** (مجموع صفات كل صنف = المستلم للفحص)، مرفوضات سالبة، صفات خارج المنتج مرفوضة (الثانوي/الفاقد خارج الشاشة)، معادلة الكرتون/كجم تلقائية.
- `SaveCheck`: معادلة checked = accepted + rejected (كجم وكراتين)، **منع التغطية المزدوجة** (الفحوص السابقة تُحسب)، سماحية فحص ثانٍ جزئي، أسقف ضد المنتَج.
- `ApproveCheck`: تغطية كاملة **بإعادة احتساب حية** (الفحص الثاني المكمل يسمح باعتماد الأول)، حالة `Completed` إلزامية.
- `RequestCorrection`: صلاحية + سبب مكتوب + تدقيق.
- البوابة: قرار واعية (Rejected/Quarantine تمنع)، الاشتقاق من الدفعة عند غياب الأمر (لا تجاوز بـnull)، سقف «المطابق المعتمد» للتسليم. *(إلا ثغرة تعدد الأوامر P1-1.)*

**الإنتاج التام (FinishedGoodsService):**
- `SaveReceipt`: سقف المنتَج (كجم) بحصة «المستلم فعلاً» (المسودات لا تحجب بعضها، الملغاة لا تحجز) + تنبيه كراتين (B86/H7) + حارس هوية الدفعة (لا استبدال) + استبعاد العملاء مشتق من ملكية السطر ثم الأمر + حارس تكرار (صنف+دفعة) داخل السند.
- `Receive`: سقف المتبقي لكل بند، سقف المنتَج **يُعاد فحصه عند الاستلام** (مساران مباشر+مربوط معاً)، **تقيد الكراتين تناسبياً مع الوزن** (`pkgRecv = recv/NetWeightKg × PackageCount`)، ترقيم `RCV#n` لكل متابعة، إغلاق تلقائي للأمر عند الاكتمال + `PendingDelivery` للثابت.
- `Unapprove`: **قيد عكسي** `#REVn` (لا حذف دفتر — إصلاح تصادم البادئات 1000/100 موثق)، تصفير `ReceivedQtyKg`، إعادة فتح الأمر/أمر التسليم المربوط بدقة.
- `Issue`: بلا أثر على الأرصدة + بوابة صلاحية.

**تسليم العملاء (CustomerDeliveryService + DeliveryPickService):**
- `Approve`: البوابة **داخل المعاملة** لكل بند، رفض الصفر/السالب، `LOT_REQUIRED` عند تعدد الدفعات، الرصيد بالسند + مفتاح الرصيد الكامل (مخزن+صنف+دفعة+عميل+عبوة)، `#Rn` لإعادة الاعتماد بعد الإلغاء.
- `Unapprove`: `#REVn` تراكمي + فك المزامنة LIFO + تصفير المفوتر.
- `Update/DeleteDraft`: مسودة فقط (معتمد = مقفل).
- `AllocateFifo`: بالكرتون، يحترم `PackagingTypeId` (4كجم لا يختلط بـ8كجم)، كل سطر بدفعته (لا دمج/فقدان تتبع)، الزائد = رفض صريح بالمتاح والمطلوب.

**إقفال الخطط (PlanClosureService + PlanProgressService):**
- `GetInfo`: حواجز كاملة (أوامر مفتوحة/قيد/مكتملة-غير مقفلة + بنود خطة غير مصدرة، الملغاة تُشمَّ فقط بعلامة `[إلغاء` موثقة).
- `ClosePlanFinal` (قوة): سبب مكتوب + تدقيق. `ReopenPlan`: صلاحية + سبب + تدقيق + تحرير حجزات الدفعات. معاملة Serializable.
- `UpdatePlanItemCore`: **كل الفحوص قبل كل الإسنادات** (إصلاح تسرب ChangeTracker موثق)، تحديث `ReservedQtyKg` عند تغيير الكمية (إصلاح تسرب الحجز)، إعادة فحص الطاقة، نافذة الخطة (B80)، قفل هوية الصنف بعد العمليات، رفض صريح بلا وزن كرتون (لا كرتون زائف).
- شاشة الإقفال: شارة جاهزية بالبيانات الفعلية (لا بالمظهر)، تعديل مقفلة = مرفوض، حذف مسودة فقط، أزرار بحالة.

**كرتون التفريغ (CartonService):** مفتاح رصيد كامل (مع Packaging)، حارس سالب، البيع فوق الرصيد مرفوض (فحص خطي + **شبكة أمان نهائية** `AddStock` تحظر صفّين لنفس الصنف يتجاوزان الرصيد معاً — تحقق)، تسوية بالعدّ الآلي.

**شاشة تسجيل الفعلي (ProductionDeliveryView):** لا مسار تجاوز (لا قائمة اختيار bypass)، **الخام ≤ 0 مرفوض في الواجهة** (لا إسقاط صامت بالمخطط عند الإدخال)، كل البند يتطلب فعلياً، الطباعة من التنفيذ المحفوظ فقط، أوامر اليوم من خطة اليوم المعتمدة فقط.

**بوابة التجاوز (ProductionDeliveryService B96):** `production:BypassInspection` + سبب مكتوب + تدقيق، أسقف مزدوجة (متبقي المصدر + الفيزيائي عبر كل المصادر)، `AMBIGUOUS_CUSTOMER` عند تعدد العملاء.

---

## جدول الملخص التنفيذي

| # | الخطورة | المرحلة | البند (اختصار) |
|---|---|---|---|
| P1-1 | 🔴 | تسليم العملاء | بوابة الجودة تفحص أمر الرأس فقط في السندات متعددة الأوامر (مسار «تسليم كامل المتاح» الافتراضي) |
| P1-2 | 🔴 | الإنتاج التام | «نصف/جزئي» يُستبدل بصمت بالتسليم الكامل (الكراتين الكاملة تكتب الكيلو) + «المتاح» بلا فلتر حالة |
| P2-1 | 🟠 | تسليم العملاء | تاريخ استلام التام/مدة البقاء دائماً «—» وFIFO مكسور (فلتر `Approved` لا يوجد لسندات التام) |
| P2-2 | 🟠 | الجودة/التسليم | `CheckType=="نهائي"` الصارم لا يطابق أي قيمة فعلية ← الصفة/القرار «بانتظار الفحص» دائماً (قائمة الالتقاط + تقرير التسليمات) |
| P2-3 | 🟠 | التنفيذ | تسويات الأصناف المساعدة تكتب الرصيد مباشرة (WAUX قد يسلب — خارج حارس PostStockMovement) |
| P2-4 | 🟠 | الجودة | مرفوضات تُحفظ تحت قرار «مطابق» دون تحذير + إسقاط القرار غير الصالح «Passed» بصمت |
| P3-1 | 🟡 | تسليم العملاء | صفوف اختيار الدفعة بلا وزن كرتون ← تعديل الكراتين يصفّر الوزن بصمت |
| P3-2 | 🟡 | الإنتاج التام | «تم اعتماد الجودة» في رسالة النجاح حتى والفحص معلّق (متغير coolingNote ميت) |
| P3-3 | 🟡 | تسليم العملاء | استبعاد الصفوف غير المكتملة من السند المحفوظ بصمت |
| P3-4 | 🟡 | أوامر الإنتاج | ازدواج صرف مساعد ممكن (مساران قديم+جديد بلا دمج) |
| P3-5 | 🟡 | الإعدادات/الأوامر | «لكل كجم» في BOM بحقل مسمّى «لكل كرتون» ← خطر صرف مفرط (×8 لكرتون 8كجم) |
| P3-6 | 🟡 | التنفيذ | إسقاط خام المستهلك «بالمخطط» بدون وسم/تنبيه |
| P4-1…7 | 🔵 | مختلف | حفظ تلقائي متبقٍ، استثناء عام، تعبئة «مقبول» مسبقة، سقف محلي مفقود، فوترة بلا تدقيق عكس، رصيد كجم خفي |

---

## ملاحظات منهجية

- الفحص **استاتيكي** (لا يوجد dotnet في بيئة الفحص — لا ادعاء ببناء/اختبار تشغيلي). كل نتيجة أعلاه قابلة للتحقق من السطور المشار إليها.
- P1-1 وP1-2 هما الوحيدان من فئة «فقدان سيطرة/بيانات»؛ الباقي عرض/توثيق/انحرافات محمية بأصفاف ثانوية.
- لا تغيير تم إجراؤه في هذا الفحص — التقرير للمراجعة. إن أُقرّ الإصلاح، يُنصح بترتيب: P1-1 → P1-2 (مع إصلاح «المتاح») → P2-1/P2-2 (تصحيح فلاتر) → P2-3/P2-4 → P3 → P4.
