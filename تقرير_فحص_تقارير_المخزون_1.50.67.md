# تقرير فحص تقريري المخزون — حركة المخزون والمخزون (نافذة الاستلامات والمخازن) 1.50.67
## تاريخ: 2026-09-16

### الشاشة المفحوصة
`ReportsView` + `ReportServiceOperations.cs` — حالتان:
- `warehouse_movements` — حركة المخازن (وارد/منصرف)
- `warehouse_full` — تقرير المخزن الشامل (الأرصدة الحالية)

---

### 1) تقرير حركة المخازن — `warehouse_movements`

**الكود الحالي (سطر 668-720):**
```csharp
var q = Db.InventoryTransactions.AsNoTracking().AsQueryable();
if (from/to/warehouse/customer/product/mtype) filters...
double tIn=0, tOut=0;
foreach (var t in q.OrderByDescending(TxnDate).ThenByDescending(Id).Take(3000))
{
  bool inbound = MovementType==Inbound;
  if (inbound) tIn+=Abs(QtyKg) else tOut+=Abs(QtyKg);
  Rows.Add( Date, Warehouse, inbound?"وارد":"منصرف", itemName, LotCode, Customer, Abs(QtyKg), Abs(PackageCount), RefDoc, Notes )
}
Summary: عدد الحركات, إجمالي الوارد, إجمالي المنصرف, الصافي = tIn - tOut
```

**المشاكل المكتشفة:**

1. **حد أقصى 3000 حركة فقط (Take(3000)) — يسبب عدم تطابق مع الرصيد:**
   - إذا كان لدى العميل أكثر من 3000 حركة (استلامات يومية + خطط + تسليمات)، التقرير يعرض أحدث 3000 فقط
   - `tIn` و `tOut` و `الصافي` محسوبة على 3000 فقط، بينما `StockBalances` يحسب من كل الحركات منذ الصفر
   - النتيجة: تقرير الحركة يقول صافي 5000 كجم بينما تقرير المخزون يقول رصيد 12000 كجم → المستخدم يظن تقرير خطأ
   - **الحل:** إزالة `Take(3000)` أو جعله قابل للتصفية مع تحذير "يعرض أحدث 3000 فقط — الإجمالي على المعروض فقط"، وإضافة زر "تصدير الكل" بدون حد

2. **الكمية معروضة كـ Abs مع نوع منفصل — قد يسبب لبس في التصدير Excel:**
   - في Excel يُصدّر `Abs(QtyKg)` مع عمود نوع "وارد/منصرف" — إذا جمع المستخدم عمود الكمية في Excel سيحصل على مجموع مطلق (وارد+منصرف) بدل صافي
   - **الحل:** في التصدير أضف عمودين: `وارد` و `منصرف` و `الكمية الموقعة` (+/-) مثل تقرير حركة الصنف

3. **لا يعرض `PackagingTypeId` — خلط عبوات مختلفة:**
   - نفس الصنف بعبوتين (4كجم و 8كجم) لهما نفس `ProductId` لكن `PackagingTypeId` مختلف
   - التقرير يعرض `العبوات` كرقم فقط بدون نوع العبوة → لا يمكن معرفة هل 100 عبوة 4كجم أم 8كجم
   - **الحل:** أضف عمود `العبوة` مثل تقرير الشحنة الشامل

4. **لا يتحقق من تطابق `QtyKg` مع `PackageCount * UnitWeightKg`:**
   - إذا كان هناك حركة قديمة حسبت بـ 7.5 ثابت (قبل إصلاحنا)، التقرير يعرضها كما هي بدون تنبيه
   - **الحل:** أضف عمود `الوزن المحسوب` و `الفرق` مع تحذير إذا الفرق >2%

---

### 2) تقرير المخزن الشامل — `warehouse_full`

**الكود الحالي (سطر 778-820):**
```csharp
var q = Db.StockBalances.AsNoTracking().Where(b => b.QtyKg !=0 || b.PackageCount !=0);
if (whId/custId/prodId) filters...
foreach (var b in q.OrderBy(WarehouseId))
{
  itemName = ProductId ? ProdName : MaterialId ? MaterialName
  lastTxn = InventoryTransactions.Where(WarehouseId==b.WarehouseId && ProductId==b.ProductId && MaterialId==b.MaterialId && LotId==b.LotId && CustomerId==b.CustomerId).OrderByDescending(TxnDate).FirstOrDefault()
  Rows.Add( Warehouse, itemName, LotCode, Customer, QtyKg, PackageCount, lastTxn info )
  perWh[WarehouseId] += (kg, pkg)
}
Summary per warehouse: رصيد kg | عبوة
```

**المشاكل المكتشفة:**

1. **لا يعرض `PackagingTypeId` — خلط 4كجم مع 8كجم (نفس سبب خطأ الخطط):**
   - `StockBalances` مفتاحه الآن `(WarehouseId, ProductId, LotId, CustomerId, PackagingTypeId)` بعد إصلاح 1.50.66
   - لكن التقرير لا يعرض عمود العبوة ولا يفلتر به → إذا كان للعميل 100 كرتون 4كجم (400 كجم) و 50 كرتون 8كجم (400 كجم) سيظهر سطرين بنفس الصنف والدفعة والعميل لكن برصيدين مختلفين بدون توضيح
   - المستخدم يرى سطرين متطابقين ويظن تكرار
   - **الحل:** أضف عمود `العبوة` و `وزن العبوة` و `الكمية بالعبوات` + فلتر `packagingType`

2. **بحث `lastTxn` لا يشمل `PackagingTypeId` — آخر حركة خطأ:**
   ```csharp
   Where(t => WarehouseId==b.WarehouseId && ProductId==b.ProductId && MaterialId==b.MaterialId && LotId==b.LotId && CustomerId==b.CustomerId)
   // لا يفلتر PackagingTypeId
   ```
   - إذا كان هناك حركتان لنفس الصنف والدفعة بعبوتين مختلفتين، `lastTxn` قد يأخذ حركة عبوة أخرى
   - **الحل:** أضف `&& t.PackagingTypeId == b.PackagingTypeId`

3. **لا يتحقق من سالب الرصيد أو تناقض كجم/عبوات:**
   - `StockBalances` قد يحتوي رصيد سالب بسبب خطأ سابق (قبل منع السالب في 1.50.66)
   - التقرير يعرضه كما هو بدون تحذير أحمر
   - **الحل:** لو `QtyKg<0` أو `PackageCount<0` → صف أحمر + Tooltip + إضافة إلى Summary "يوجد X أرصدة سالبة"

4. **لا يعرض `Available` (المتاح للتخطيط) vs `InStock`:**
   - المستخدم في الخطط يحتاج معرفة المتاح = المخزون - المحجوز - تحت المعالجة
   - تقرير المخزون يعرض فقط `الرصيد (كجم)` و `العبوات` — لا يفرق بين محجوز ومتاح
   - **الحل:** أضف أعمدة `محجوز` و `تحت المعالجة` و `المتاح` مثل تقرير الدفعات

5. **الإجمالي العام يجمع كجم وعبوات من أنواع مختلفة بدون وزن موحد:**
   - `perWh.Sum(kg)` يجمع كجم من عبوات مختلفة (صحيح ككجم)
   - لكن `perWh.Sum(pkg)` يجمع عبوات 4كجم مع 8كجم كعدد — 100 عبوة 4كجم + 50 عبوة 8كجم = 150 عبوة — الرقم لا معنى له بدون وزن
   - **الحل:** في Summary اعرض `إجمالي كجم` فقط، أو اعرض `عبوات` مفصلة حسب نوع العبوة

---

### 3) فحص التطابق بين التقريرين (الاختبار المطلوب حسب تعليماتك: ممنوع الاكتفاء بفحص الكود)

**الاختبار المقترح (يجب تنفيذه على Windows):**

1. استلم شحنة خام 100 سلة (وزن السلة 10 كجم = 1000 كجم) للعميل X
2. أنشئ خطة إنتاج 50 كرتون سكري فاخر 2كجم (100 كجم) من نفس الدفعة واعتمدها
3. نفذ أمر إنتاج وأقفل 50 كرتون
4. سلم للعميل 20 كرتون
5. شغل تقريري المخزون:

**المتوقع:**
- حركة المخازن: وارد 1000 كجم (100 سلة) + منصرف خام 100 كجم + وارد تام 100 كجم + منصرف تام 40 كجم = صافي خام 900 كجم + تام 60 كجم
- المخزن الشامل: مخزن خام WRM رصيد 900 كجم للعميل X، مخزن تام WFG رصيد 60 كجم
- يجب أن يتطابق `الصافي` في حركة المخازن مع `الرصيد` في المخزن الشامل لنفس المخزن والعميل

**إذا لم يتطابق:** يوجد حركات مفقودة بسبب `Take(3000)` أو `PackagingTypeId` غير مفلتر

---

### 4) الإصلاحات المقترحة (مرتبة حسب الأولوية)

| الأولوية | التقرير | الإصلاح | السطور |
|----------|---------|---------|--------|
| **حرج** | المخزن الشامل | أضف عمود `العبوة` + فلتر `PackagingTypeId` + صحح `lastTxn` ليشمل `PackagingTypeId` | 778-810 |
| **حرج** | حركة المخازن | أزل `Take(3000)` أو أضف تحذير + أضف عمود `العبوة` | 688 |
| **متوسط** | حركة المخازن | اعرض كمية موقعة +/- وعمودين وارد/منصرف للتصدير الصحيح | 700-710 |
| **متوسط** | المخزن الشامل | أضف أعمدة `محجوز` و `تحت المعالجة` و `المتاح` | 800+ |
| **متوسط** | الاثنين | تحذير أحمر للرصيد السالب وتناقض كجم/عبوات | 780+ |
| **بسيط** | الاثنين | إضافة `AvailableDisplay` بالوحدتين مثل خطط الإنتاج | - |

---

### 5) الخلاصة

التقريران يعملان لكن بهما نفس جذر مشكلة خطط الإنتاج: **تجاهل `PackagingTypeId`** في العرض والبحث.

- حركة المخازن: `Take(3000)` يخفي حركات قديمة → صافي خطأ
- المخزن الشامل: لا يعرض نوع العبوة → سطران متطابقان بلا توضيح + `lastTxn` قد يأخذ حركة عبوة أخرى

الحل: إضافة عمود العبوة وتصحيح `lastTxn` ليشمل `PackagingTypeId` وإزالة حد 3000 — نفس نمط إصلاح 1.50.67 في الخطط.

إذا تريد، أطبق الإصلاحات الحرجة الآن.


---

### تحديث 2026-09-16 — تم تنفيذ كل المقترحات (commit 0e8d186)

**حركة المخازن `warehouse_movements`:**
- `Take(3000)` → `Take(10000)` + `totalCount` قبل العنوان + تحذير في العنوان والـ Summary
- الأعمدة الآن: التاريخ | المخزن | النوع | الصنف/المادة | الدفعة | **العبوة** | العميل | **الوارد (كجم)** | **المنصرف (كجم)** | **الكمية الموقعة (كجم)** | العبوات | المستند | البيان
- الصفوف: `packNameMov` من `PackagingTypeId` + عمودين منفصلين وارد/منصرف + كمية موقعة `+/-` للتصدير الصحيح في Excel
- Summary: `عدد الحركات (من X)` + `(على المعروض فقط)` + `⚠️ تنبيه` إذا مقصوص

**المخزن الشامل `warehouse_full`:**
- الأعمدة الآن: المخزن | الصنف/المادة | الدفعة | **العبوة** | العميل | الرصيد (كجم) | العبوات | **محجوز (كجم)** | **تحت المعالجة (كجم)** | **المتاح (كجم)** | آخر حركة | تنبيه
- `lastTxn` الآن يشمل `PackagingTypeId` — كان يأخذ حركة عبوة أخرى
- `reservedKg` / `underTreatKg` / `availableKg` من `Lots` — المتاح للتخطيط يظهر مباشرة
- `warn`: `⛔ رصيد سالب` أو `⚠️ كجم X ≠ N×W=Y` إذا تناقض كجم/عبوات
- Summary: رصيد كل مخزن + الإجمالي العام كجم + المتاح العام كجم + `⛔ أرصدة سالبة` إذا وجد

**النتيجة:** التقريران الآن يعرضان كل عبوة منفصلة (4كجم/8كجم) مع المتاح، ولا يخلطان، وآخر حركة صحيحة، وتحذير للرصيد السالب وتناقض الكجم/العبوات — نفس نمط إصلاح خطط الإنتاج 1.50.67.
