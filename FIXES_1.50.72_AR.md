# تقرير الإصلاحات — الإصدار 1.50.72 (كل ملاحظات الفحص + تشفير التخطيط)

**التاريخ:** 18/09/2026 · **الفرع:** `arena/01a0ac34-offtt`
**مدخلات الإصلاح:** (1) تقرير الفحص `UPDATES_POST_PLAN_STAGES_INSPECTION_AR.md` (19 ملاحظة)، (2) بلاغ المستخدم: زر الحفظ مشفر + فشل تحديث 1.50.71.
**الإصدار:** 1.50.71 → **1.50.72** (csproj فقط — BuildInfo يقرأه ديناميكياً).

---

## ١) إصلاح تشفير النصوص (سبب «زر الحفظ مشفر»)

**السبب الجذري:** أربعة ملفات XAML كانت **من دون ترويسة UTF-8 (BOM)** بينما كل بقية XAMLs المشروع (20+) تحملها:
`PlanningView.xaml`، `LotPickerWindow.xaml`، `DateErpTheme.xaml`، `App.xaml`.
مترجم XAML في ويندوز العربي يقرأ الملف بلا BOM بترميز نظام cp1256 → **كل** النص العربي فيها يتشفّر.
العيب قديم منذ أول مصدر 1.50.67 في المستودع — لذلك «لا يزال مشفراً» في كل النسخ المبناة منه (بما فيها نسختك المستعادة).

**الإصلاح:** إضافة BOM فقط — المحتوى متطابق بايت‑بايت (تحقق بـ round-trip).
**التحقق المتوقع بعد البناء:** شاشة التخطيط ونافذة اختيار الدفعة والرسائل العربية مقروءة بالكامل، وزر «💾 حفظ الخطة» سليم.

## ٢) P1 — حرج

| # | الإصلاح | الملف |
|---|---|---|
| P1-1 | **بوابة الجودة لكل بند على أمره الخاص** (المشتق من دفعته) بدل أمر رأس السند؛ بند بلا دفعة يحتفظ بالسلوك الأصلي. نفس الأولوية لسقف «المطابق المعتمد» (QualityGate) وللمزامنة/فك المزامنة على الخطة (دفعة البند أولاً، خطة الرأس بديلاً). سند متعدد الأوامر («تسليم كامل المتاح») لم يعد يفلت من فحوصات أوامره ولا يلوّث خطة الرأس. | CustomerDeliveryService.cs، QualityGate.cs |
| P1-2 | **التسليم الجزئي لم يعد يُستبدل بالتسليم الكامل بصمت:** الواجهتان ترسلان كراتين **تناسبية** مع الكمية المختارة (الـ UI كانت ترسل الدفعة الكاملة مع كجم جزئي فتطبق قاعدة §1.50.68 «الكراتين هي الصحيحة» وتعيد كتابة الكيلو إلى كامل المنتَج). وحساب «المتاح» أصبح `Σ ReceivedQtyKg` (ما دخل التام فعلاً، باستثناء الملغاة) — يطابق سقف الخدمة؛ السند المعكس لم يعد يحجز كامل كميته. | FinishedGoodsView.xaml.cs، FGReceiveView.xaml.cs |

## ٣) P2 — عالٍ

| # | الإصلاح | الملف |
|---|---|---|
| P2-1 | تاريخ استلام التام = سندات **`IsApproved`** (كان `Status == "Approved"` الذي لا يحدث لسندات التام أبداً — عمودا التاريخ/مدة البقاء دائماً «—» وFIFO مكسور). | DeliveryPickService.cs |
| P2-2 | فلترة الفحص النهائي: `IsApproved` + نوع يحتوي «نهائي» (كان تطابقاً صارماً بـ "نهائي" لا ينطبق على "نهائي — بعد التبريد (يومان)") → الصفة/القرار في قائمة الالتقاط **وتقرير التسليمات** يعملان الآن. | DeliveryPickService.cs، ReportService.cs |
| P2-3 | تسويات الأصناف المساعدة عند الإقفال **لم تعد قادرة على تسليب WAUX**: فرقا «صرف تكميلي» و«إدخال فعلي غير مصروف» يفحصان الرصيد مسبقاً ويرفضان الإقفال برسالة صريحة مع اسم الصنف والكميات (يُقيَّد صرف/تسوية أولاً). | ExecutionService.cs |
| P2-4 | قرار الفحص: (1) القيمة غير الصالحة ترفض (كانت تُسقط «Passed» بصمت)، (2) محضر بمرفوضات وقرار «مطابق» يُحفظ مع **تحذير صريح** في النتيجة، (3) شاشة الجودة تمنع الحفظ بمرفوضات دون اختيار قرار صريح، (4) تأكيد الـ QCWindow يحذّر. | InspectionService.cs، QualityView.xaml.cs، QCWindow.xaml.cs |

## ٤) P3 — متوسط

| # | الإصلاح | الملف |
|---|---|---|
| P3-1 | صفوف «اختيار الدفعة» في شاشة التسليم تأخذ **وزن الكرتون من الدفعة** عند الإضافة (كان صفرًا بلا محدّث → تعديل الكراتين يصفّر الوزن بصمت). | DeliveryView.xaml.cs |
| P3-2 | رسالة نجاح سندات التام لم تعد تقول «تم اعتماد الجودة»؛ تُبنى من `coolingPending` (المتغير الميت اكتمل) مع تنبيه فترة التبريد. | FinishedGoodsService.cs |
| P3-3 | حفظ سند التسليم **يُبلّغ** عن أي بند بلا كراتين صالحة بدل استبعاده بصمت. | DeliveryView.xaml.cs |
| P3-4 | ازدواج صرف الأصناف المساعدة (مسار قديم + جديد) مُنع: إن وُجدت متطلبات جديدة للصنف فهي الحاكمة الوحيدة. | ProductionOrderService.cs |
| P3-5 | شاشة الـ BOM: وسم حقل الكمية يتبع طريقة الحساب («الكمية لكل كجم *» عند PerKg) + تلميح وأمثلة + رسالة خطأ متناسقة — لا قيمة «لكل كجم» تُدخل تحت وسم «لكل كرتون» بعد الآن. | ProductBOMView.xaml(.cs) |
| P3-6 | إسقاط خام المستهلك «بالمخطط» (غياب الفعلي) **مُوسم** في حركة الدفتر وفي نتيجة الإقفال (كان انحرافًا صامتًا في دفتر الخام). | ExecutionService.cs |

## ٥) P4 — منخفض

| # | الإصلاح | الملف |
|---|---|---|
| P4-1 | **إزالة الحفظ التلقائي** المتبقي في شاشة التسليم (مؤقّت 60ث + `DeliveryDraft_*.json` + الاستعادة) — اتساقاً مع قرار إزالة النمط نفسه من شاشة الخطط. | DeliveryView.xaml.cs |
| P4-2 | فتح سند تسليم: فحص أوزان الكراتين **قبل** البناء برسالة صديقة (كان استثناء عام في منتصف الحلقة مع سند نصف محمّل). | DeliveryView.xaml.cs |
| P4-3 | القيم المعبأة مسبقاً في شاشة الجودة («كل المستلم مقبول») **مميّزة** في سطر الحالة قبل الحفظ. | QualityView.xaml.cs |
| P4-4 | سقف محلي في الـ QCWindow: المفحوص لا يتجاوز المتبقي برسالة تسمي السطر والكميات. | QCWindow.xaml.cs |
| P4-5 | `MarkInvoiced` داخل معاملة واحدة (قراءة+زيادة+حفظ) — لا فوترة مكررة تحت تنافس. | PlanProgressService.cs |
| P4-6 | رصيد «كجم فقط» (بلا وزن كرتون) لم يعد خفيًا: يظهر في شريحة الرصيد مع ما يلزم لتفعيله. | DeliveryView.xaml.cs |
| P4-7 | نافذة مراجعة التسليم ترث إصلاحَي P2-1/P2-2 (لا ملف مستقل لها). | — |

## ٦) حزمة التحديث (بعد فشل 1.50.71 المجهول السبب)

- `BUILD-PUBLISH-1.50.72-WINDOWS.bat` — بناء + نشر + فحص مخرجات (نفس المنهج).
- `UPDATE_1.50.72_ONECLICK/UPDATE-1.50.72-ONECLICK.bat` — نفس الأمان (نسخ فقط، لا حذف، استبعاد db/config) **+ توثيق كل خطوة في `update_1.50.72.log`**: قفل التطبيق، **فحص تطابق حجم MfgSystem.exe بعد النسخ**، بدء التشغيل — فإذا فشل التحديث مرة أخرى سيقول اللوگ أين بالضبط.
- `UPDATE_1.50.72_PATCH_SOURCE.zip` — **أُعيد بناؤها كاملة (18/09/2026): 90 ملفاً + README** = الفروقات الكاملة 1.50.67→1.50.72 (77 ملفاً في src + 11 اختبارات + أداة تخطيط + سكربت SQL اختياري). سبب الإعادة: الحزمة الأولى (25 ملفاً) كانت تدريجية وافترضت حالة غير مؤكدة لـ`UPDATE_WORK` بعد فشل 1.50.71 — النسخة الكاملة تعمل من أي أساس (1.50.67 أو 1.50.71) وتنتج المصدر الحالي حرفياً. تحقق بايت‑بايت لكل مدخل. **تتضمن إصلاح خطأ حفظ الخطة (Math.Max داخل Sum — §1.50.70 في PlanningService.cs).**

## ٧) ما لم يتغير (ضمانات)

- **قاعدة البيانات والإعدادات:** لا ترحيل، لا بذور، لا لمس — التحديث نسخ ملفات فقط.
- لا تغيير في سلوك صحيح قائم: سلوك البوابة للسند أحادي الأمر مطابق حرفياً، وسلوك «تسليم كامل المتاح» داخل أمر واحد مطابق.
- الفحص **استاتيكي** (لا dotnet في بيئة العمل) — البناء والتشغيل على LENOVO حسب المنهج المعتمد.
