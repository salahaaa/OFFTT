# 🔎 تدقيق عملية الحفظ — شاشة خطط الإنتاج (PlanningView)
**التاريخ:** 2026-09-16 · **الفرع:** `arena/01a0ac34-offtt` · **النطاق:** `Save_Click` + حواجز الطاقة + خدمة التخطيط + الحفظ التلقائي + القوالب

---

## النتيجة العامة

عملية الحفظ **متينة بشكل ممتاز** من ناحية حواجز البيانات (طاقة/تواريخ/اتساق كرتون-كجم/عميل واحد)، لكنها تضم:
- **عيب تجميعي واحد قاتل** (يمنع بناء المشروع كله) — **أُصلح الآن** ✅
- **4 ثغرات منطقية** متفاوتة الخطورة — موثقة أدناه مع طريقة الإصلاح المقترحة
- **6 نقاط تعمل بشكل صحيح** مؤكدة بالتحقق

---

## 🔴 P0 — عيب تجميعي (قاتل للبناء) — ✅ أُصلح

**الموقع:** `PlanningView.xaml.cs` سطور 832/839 (`SaveAsTemplate_Click`)

```csharp
private int _currentPlanId;                    // سطر 47 — int غير nullable
...
if (_currentPlanId == null) { ... }            // سطر 832 — CS0019: خطأ تجميع
var r = ps.SaveAsTemplate(_currentPlanId.Value, dlg.Value);  // سطر 839 — CS1061
```

`_currentPlanId` هو `int` (غير nullable) بينما المعالجة كتبت لـ `int?` → **CS0019 + CS1061 → فشل بناء مشروع `DatesErp.Desktop` بالكامل** (لا يصلح أي حزمة إطلاقًا). يبدو أنها دُخلت عند دمج ميزة القوالب من مسار تطوير مختلف.

**الإصلاح المطبق:** `== null` → `== 0` و`.Value` → `_currentPlanId` مباشرة.
**فحص إضافي:** تم مسح الشجرة كلها (src) عن النمط نفسه (مقارنة int بـ null / استخدام .Value على غير nullable) — **لا توجد حالات أخرى حقيقية** (الضجيج المكتشف تم رده كاذبًا: `double? produced` في InspectionService، `int? lotId/rawId` في PlanningService).

> ملاحظة: هذا يؤكد ما قالته الوثائق السابقة: «البناء لم يُنفَّذ على ويندوز» — الكود يُسلَّم دون ضمان تجمّعه. تفعيل CI (الملف الجاهز في `CI/`) كان سيكشفه فورًا.

---

## 🟠 P1 — اعتماد خطة بتعديلات غير محفوظة (خطر فقدان بيانات)

**الموقع:** `ApproveAction_ClickCore` + زر شريط الأدوات `.WithApprove((_,_) => Approve(), ...)`

```csharp
if (_currentPlanId == 0) {
    if (!Confirm("الخطة غير محفوظة بعد. هل تريد حفظها ثم اعتمادها؟")) return;
    Save_Click(sender, e);
    if (_currentPlanId == 0) return;
}
Approve();   // ← إن كان _currentPlanId > 0 (محفوظة سابقًا) يُعتمد فورًا
```

**السيناريو:** مستخدم يفتح خطة محفوظة → يعدّل بنودها في الشبكة (كراتين/تواريخ/عملاء) → يضغط «اعتماد الخطة» → يُعتمد **حالة آخر حفظ** (قبل التعديلات) **دون أي سؤال** → `SetLocked(true)` يقفل الشاشة → **تعديلاته تضيع نهائيًا** ولا يمكن حفظها بعد الاعتماد.

**الإصلاح المقترح:** تتبع «وسخ» الحالة (dirty flag) — يعلو عند أي تعديل في `RowUi_Changed`/حقول الرأس بعد آخر `OpenPlan`/`Save_Click`، و`ApproveAction_ClickCore` يسأل: «توجد تعديلات غير محفوظة — هل تريد حفظها ثم الاعتماد؟» (بنفس نمط مسار `_currentPlanId == 0`). الزر في شريط الأدوات يمر عبر `Approve()` مباشرة فيحتاج نفس الحارس.

---

## 🟠 P2 — بند 0 كجم ممكن عند وزن كرتون غير معرف (من مسارات إدخال أخرى)

**الحالة:** اختيار الصنف من `ProductName_Click` **يرفض** صنفًا بلا وزن كرتون برسالة واضحة (إصلاح 1.50.67 ✅). لكن:
- `PlanRowUi.CartonsText`: `if (CartonWeight > 0) QtyKg = ...` — إن كان `CartonWeight == 0` يبقى `QtyKg` صفرًا.
- الخدمة: `EnsureCartonKgConsistency` عند `weight <= 0` **ترجع `qtyKg` كما هو** (لا رفض).
- النتيجة: صف بكراتين>0 + كجم=0 + وزن غير معرف **يُحفظ كبنية خطة 0 كجم** — لا يستهلك طاقة ولا يُكشف في التقارير.

**المسارات المعرّضة:** أي مسار يملأ صفًا دون مرور بـ `ProductName_Click` — استيراد Excel، «أصناف العملاء»، «من استلام مباشر»، «توزيع عادل»، تكرار صف (إن فقد الوزن من المصدر).

**الإصلاح المقترح:** في `Save_Click` بعد `validRows`:
```csharp
var zeroW = validRows.FirstOrDefault(r => r.CartonWeight <= 0);
if (zeroW != null) { Error($"وزن الكرتون غير معرف للصنف «{zeroW.ProductName}» — عرّفه في بطاقة الصنف قبل الحفظ."); return; }
```

---

## 🟡 P3 — مسودة الحفظ التلقائي: لا تُمسح بعد الحفظ + مفقودات في الاستعادة

**المشاكل:**
1. **لا حذف للمسودة بعد حفظ ناجح** (`Save_Click` لا يلمس `AutoSavePath`) → `TryRestoreAutoSave` يعرض مسودة قديمة (قد تكون لخطة **أخرى** — المسودة لكل مستخدم لا لكل خطة) عند أول شاشة فارغة → خطر تكرار بنود.
2. **JSON المسودة يفقد حقولًا:** `RawProductId, ShipmentId, ShipmentNo, SourceUnit, SourceQtyInUnit, SourceUnitWeightKg, SourceQtyKg, Priority` → المسودة المستعادة تفقد **ربط الخام والشحنة والأولوية** (تتحول لمعطيات ناقصة رغم بقاء `LotId`).

**الإصلاح المقترح:**
- عند نجاح `Save_Click`: `try { File.Delete(AutoSavePath); } catch { }`
- توسعة كائن التسلسل في `AutoSaveDraft` بالحقول السبع + `ItemId`.
- (اختياري) تسمية المسودة بالمستخدم+الخطة: `PlanningDraft_{user}_{planId}.json`.

---

## 🟡 P4 — تعارض تسمية F10 (وهم اختصار)

زر `ApproveActionBtn` في الـ XAML مكتوب عليه **«✅ اعتماد الخطة (F10)»** بينما **F10 فعليًا = حفظ** (`RowsGrid_PreviewKeyDown` + شريط الأدوات `.WithSave(..., "حفظ الخطة (F10)")`). الاعتماد لا يملك أي اختصار. مستخدم يضغط F10 معتقدًا أنه يعتمد → **يحفظ**.

**الإصلاح:** تغيير نص الزر إلى «اعتماد الخطة» (بدون F10)، أو إسناد F9… (F9 محجوز للبحث) — الأفضل حذف الوهم.

---

## ⚪ ملاحظات ثانوية

| # | الملاحظة |
|---|---|
| P5 | F2 مزدوج السياق: شريط الأدوات «خطة جديدة (F2)» مقابل الشبكة «F2 = صف فارغ» — يعمل حسب التركيز ولافتان مختلفتان؛ مقبول لكن يُربك. |
| P6 | فترة خطة غير صالحة (النهاية قبل البداية) لا تُرفض برسالة مباشرة — يظهر خطأ «تاريخ البند خارج الفترة» للبند الأول (محجوز لكن برسالة مبهمة). فحص مباشر `if (perStart > perEnd) Error("تاريخ النهاية قبل البداية")` أوضح. |

---

## ✅ ما تم تأكيده أنه يعمل صحيحًا

1. **فلاتر الصفوف الفارغة** (إصلاح 1.50.67): `allValid`/`validRows` في الحفظ + في الطاقة + رسالة «أكمل كمية الكراتين… الصفوف الفارغة لا تُحفظ» — لا صف يعطل الحفظ.
2. **بوابة الطاقة + أخطاء السطر**: `_capacityValid = result.IsValid && inputError == null && completeRows.Count > 0` — تناقض كجم/كرتون في بند مكتمل (`QuantityError`) **يسقط الحفظ**، والصفوف قيد الإدخال لا تسقطه.
3. **حارس فوري أثناء الكتابة**: `GuardQuantity`/`ValidateCartonKgImmediate` يعيدان حساب وتطابق الكجم أثناء تحرير الكراتين — و`quantity==0` لا يرمي (المستخدم يستطيع تصفير الصف).
4. **الخدمة (`SavePlan`/`UpdatePlan`)**: `RequireItemType(Finished)`، `EnsureCartonKgConsistency` (CARTON_KG_MISMATCH)، فرض عميل واحد من الخلفية، توريث الوردية/الخط للبنود، `PriorReservationByPlan` (تراكم بنود الخطة نفسها)، تحديث = استبدال كامل للبنود (`RemoveRange`) بلا نسخ مكررة.
5. **B80 تاريخ الإنتاج**: كل بند بتاريخ صالح **داخل فترة الخطة** إلزامي في الواجهة والخلفية.
6. **B108 بعد الحفظ**: `OpenPlan` يعيد تحميل البنود من القاعدة فتتحديث معرفاتها — «تعديل البند» يعمل على المعرفات الصحيحة.

---

## خطة الإصلاح المقترحة (بالتسلسل)

| أولوية | البند | الجهد |
|---|---|---|
| **تم** | P0 عيب التجميع | ✅ (في هذا الالتزام) |
| 1 | P1 حارس «تعديلات غير محفوظة» قبل الاعتماد | ~30 سطر (dirty flag + سؤال) |
| 2 | P2 رفض الحفظ عند وزن كرتون غير معرف | ~5 أسطر |
| 3 | P4 حذف وهم F10 من زر الاعتماد | سطر |
| 4 | P3 تهيئة المسودة (حذف بعد الحفظ + الحقول الناقصة) | ~15 سطر |
| 5 | P6/P5 رسائل أوضح | أسطر |

**كلها إصلاحات واجهة/منطق محلية** (لا تلمس المخطط أو البيانات) — يمكن تنفيذها فورًا والبناء على ويندوز لتأكيدها.
