# 🔍 تقرير الفحص المتكامل — DateERP (OFFTT)
## لماذا «يُرفض» التنصيب؟ — تشخيص جذري موثّق بالأدلة
**التاريخ:** 2026-09-16 · **الفرع:** `arena/01a0ac34-offtt` · **نطاق الفحص:** كامل محتويات المستودع (4 أرشيفات ZIP)

---

## 1) الملخص التنفيذي

المشروع **تطبيق سطح مكتب C#/.NET 8 (WPF)** لإدارة مصنع تمور، والمستودع لا يحتوي على كود مباشر بل **4 أرشيفات ZIP**:

| الأرشيف | الحجم | المحتوى | حالة التنصيب فيه |
|---|---|---|---|
| `DateERP_1.50.37_FULL_SOURCE.zip` | 1.6 م.ب | مصدر كامل + أدوات تنصيب bat | ✅ بنيويًا سليم (أساسي للبناء) |
| `DateERP_1.50.41_Update_Mini.zip` | 1.2 م.ب | ثنائيات مصغّرة + سكربتات تحديث | ❌ **حزمة تحديث فقط — ترفض على جهاز نظيف** |
| `DateERP_1.50.42_Update_Mini.zip` | 1.2 م.ب | نفس الشيء للإصدار 1.50.42 | ❌ **نفس الرفض** |
| `DateERP_Source_1.50.67.zip` | 4.8 م.ب | أحدث مصدر + «حزم OneClick» | ❌ **المصدر لا يُبنى + الحزم فارغة (Placeholder)** |

### الأسباب الجذرية لرفض التنصيب (مرتبة حسب الاحتمال)

| # | السبب | أين يظهر |
|---|---|---|
| **1** | **حزم «Update_Mini» ليست مثبّتات أصلية بل حزم تحديث فقط** — السكربت يرفض تشغيلها خارج مجلد نسخة سابقة (شريط رسالة + إغلاق) | حزم 1.50.41/42 |
| **2** | **الحزم المصغّرة framework-dependent** (3.4 م.ب — بلا `coreclr.dll`) مع أنها توصف بأنها «مكتفية ذاتياً» → على جهاز بلا .NET 8 Desktop Runtime لا يعمل `DateERP.exe` إطلاقًا | حزم 1.50.41/42 |
| **3** | **`2-تنصيب.bat` ينسخ إلى `C:\Program Files` بدون طلب صلاحيات مسؤول** (وترويسته تدّعي عكسها!) → رفض وصول → «[خطأ] فشل النسخ» → خروج | كل الحزم |
| **4** | **حزم OneClick/FULL-200MB (1.50.66/67) فارغة**: لا ثنائيات، ولا **موجود `.bat` واحد في كامل الأرشيف** بينما كل الأدلة تشير إلى ملفات غير موجودة (`RUN-FULL-READY-200MB.bat`، `INSTALL-1.50.66.bat`، `SETUP-FROM-ZERO.bat`…) | 1.50.66/67 |
| **5** | **مصدر 1.50.67 لا يُبنى**: مشروع `audit/UnitRuleAudit` مفقود ومُشار إليه من `DateERP.sln`، وملف `app.manifest` مفقود ومُشار إليه من الـ csproj، واسم الناتج تغيّر إلى `MfgSystem.exe` بينما كل الأدوات تبحث عن `DateERP.exe` | 1.50.67 |
| **6** | **تضارب هوية الإعداد**: المثبّت يكتب `config.json` في `%LocalAppData%\DateERP_15021` بخادم `.\SQLEXPRESS01`، بينما أحدث كود يقرأ من `%LocalAppData%\MfgSystem` وقاعدته `MfgSystemDB` → إعدادات المثبّت **تُتجاهل كليًا** | 1.50.42+ مقابل 1.50.66/67 |

---

## 2) البنية العامة للمشروع

```
src/
  DatesErp.Core/            الكيانات والقيم والواجهات
  DatesErp.Application/     25+ خدمة أعمال (تخطيط، إنتاج، جودة، تسليم…)
  DatesErp.Infrastructure/  EF Core (SQL Server + SQLite) + هجرة مخطط + DPAPI
  DatesErp.Desktop/         واجهة WPF — 25+ شاشة عربية RTL (net8.0-windows)
tests/DatesErp.Tests/       اختبارات xUnit
tools/                      أدوات قبول ودخان (AcceptanceRunner، WpfSmoke…)
Installer/                  حزمة تنصيب عربية (bat/ps1/sql)
```

- **الإطار:** .NET 8 (`net8.0-windows`)، WPF، EF Core، `ClosedXML`، `PdfSharp`، `QRCoder`.
- **الإقلاع:** `Splash ← فحص الإعداد ← AutoDbResolver (يكتشف نسخ SQL المحلية أو يهبط لوضع SQLite محلي) ← إنشاء تلقائي للقاعدة (EnsureCreated + Seed) ← تسجيل دخول`.
- **التنبيهات المهمة في `Directory.Build.props`:**
  ```xml
  <NuGetAudit>true</NuGetAudit>
  <NuGetAuditMode>all</NuGetAuditMode>
  <WarningsAsErrors>…NU1900;NU1901;NU1902;NU1903;NU1904</WarningsAsErrors>
  ```
  أي أن **أي فشل في تدقيق NuGet (بما فيه انقطاع الإنترنت NU1900) يحوّل البناء إلى فشل**.

### ملاحظات على مستودع GitHub نفسه
- المستودع فيه **التزام واحد فقط** («Add files via upload») بلا تاريخ تطوير — الكود الفعلي لا يدار هنا كنظام.
- شارة CI في `README.md` تشير إلى **مستودع مختلف** (`salahaaa/TASNEE1212`)، ورأس الـ README يذكر الإصدار **1.50.5** بينما المحتوى 1.50.67 — توثيق متقادم.
- الأرشيف 1.50.67 بلا `LICENSE` (و1.50.37 يحتويه).

---

## 3) التشخيص التفصيلي لكل مسار تنصيب

### المسار أ) الحزم المصغّرة `DateERP_1.50.41/42_Update_Mini.zip` — **الأرجح أنها سبب الرفض الذي تراه**

**أدلة على أن الحزمة «تحديث فقط» وليست مثبّتًا:**

1. ملف `اقرأني_التحديث_المصغر.html` المرفق مع الحزمة يقول نصًّا:
   > **«انسخ إلى DateERP_1.50.20 ثم 1-حدّث_وشغل.bat»**
   أي أن الاستخدام المطلوب هو نسخ الملفات **داخل مجلد نسخة قديمة موجودة مسبقًا**.

2. سكربت `1-حدّث_وشغل.bat` (أول ملف ينقّره المستخدم عادة) يحتوي حارسًا صريحًا:
   ```bat
   if exist "%SRC%coreclr.dll" goto :inplace
   …
   echo [تنبيه] هذا السكربت لا يعمل من مكانه الحالي.
   echo   المطلوب: ضع ملفات التحديث كلها داخل مجلد النسخة DateERP_1.50.20
   …
   powershell … MessageBox «DateERP — مكان التحديث غير صحيح»
   exit /b 1
   ```
   والحزمة المصغّرة **لا تحتوي `coreclr.dll`** (محتواها 14 ملفًا = 5 DLLs + exe + runtimeconfig + 2 bat + html فقط) → على أي جهاز جديد **تُرفض الفوريًا** بنافذة «مكان التحديث غير صحيح» وتغلق. هذا هو «رفض التنصيب» الحرفي.

3. **عيب إضافي في نفس السكربت:** التسمية `:inplace` **غير موجودة** في الملف (التسميات الوحيدة `:installparent`) → حتى لو وُجد `coreclr.dll` بجانب السكربت (التحديث داخل مجلد النسخة) فإن `goto :inplace` يُعطي خطأ cmd: *The system cannot find the batch label specified* ويتوقف السكربت. **مسار التحديث مكسور من الطرفين.**

4. **الحزمة framework-dependent وليست مكتفية ذاتيًا** (رغم ما تدّعيه ترويسة السكربت «لا يحتاج .NET (الحزمة مكتفية ذاتياً)»):
   - الحجم الإجمالي 3.4 م.ب (حزمة WPF مكتفية ذاتيًا تكون ~150+ م.ب).
   - `DateERP.runtimeconfig.json` يحتوي `includedFrameworks` (Microsoft.NETCore.App 8.0.31 + Microsoft.WindowsDesktop.App 8.0.31) — هذا هو توقيع البناء **غير** المكتفي ذاتيًا.
   - النتيجة على جهاز نظيف: نافذة «**To run this application, you must install .NET**» → المستخدم يقرأها كـ«رفض».

5. **`2-تنصيب.bat` (مثبّت الحزمة المصغّرة) يفشل بدون صلاحيات:**
   ```bat
   set "DEST=%ProgramFiles%\%APPNAME%"
   …
   if exist "%DEST%" rmdir /s /q "%DEST%"
   mkdir "%DEST%"          ← بدون elevation: Access is denied (لا يتحقق من الخطأ!)
   xcopy /e /i /y /q "%SRC%*.*" "%DEST%\" >nul
   if errorlevel 1 ( echo [خطأ] فشل النسخ إلى %DEST% … pause & exit /b 1 )
   ```
   ترويسة الملف تقول «لا يحتاج … صلاحيات مدير في الوضع العادي» — **غير صحيح** لـ `Program Files`؛ النقر المزدوج العادي (بدون «تشغيل كمسؤول») = رفض وصول → «فشل النسخ» → خروج.

6. **ملفات مستدعاة داخل الحزمة لكن غير مضمّنة:**
   | الملف | الاستدعاء | الحالة |
   |---|---|---|
   | `SHA256.txt` | خطوة التحقق `[2/6]` | غير موجود (يُتخطى التنبيه — لكن بلا حماية سلامة) |
   | `إنشاء_اختصار.ps1` | الخطوة `[6/6]` | **غير موجود** → فشل خطوة الاختصارات (تحذير فقط) |
   | `إلغاء_التنصيب.bat` | رسالة النهاية | غير موجود |

7. **إعدادات لا يقرأها التطبيق الحديث:** السكربت يكتب:
   ```
   %LocalAppData%\DateERP_15021\config.json
   "Server": ".\SQLEXPRESS01", "Database": "DateFactory_15021"
   ```
   بينما `AppConfig` في أحدث الكود يقرأ من **`%LocalAppData%\MfgSystem`** وقاعدتها الافتراضية **`MfgSystemDB`**. أي: مثبّت 1.50.42 + كود 1.50.66/67 = **إعدادات غير فاعلة**، وخادم `.\SQLEXPRESS01` (named instance مخصص) غير موجود أصلًا في أغلب الأجهزة.

### المسار ب) حزم «OneClick / FULL 200MB» (داخل `DateERP_Source_1.50.67.zip`) — **حزم فارغة**

- `PACKAGE-INFO.txt` يعترف صراحة:
  > `Size: 11 KB (placeholder - real build will be ~150-200 MB after dotnet publish on Windows)`
  > `Source Build: FAIL (dotnet SDK not available in Linux sandbox, TLS blocked to builds.dotnet.microsoft.com)`
  > `Windows Installation: NOT TESTED`
  > `publish/ (placeholder - will be replaced with real binaries on Windows)`
- فحص شامل بالأدلة: **عدد ملفات `.bat` / `.ps1` / `.sql` في كامل الأرشيف 1.50.67 = صفر**.
  - `README-FULL-200MB.txt` يأمر المستخدم: «كليك يمين على **`RUN-FULL-READY-200MB.bat`**» — **الملف غير موجود في الحزمة**.
  - `INSTALL-GUIDE-AR.md` يشير إلى `SETUP-FROM-ZERO.bat` و`تنصيب_نظام_التصنيع.bat` و`RUN-SYSTEM.bat` و`OPEN-VISUAL-STUDIO.bat` — **كلها غير موجودة**.
  - مجلد `Tools/` الذي يُفترض أن يحوي أدوات التثبيت الأوفلاين يحتوي **ملف README نصيًا واحدًا فقط**.
- **النتيجة:** المستخدم يفك الضغط ولا يجد أي ملف تشغيل → «رفض» صريح (لا يوجد ما يُنفَّذ).

### المسار ج) البناء من مصدر 1.50.67 (مسار المطوّر الموثّق) — **لا يُبنى**

أخطاء مؤكدة استاتيكيًا:

1. **مشروع مفقود من الحل:**
   - `DateERP.sln` (المستخدم في CI وكل السكربتات) يحتوي:
     `Project(…) = "UnitRuleAudit", "audit\UnitRuleAudit\UnitRuleAudit.csproj"`
   - **مجلد `audit/` غير موجود** في الأرشيف 1.50.67 (كان موجودًا في 1.50.37) →
     `MSB3202: The project file "…\audit\UnitRuleAudit\UnitRuleAudit.csproj" was not found` → **restore/للبناء يفشل قبل أي ترجمة**.
2. **ملف manifest مفقود:**
   - `DatesErp.Desktop.csproj` يحتوي `<ApplicationManifest>app.manifest</ApplicationManifest>`
   - **`app.manifest` محذوف** من مجلد المشروع في 1.50.67 (موجود في 1.50.37) → خطأ بناء قاسٍ لمشروع الـ Desktop حتى لو بُني مباشرة دون الحل.
3. **تضارب اسم الناتج (يتسبب في رفض حتى لو نجح البناء):**
   ```xml
   <AssemblyName>MfgSystem</AssemblyName>   ← يبني MfgSystem.exe
   ```
   بينما `1-بناء_الحزمة.bat` وكل المثبّتات تكتب `DateERP.exe`:
   - خطوة البصمة: `Get-FileHash '%OUT%\DateERP.exe'` → الملف غير موجود → **`SHA256.txt` يُنشأ فارغًا**.
   - ثم `2-تنصيب.bat` يقارن البصمة: المتوقع (فارغ) ≠ الفعلي →
     **`[خطأ] البصمة غير مطابقة — الحزمة تالفة أو معدَّلة` → `exit /b 1`** ← رفض تنصيب بسبب اسم exe تغيّر فقط.
4. **تدقيق NuGet صارم يحوّل البيئة إلى ساحة فشل:**
   - `NuGetAuditMode=all` + `NU1900…NU1904` كـ errors → في شبكة بلا إنترنت/بلا وصول لـ api.nuget.org (وهو نفس الوضع الذي اعترت به الحزمة السابقة: «TLS blocked») → **restore يفشل**.
   - `ClosedXML 0.102.3` نسخة قديمة (2023) ضمن تدقيق `all` — نقطة حساسة إضافية لأي advisory.
5. **سلسلة البناء متسلسلة على الاختبارات:** `1-بناء_الحزمة.bat` ينفّذ `dotnet test` **قبل** الـ publish ويتوقف عند أي فشل — والـ README نفسه في 1.50.67 يقر: «الفحص الشامل به إخفاقات مفتوحة» → الحزمة لا تُبنى أصلًا.
6. **التباس الحلين:** يوجد `DateERP.sln` و`DatesErp.sln` معًا؛ الأول (المرجعي في كل الأدلة) هو المعطوب بغياب `audit`، والثاني لا يحتوي `audit` لكنه ليس الموثّق.

### المسار د) سيناريو «التشغيل على جهاز فيه نسخة كاملة سابقة»

- `1-حدّث_وشغل.bat` يستدعي `robocopy` ثم `taskkill /f /im DateERP.exe` ثم التشغيل — منطقي، لكن **`goto :inplace` المكسور (النقطة 3-أ-3)** يقتل المسار قبل `robocopy`.
- `2-تنصيب.bat` لا يقفل `DateERP.exe` قبل `rmdir/xcopy` → إن كان النظام يعمل، ملفات مقفلة → «فشل النسخ».

---

## 4) ملاحظات ثانوية (حِرفة التغليف والاتساق)

| # | الملاحظة | الأثر |
|---|---|---|
| 1 | **أسماء ملفات عربية معطّبة الترميز داخل الـ ZIP** (مثل `Ϭ+Ϧ+Ϧ_Ϻ+…1.50.66_AR.md`) مع **نسخ مكررة** من نفس المستندات بأسماء سليمة | الـ ZIP وُظّف بترميز غير UTF-8 → خلط فوضوي، وقد تظهر أسماء مشوهة عند الاستخراج في ويندوز |
| 2 | `فحص_المتطلبات.bat` يقرأ الإعداد من `%LocalAppData%\DateERP` بينما المثبّت يكتب إلى `%LocalAppData%\DateERP_15021` | الفحص يتجاهل الإعداد الفعلي |
| 3 | سكربتات bat بترميز UTF-8 **بدون BOM** مع `chcp 65001` | تعمل غالبًا على Win10+ لكن هشة (بعض إصدارات cmd تُخطئ عدّ الإزاحات مع UTF-8) |
| 4 | `UPDATE-AND-RUN.bat` (في 1.50.37) يفترض `git pull` داخل مجلد التشغيل — غير منطقي لحزمة ZIP | مسار ميت عمليًا |
| 5 | `INSTALL-GUIDE-AR.md` (OneClick) يقول إنه «لم يُختبر في بيئة CMD حقيقية (بيئة التطوير بلا ويندوز)» | كل سكربتات OneClick غير مثبتة الفعالية |
| 6 | توثيق «نشر Self-contained — لا يحتاج الجهاز الهدف تثبيت .NET» في README مقابل حزم مصغّرة غير مكتفية | تضارب يعرّض المستخدم لـ«رفض» غير مفهوم |
| 7 | `DatesErp.sln` أحدث تعديلًا (16/9) من `DateERP.sln` (15/9) لكن لا دليل على أيهما الرسمي | ارتباك في نقطة البناء |

---

## 5) التوصيات — بالأولوية

### 🔴 فورية (تفتح التنصيب)
1. **حدد «سبب الرفض» على جهازك** بمطابقة الرسالة الظاهرة (جدول التشخيص السريع في §6) — كل رسالة تشير لسبب محدد أعلاه.
2. **إن أردت نسخة تعمل الآن على جهاز جديد:**
   - ثبّت **.NET 8 Desktop Runtime** + **SQL Server Express** (نسخة افتراضية أو بالاسم `SQLEXPRESS01` إن أصررت عليه).
   - انشر حزمة **مكتفية ذاتيًا** حقيقية من مصدر 1.50.37 (السليم بنيويًا) على أي جهاز ويندوز:
     `dotnet publish src\DatesErp.Desktop\DatesErp.Desktop.csproj -c Release -r win-x64 --self-contained true -o DateERP_Publish`
     — وهذا ما يفعله `1-بناء_الحزمة.bat` (يضيف SHA256 وأدوات التنصيب).
3. **إن أردت استخدام حزم 1.50.41/42 المصغّرة كما صُممت:** انسخ محتواها **داخل مجلد نسخة كاملة سابقة** (تحوي `coreclr.dll`) ثم `1-حدّث_وشغل.bat` — وأصلح تسمية `:inplace` المفقودة قبل ذلك.

### 🟠 قصيرة المدى (إصلاح الحزم المرفوضة)
4. **إعادة بناء حزمة OneClick حقيقية على ويندوز** (لا يمكن بناؤها في Linux بلا WPF) مع تضمين **كل** السكربتات الموثقة (`INSTALL-*.bat`، `RUN.bat`، `VERIFY-*.bat`) والتحقق أن `publish/` حقيقي (~150-200 م.ب) لا placeholder.
5. **توحيد الهوية** (قرارات مصدر واحد):
   - إبقاء `<AssemblyName>` = `DateERP` (أو `MfgSystem` مع تحديث **كل** السكربتات/التوثيق/البصمات معًا).
   - توحيد مجلد الإعداد وقاعدة البيانات الافتراضية (واحد من: `DateERP_15021`+`DateFactory_15021` أو `MfgSystem`+`MfgSystemDB`) — والكود الحالي يقرأ من `MfgSystem` فقط.
6. **إصلاح `2-تنصيب.bat`:**
   - طلب الرفع الذاتي (`powershell Start-Process -Verb RunAs`) **أو** التنصيب إلى `%LocalAppData%\Programs\DateERP` بلا صلاحيات (الأفضل للمستخدم).
   - إضافة `taskkill` قبل النسخ، والتحقق من `mkdir`، وتضمين `إنشاء_اختصار.ps1` و`SHA256.txt` في الحزمة.
7. **استعادة الملفات المفقودة من 1.50.67:** مجلد `audit/` (من 1.50.37) وملف `app.manifest`، أو حذف المراجع منهما.

### 🟢 هيكلي (يمنع تكرار المشكلة)
8. **CI حقيقي على ويندوز:** مصفوفة GitHub Actions موجودة (`windows-latest`) لكن المستودع لا يدير الكود (ZIPs فقط) → انقل الكود الفعلي كمسارات git وفعّل: `restore → build -warnaserror → test → publish self-contained → إنشاء ZIP + SHA256 → رفع artifact`. خطوة واحدة على ويندوز تلتقط كل أخطاء هذا التقرير تلقائيًا.
9. **تغليف الـ ZIP بترميز UTF-8** (PowerShell `Compress-Archive` أو `7z`/`zip -UN=UTF8` مع `UTF8` flag) وحذف التكرارات المشوهة.
10. **عزل التدقيق الأمني عن التجهيز:** اجعل `NuGetAudit` يفشل البناء فقط على advisories فعلية (NU1901-1904) وسمّح بتجاوز NU1900 (عدم توفر الـ feed) عبر `--no-restore` في الخطى اللاحقة — حتى لا يرفض البناء بسبب انقطاع الشبكة.
11. **توثيق واحد حي:** README بإصدار مطابق، شارة CI للمستودع الصحيح، `LICENSE` في كل حزمة.

---

## 6) جدول التشخيص السريع (مطابقة الرفض الذي تراه)

| ما تراه على الشاشة | السبب الجذري (من §3) | الحل الفوري |
|---|---|---|
| نافذة: «DateERP — مكان التحديث غير صحيح» + رسالة «هذا السكربت لا يعمل من مكانه الحالي» | حزمة Mini على جهاز بلا نسخة سابقة (§3-أ-2) | انسخ الملفات داخل مجلد نسخة كاملة سابقة، أو استخدم حزمة كاملة مكتفية ذاتيًا |
| رسالة cmd: `[خطأ] فشل النسخ إلى C:\Program Files\DateERP` | لا صلاحيات مسؤول (§3-أ-5) | «تشغيل كمسؤول» — أو أعد كتابته لـ `%LocalAppData%\Programs` |
| نافذة: «To run this application, you must install .NET» | حزمة framework-dependent بلا runtime (§3-أ-4) | ثبّت .NET 8 Desktop Runtime، أو استبدلها بحزمة self-contained |
| رسالة: `[خطأ] البصمة غير مطابقة — الحزمة تالفة أو معدَّلة` | `SHA256.txt` فارغ بسبب تغيّر اسم exe (§3-ج-3) | أعد توليد البصمة من `MfgSystem.exe` أو أعد تسمية الناتج `DateERP` |
| خطأ بناء: `The project file "…audit\UnitRuleAudit…" was not found` | مشروع مفقود من الحل (§3-ج-1) | استعد `audit/` من 1.50.37 أو حذره من `DateERP.sln` |
| خطأ بناء متعلق بـ `app.manifest` | الملف محذوف من 1.50.67 (§3-ج-2) | انسخه من 1.50.37 |
| بعد فك ضغط 1.50.66/67: **لا يوجد أي `.bat` للضغط عليه** | حزمة placeholder فارغة (§3-ب) | لا حزمة صالحة — أعد البناء على ويندوز |
| التطبيق يبدأ ثم لا يتصل بالقاعدة | `.\SQLEXPRESS01` غير موجود + إعدادات تتجاهلها النسخة الحديثة (§3-أ-7) | ثبّت SQL Express بالاسم المربط، أو اسمح لـ `AutoDbResolver` بالهبوط لوضع SQLite المحلي |

---

## 7) ميثاق الفحص (ما تم وكيف)

- استخراج وفحص كل الأرشيفات الأربعة كاملًا (قوائم ملفات، أحجام، timestamps، ترميز الأسماء).
- قراءة كل سكربتات التنصيب (bat/ps1) سطرًا بسطر: `1-حدّث_وشغل.bat`، `2-تنصيب.bat`، `1-بناء_الحزمة.bat`، `3-تنصيب_الحزمة_المصغّرة.bat`، `فحص_المتطلبات.bat` + التوثيق المرافق.
- مطابقة التسميات (`grep ^:`) ضد `goto` — كشف `:inplace` المفقودة.
- فحص BOM وترميز ملفات bat (od/hexdump)، وتطابق CRLF.
- قراءة `*.csproj`/`Directory.Build.props`/`*.sln`/`app.manifest`/`runtimeconfig.json`/`deps` ومقارنة `AssemblyName` عبر النسخ.
- مقارنة أشجار الملفات 1.50.37 ← 1.50.67 (بحث عن ملفات محذوفة: `app.manifest`، `audit/`، `tools/release/*`، `tools/SqlFixes/*`).
- فحص اكتمال أزواج `.xaml/.xaml.cs` (سليم)، و`x:Class` (سليم).
- مطابقة مسارات الإعداد في الكود (`AppConfig`) ضد ما تكتبه المثبّتات.
- فحص الشبكة: `api.nuget.org` و`builds.dotnet.microsoft.com` **مقفلان في هذه البيئة** (HTTP 000) → البناء الحي غير ممكن هنا، وكل استنتاجات البناء **استاتيكية مؤكدة** (ملف مفقود ↔ مرجع موجود).
- مقارنة بصمات SHA-256 لملفات 1.50.41 و1.50.42 (ثنائيات مختلفة = إصداران مختلفان حقيقيان).

> **خلاصة سطر واحد:** التنصيب «يرفض» لأن ما يُسلَّم للمستخدم ليس مثبّتات متناسقة: حزم التحديث مصممة لتعمل فوق نسخة قديمة فقط، الحزمة الكبيرة فارغة placeholder، والمصدر الأحدث مكسور البناء (مشروع وmanifest مفقودان) وباسم ناتج لا تطابقه أي أداة — وكلها تُبنى في بيئة بلا ويندوز بلا أي اختبار تثبيت حقيقي.

---

## 8) الإصلاحات المنفّذة (2026-09-16 — على فرع `arena/01a0ac34-offtt`)

**القرار المعتمد مع المالك:** الهوية الموحّدة = **MfgSystem** (نظام التصنيع المنفصل: `MfgSystem.exe` + `%LocalAppData%\MfgSystem` + `MfgSystemDB`)، يتعايش مع نسخ DateERP القديمة دون تأثير.

| # | الإصلاح | الملفات | يزيل سبب الرفض |
|---|---|---|---|
| 1 | **المصدر الأحدث أصبح ملفات في المستودع** (560 ملفًا) بدل ZIP فقط — قابل للـ git/CI | جذر المستودع | الأساس لكل ما يلي |
| 2 | **إصلاح 38 اسمًا عربيًا معطّب الترميز** (double-encoding CP1256) وحذف 6 تكرارات مضمونة البصمة | ملفات جذر + `Documentation/` + `Installer/` | فوضى التغليف |
| 3 | **استعادة `audit/UnitRuleAudit`** (مفقود ومُشار إليه من `DateERP.sln`) | `audit/` | `MSB3202` — فشل الـ restore |
| 4 | **استعادة `app.manifest`** (مفقود ومُشار إليه من الـ csproj) + تحديث هويته إلى `MfgSystem.app` | `src/DatesErp.Desktop/app.manifest` | خطأ البناء القاسي |
| 5 | **مطابقة اسم الناتج عبر السلسلة**: `MfgSystem.exe` في البصمة والمثبّت والتوثيق | كل سكربتات `Installer/` الجديدة | «البصمة غير مطابقة» |
| 6 | **مثبّت جديد صلب `2-تنصيب.bat`**: يكتشف الصلاحيات (مسؤول → Program Files / بدون → `%LocalAppData%\Programs` بلا UAC)، يقفل العملية النشطة قبل النسخ، يتحقق من `mkdir`، بصمة SHA256 لا تُتخطى بصمت إن كانت فارغة، اختصارات Inline بلا ملفات خارجية، **ولا يكتب أي إعداد قاعدة بيانات** — ترك `AutoDbResolver` يكتشف المحركات (بما فيها `.\SQLEXPRESS01`) أو يهبط لوضع SQLite | `Installer/2-تنصيب.bat` | «فشل النسخ» + تضارب الإعدادات |
| 7 | **سكربت بناء جديد `1-بناء_الحزمة.bat`**: restore ← tests (توقف عند الفشل) ← publish win-x64 self-contained ← `VERSION.txt` من الـ csproj (مصدر واحد) ← `SHA256.txt` من `MfgSystem.exe` ← ZIP | `Installer/1-بناء_الحزمة.bat` | حزم placeholder + بصمة فارغة |
| 8 | **فحص متطلبات + إلغاء تنصيب** (يحفظ البيانات افتراضيًا ولا يلمس نسخ DateERP) | `Installer/فحص_المتطلبات.bat`، `Installer/إلغاء_التنصيب.bat` | أدوات ناقصة |
| 9 | **CI يبني حزمة تثبيت حقيقية**: خطوة Windows `publish` + أدوات + VERSION/SHA256 + `Compress-Archive` + رفع artifact (`mfgsystem-installer-package`) | `CI/CI_WORKFLOW_READY.yml` (+ `CI/README_AR.md`) | «لا يوجد ما يُثبّت» — الحزمة تُنتج تلقائيًا عند كل دفع |
| 10 | **عزل تدقيق NuGet عن التجهيز**: `NU1900` (انقطاع الـ feed) لم يعد يسقط البناء؛ `NU1901-1904` (ثغرات فعلية) تبقى مرفوضة | `Directory.Build.props` | فشل restore في البيئات المقيدة |
| 11 | **توثيق موحد**: README بإصدار 1.50.67 وشارة CI للمستودع الصحيح ونقطة الدخول `MfgSystem.exe` + دليل تنصيب جديد `Installer/README_التنصيب.md` + علامة «أرشيف» على دليل OneClick القديم | `README.md`، `Installer/README_التنصيب.md`، `Installer/OneClick/INSTALL-GUIDE-AR.md` | تضارب التوثيق |
| 12 | **استعادة ملفات CI/أدوات من 1.50.37**: `tools/ci/*`، `tools/release/*`، `tools/SqlFixes/*`، `LICENSE`، `.gitignore` | متعددة | خطوات CI ميتة |

**بقي غير قابل للإنجاز في هذه البيئة (يتطلب ويندوز):**
- البناء الحي `dotnet publish` — لا يوجد .NET SDK هنا وشبكة NuGet مقفلة (HTTP 000)؛ كل الفحوصات أعلاه استاتيكية مؤكدة (ملف مفقود ↔ مرجع موجود، تسميات ↔ goto، أزواج xaml).
- اختبار تنصيب حقيقي على ويندوز 10/11 — أول دفع إلى الفرع سيشغّل CI وينتج حزمة `mfgsystem-installer-package` جاهزة للتجربة.

**خطوات التثبيت بعد الدمج (على أي جهاز ويندوز):**
1. شغّل `Installer\1-بناء_الحزمة.bat` على جهاز المطوّر (أو خذ حزمة artifact من CI).
2. انسخ الناتج `MfgSystem_Publish` إلى الجهاز المستهدف وشغّل `2-تنصيب.bat` — نقرة واحدة، بلا UAC، بلا .NET، بلا إعدادات يدوية.
