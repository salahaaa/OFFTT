# 📦 تقرير التسليم — دمج 1.50.71 على فرع واحد (منضبط)
**الفرع:** `arena/01a0ac34-offtt` @ `8fe07b8` · **التاريخ:** 2026-09-17
**التحميل:** https://github.com/salahaaa/OFFTT/tree/arena/01a0ac34-offtt

---

## 1) الملفات التي تم دمجها

**من شجرة `arena/01a091dd-offtt` @ `1ebf320`** (محددة يدوياً، بدون جلب حزمهم الوهمية):
- **71 ملفاً في `src/`** — إصلاحات 1.50.68 → 1.50.71:
  - `UnitsPolicy.cs` (تصحيح كرتون/كجم التلقائي) · `PlanningRows.cs` (إعادة الحساب الحي)
  - `PlanningView.Capacity.cs` + `PlanningView.xaml(.cs)` (فك تشفير زر الحفظ + رقم السند + تعطيل الحفظ التلقائي)
  - `PlanningService.cs` (إصلاح انهيار EF `Math.Max` في `SavePlan`)
  - `ReceivingView.xaml(.cs)` + `DeliveryView.xaml(.cs)` + `PrintPreviewWindow.xaml(.cs)` + بقية ملفات العرض (تطبيع BOM/ترميز + إصلاحات الأسماء)
- **`tools/ci/verify_xaml_names.py`** (فاحص أسماء XAML ضد C# — أُضيف أيضاً إلى CI).
- `tests/` غير متغيّر عندهم، والجذور (حزمهم/تقاريرهم/الـ ZIPs) **لم تُجلب** — بَقيت أدوات التغليف والتوثيق عندنا فقط.

**ما بقي من فرعنا:** المثبّت (`Installer/`)، `audit/`، `CI/`، `Directory.Build.props` (تدقيق NuGet)، كل التقارير العربية.

## 2) التعارضات التي تم حلها
| التعارض | الحل |
|---|---|
| **`PlanningView.xaml.cs`** — فرنا عالج P0 (`.Value` على int) بـ `== 0` وشجرتهم أعادته `int?` مع حراسة كاملة (يشمل `SaveAsTemplate`) | **عُدِّد شكلهم** (الأحدث والأشمل، ومرّ الفحوص) — السلوك المكافئ محفوظ |
| `Directory.Build.props` (موجود عندنا فقط — تدقيق NuGet) | بَقي عندنا (لا تعارض فعلياً) |
| csproj الجذر (متطابقان) | بدون تغيير + رفع الإصدار |
| جذور الشجرة (ZIPs وهمية/تقاريرهم) | استُبعدت عن الدمج عمداً |

## 3) ما تم إصلاحه في P1 وP4
**P1 — اعتماد خطة بتعديلات غير محفوظة (فقدان بيانات):**
- أُضيفت `PlanFingerprint()` = بصمة (عنوان/فترة/نوع/نطاق/ملاحظات + كل البند الصالح بحقوله الـ16 المطابقة لـ `Save_Click`) — **الصفوف الفارغة مستثناة** (لا تُحفظ أصلاً فلا تُعد تعديلاً).
- خط الأساس يُلتقط في `OpenPlan` (بعد كل تحميل) و`NewPlan`.
- الحارس في **`Approve()` نفسه** (نقطة التقاء المسارين: زر الاعتماد في الشاشة **و** زر الاعتماد في شريط الأدوات — الثاني كان سيمر من أي حارس في زر بعينه): إن اختلفت البصمة ← تأكيد «تعديلات غير محفوظة — ستُحفظ قبل الاعتماد» ← `Save_Click` ← إعادة الفحص.
- لا تغيير سلوكي في أي مسار آخر (الحفظ/الإرسال/الحذف/الطباعة كما هي).

**P4 — وهم (F10):** زر «✅ اعتماد الخطة (F10)» أصبح «✅ اعتماد الخطة» — **F10 بقي = حفظ فعلياً** (لم يُغيَّر أي ربط مفاتيح).

## 4) الإصدار الرسمي 1.50.71
- `DatesErp.Desktop.csproj`: `<Version>1.50.71</Version>` (كان 1.50.67).
- **اكتشاف جوهري:** ختم شريط العنوان `BuildInfo.Stamp` كان **مثبتاً يدوياً على «1.50.37»** — هو السبب الحقيقي في عدم معرفة أي نسخة تعمل. صار يُقرأ **تلقائياً من إصدار التجميع**: شريط العنوان وشاشة «معلومات النظام» سيفتحان بـ **1.50.71** في أي بناء قادم ولن ينزاحا أبداً.

## 5) Result of Build — الصدق أولاً
**لم يُنفذ `dotnet build` داخل هذه البيئة** (Linux بلا .NET SDK وبلا وصول إلى NuGet — مستحيل بناء WPF هنا)، ولن أُدّعي 0 Errors لم أشهدها. ما نُفّذ بدلها:
| الفحص | النتيجة |
|---|---|
| فاحص أسماء XAML/C# (أداتهم + تشغيلي) | ✅ 0 أخطاء |
| مسح `int == null`/`.Value` على غير nullable | ✅ نظيف (مطابقاتهم الموثقة فقط) |
| `Math.Max` داخل Sum عبر EF | ✅ الباقي بعد `ToList()` (تقييم عميل) |
| `x:Class` ↔ أسماء الملفات | ✅ متطابق |
| مراجع `DateERP.sln` (13 مشروعاً **بما فيه audit/UnitRuleAudit**) و`DatesErp.sln` (12) | ✅ كلها موجودة |
| توازن الأقواس في الملف المعدّل | ✅ |

**التأكد القاطع من 0 Errors** يكون بدفعة واحدة على ويندوز:
`BUILD-PUBLISH-1.50.71-WINDOWS.bat` — يبني **`DateERP.sln` (بما فيه audit/)** ثم **`DatesErp.sln`** ثم ينشر، **ويوقف فوراً عند أي خطأ** (لا ينتج publish إلا بنجاح كامل).

## 6) مسار نسخة Publish (حقيقية، ليست placeholder)
- **تُنشأ على ويندوز** بتشغيل واحد لـ `BUILD-PUBLISH-1.50.71-WINDOWS.bat` من جذر المستودع →
  **`publish_1.50.71\MfgSystem.exe`** (self-contained win-x64 + `coreclr.dll` + `VERSION.txt` + `SHA256.txt`).
- البات **يبطّئ الحزمة الوهمية**: يتحقق من وجود `MfgSystem.exe` و`coreclr.dll` ومن حجم ملف قابل للحياة قبل أن يعتبر النجاح.
- بديل آلي: تفعيل `CI/CI_WORKFLOW_READY.yml` (يُنتج `mfgsystem-installer-package` كـ artifact من GitHub مباشرة).
- (الملفات الضخمة خارج Git عمداً — `*.zip`/`publish_1.50.71/` في `.gitignore` التزاماً بتقاليد المستودع.)

## 7) حزمة التحديث بضغطة واحدة (ليست ملفات مصدر)
`UPDATE_1.50.71_ONECLICK/` + ما يبنيه البات في `UPDATE_1.50.71_PACKAGE/`:
- `publish_1.50.71/` (الثنائيات الحقيقية) + `UPDATE-1.50.71-ONECLICK.bat`.
- **آمن على قاعدة البيانات** (طلبك صريحاً): النسخ **بدون حذف** + `/EXCLUDE` صريح لـ `mfgsystem_local.db`/`config.json`/`*.db`؛ والقاعدة والإعدادات أصلها في `%LocalAppData%` خارج مجلد التشغيل إطلاقاً. يسجّل سبراً في `UPDATE-LOG.txt` ويعيد التشغيل.

## 8) تشغيل النسخة المنشورة مباشرة (بدون اختصار قديم)
1. شغّل `BUILD-PUBLISH-1.50.71-WINDOWS.bat` → ينتج `publish_1.50.71\`.
2. **انسخ/شغّل `publish_1.50.71\MfgSystem.exe` مباشرة** — النسخة self-contained لا تحتاج أي runtime ولا اختصاراً قديماً؛ تقرأ قاعدة البيانات والإعدادات الحالية تلقائياً (نفس البيانات، نفس الاستمرارية).
3. للتثبيت الدائم: اختصار جديد إلى هذا المسار (أو مثبّت `Installer/` القياسي).
4. للتحديث المستقبلي: حزمة `UPDATE_1.50.71_PACKAGE\` بضغطة واحدة.
5. **التحقق البصري:** شريط العنوان «DateERP — إصدار 1.50.71» + «معلومات النظام».

## 9) ما تبقى (مفتوح، لم يُنفذ لعدم طلبه)
- P2 (بند 0 كجم عبر مسارات الإدخال غير منتقي الأصناف) — مخفف بالتصحيح التلقائي 1.50.68.
- Toast عند تصحيح 1.50.68 الصامت (شفافية البيانات).
- تفعيل CI على المستودع (الملف جاهز، يحتاج نسخاً إلى `.github/workflows/`).
