# حزمة التسليم المتكاملة 1.50.67 للمطور / المنصب — إصلاح التقارير والشاشات

**تاريخ:** 2026-09-16
**الفرع:** `arena/01a091dd-offtt` — آخر commit `16250a2`
**الإصدار:** 1.50.67 (يلي 1.50.66)

---

## ما الذي تم إصلاحه في هذه الحزمة؟

### 1) إصلاحات حرجة سابقة (commit 277a4af):
- `DeliveryView.xaml.cs`: إزالة كل fallback 7.5 ثابت — الآن يجلب `CartonWeightKg` من بطاقة الصنف ويرمي إذا غير معرف
- `FinishedGoodsView.xaml.cs`: إزالة 7.5 — يجلب الوزن من DB
- `PlanningView.xaml.cs`: إزالة fallback 5 كجم — يرفض إضافة صنف بوزن 0

### 2) إصلاحات متوسطة (commit 9340a80):
- `ItemsView.xaml.cs`: تحذير تناقض وزن الكرتون مقابل وزن العبوة المرتبطة (سبب خطأ سكري فاخر 0.5 vs 2 كجم) — يمنع حفظ بيانات تسبب mismatch لاحقاً
- `DeliveryView.xaml.cs` Save(): تجاهل الصفوف الفارغة placeholder — `validItems = !IsEmptyRow && ProductId!=0 && Packages>0`
- `ReceivingView.xaml.cs` Save(): تجاهل الصفوف غير المكتملة — `validRows = PackageCount>0 && UnitWeight>0` + رسالة "أكمل عدد الوحدات..."

### 3) إصلاحات تقارير المخزون (commit af11528 + 0e8d186):
- `ReportServiceOperations.cs` — `warehouse_movements`:
  - `Take(3000)` → `Take(10000)` مع `totalCount` وتحذير في العنوان
  - إضافة عمود **العبوة** من `PackagingTypeId`
  - فصل **الوارد (كجم)** و **المنصرف (كجم)** و **الكمية الموقعة (كجم)** +/- للتصدير الصحيح في Excel
- `ReportServiceOperations.cs` — `warehouse_full`:
  - إضافة عمود **العبوة** + **محجوز (كجم)** + **تحت المعالجة (كجم)** + **المتاح (كجم)** + **تنبيه**
  - إصلاح `lastTxn` ليشمل `PackagingTypeId` — كان يأخذ حركة عبوة أخرى
  - تحذير `⛔ رصيد سالب` و `⚠️ كجم X ≠ N×W=Y`
  - Summary: رصيد كل مخزن + الإجمالي العام + المتاح العام + عدد الأرصدة السالبة

---

## الملفات المحدثة (6 ملفات كود + 2 تقرير):

```
src/DatesErp.Application/Services/ReportServiceOperations.cs  ← الأهم (التقارير)
src/DatesErp.Desktop/Views/Screens/DeliveryView.xaml.cs
src/DatesErp.Desktop/Views/Screens/FinishedGoodsView.xaml.cs
src/DatesErp.Desktop/Views/Screens/PlanningView.xaml.cs
src/DatesErp.Desktop/Views/Screens/ItemsView.xaml.cs
src/DatesErp.Desktop/Views/Screens/ReceivingView.xaml.cs
تقرير_فحص_تقارير_المخزون_1.50.67.md
تقرير_فحص_شامل_الشاشات_1.50.67.md
```

---

## روابط التحميل المباشر (Raw) — آخر تحديث:

### الحزمة الكاملة (ZIP) — كل المشروع:
```
https://github.com/salahaaa/OFFTT/archive/refs/heads/arena/01a091dd-offtt.zip
```
هذا هو الملف المتكامل الذي تسلمه للمطور — يحتوي كل الإصلاحات.

### الملفات منفردة (للتحديث السريع فوق السورس القديم):
```
https://raw.githubusercontent.com/salahaaa/OFFTT/arena/01a091dd-offtt/src/DatesErp.Application/Services/ReportServiceOperations.cs
https://raw.githubusercontent.com/salahaaa/OFFTT/arena/01a091dd-offtt/src/DatesErp.Desktop/Views/Screens/DeliveryView.xaml.cs
https://raw.githubusercontent.com/salahaaa/OFFTT/arena/01a091dd-offtt/src/DatesErp.Desktop/Views/Screens/FinishedGoodsView.xaml.cs
https://raw.githubusercontent.com/salahaaa/OFFTT/arena/01a091dd-offtt/src/DatesErp.Desktop/Views/Screens/PlanningView.xaml.cs
https://raw.githubusercontent.com/salahaaa/OFFTT/arena/01a091dd-offtt/src/DatesErp.Desktop/Views/Screens/ItemsView.xaml.cs
https://raw.githubusercontent.com/salahaaa/OFFTT/arena/01a091dd-offtt/src/DatesErp.Desktop/Views/Screens/ReceivingView.xaml.cs
```

---

## تعليمات للمنصب / المطور:

### الطريقة 1: حزمة كاملة (مفضل):
1. حمّل `arena/01a091dd-offtt.zip` من الرابط أعلاه
2. فك الضغط إلى `D:\DateERP_1.50.67_SOURCE`
3. افتح `DateERP.sln` في Visual Studio 2022
4. شغل:
```bat
dotnet restore DateERP.sln
dotnet build DateERP.sln -c Release
dotnet publish src\DatesErp.Desktop\DatesErp.Desktop.csproj -c Release -r win-x64 --self-contained true -o D:\DateERP_1.50.67_Publish
```
5. شغل `Installer/1-بناء_الحزمة.bat` ثم `Installer/2-تنصيب.bat` على جهاز العميل
   - الحزمة لا تحذف قاعدة البيانات — تنشئها فقط إذا غير موجودة
   - تنشئ اختصار سطح المكتب + قائمة ابدأ + RUN.bat

### الطريقة 2: تحديث سريع فوق 1.50.66 المنصب:
1. حمّل الملفات الستة من الروابط Raw أعلاه
2. انسخها فوق نفس المسارات في `D:\DateERP_1.50.43_FULL_SOURCE\src\...`
3. ابنِ:
```bat
dotnet publish src\DatesErp.Desktop\DatesErp.Desktop.csproj -c Release -o publish_NEW
xcopy publish_NEW\* C:\DateERP_Publish\ /E /Y
```
4. أعد تشغيل النظام

---

## التحقق بعد التنصيب:

1. افتح شاشة الاستلامات والمخازن → تقرير **حركة المخزون**:
   - يجب أن يظهر عمود **العبوة**
   - يجب أن يظهر عمود **الوارد** و **المنصرف** و **الكمية الموقعة**
   - إذا كان هناك >10000 حركة يظهر تحذير في العنوان

2. تقرير **المخزون**:
   - يجب أن يظهر عمود **العبوة** + **محجوز** + **تحت المعالجة** + **المتاح** + **تنبيه**
   - نفس الصنف بعبوتين 4كجم و 8كجم يظهر سطرين مميزين
   - إذا رصيد سالب يظهر `⛔` وإذا تناقض كجم/عبوات يظهر `⚠️`

3. شاشة الأصناف:
   - عدّل وزن كرتون صنف مرتبط بعبوة مختلفة → يجب أن يظهر تحذير قبل الحفظ

4. شاشة التسليم والاستلام:
   - أضف صف فارغ جديد ولا تملأه → زر حفظ يبقى مفعلاً ولا يحفظ الصف الفارغ

---

## ملاحظات:

- Windows Installation NOT TESTED — الذكاء الاصطناعي لا يستطيع تشغيل Windows، يجب اختبار التنصيب على جهاز Windows حقيقي
- لا تحذف `D:\DateERP_1.50.20_Full\DateERP_1.50.20` — أرشيف
- الحزمة تعتمد .NET 8 SDK — حمّله من https://aka.ms/dotnet/8.0/dotnet-sdk-win-x64.exe إذا غير موجود

---

## SHA / إصدار:

- Branch: `arena/01a091dd-offtt`
- Last commit: `16250a2` (بعد هذا التقرير سيكون commit جديد)
- تاريخ البناء: 2026-09-16
