# تقرير المشاكل المكتشفة خلال 24 ساعة الأخيرة — 1.50.67
**تاريخ التقرير:** 2026-09-16 (يغطي من 2026-09-15 12:00 حتى 2026-09-16 12:00 Asia/Aden)
**الفرع:** `arena/01a091dd-offtt` — commits من `464c8a7` حتى `839c80d`

---

## ملخص تنفيذي:

تم اكتشاف **12 مشكلة** خلال 24 ساعة، منها **5 حرجة** و **4 متوسطة** و **3 بسيطة**، كلها تتعلق بنفس الجذر: **تجاهل `PackagingTypeId` ووجود أوزان ثابتة مقنعة**.

- **الحرجة:** أوزان ثابتة 7.5 كجم و 5 كجم في 3 شاشات تسبب حساب كمية خطأ (سكري فاخر 0.5 vs 2)
- **المتوسطة:** زر حفظ يتشفر بسبب صف فارغ + لا تحذير تناقض وزن العبوة + تقارير تخلط 4كجم/8كجم
- **البسيطة:** `Take(3000)` يخفي حركات + تصدير Excel يجمع Abs خطأ + لا عرض متاح/محجوز

**كل المشاكل تم إصلاحها ودفعها إلى GitHub.**

---

## الجدول التفصيلي:

| # | التاريخ | الشاشة/التقرير | الخطورة | المشكلة المكتشفة | الأثر على العمل | الإصلاح | Commit |
|---|---------|----------------|---------|------------------|-----------------|----------|--------|
| 1 | 2026-09-15 14:30 | `DeliveryView.xaml.cs` سطر 32 | **حرج** | `Packages setter = 7.5` ثابت بدل `CartonWeightKg` | كمية كجم خطأ عند تعديل عدد الكراتين — نفس سبب سكري فاخر | جلب `CartonWeight` من `Products` + throw إذا 0 | 277a4af |
| 2 | 2026-09-15 14:32 | `DeliveryView` سطر 83 | **حرج** | `CellEditEnding` يعيد حساب الكجم بـ `7.5` | تعديل عدد الكراتين في الشبكة يعطي كجم خطأ | استخدام `effectiveW` من بطاقة الصنف | 277a4af |
| 3 | 2026-09-15 14:35 | `DeliveryView` سطر 502 | **حرج** | `row.UnitWeight = batch.PackName!=null?7.5:7.5` — دائماً 7.5 | تسليم العميل دائماً 7.5 مهما كان الصنف | `UnitWeight=0` + يرمى إذا `unitW<=0` في `OpenDelivery` | 277a4af |
| 4 | 2026-09-15 15:10 | `FinishedGoodsView.xaml.cs` سطر 233 | **حرج** | `fallback 7.5` في `Receive_Click` | استلام إنتاج تام بوزن خطأ | جلب `CartonWeightKg` من DB + رسالة "وزن الكرتون غير معرف... عرّفه في بطاقة الصنف" | 277a4af |
| 5 | 2026-09-15 15:20 | `PlanningView.xaml.cs` | **حرج** | `fallback 5 كجم` في `ProductName_Click` و `ImportExcel` | خطة إنتاج بوزن 5 كجم وهمي لصنف وزنه 0.5 | `0` + رسالة خطأ تمنع الإضافة | 277a4af |
| 6 | 2026-09-15 18:00 | `ItemsView.xaml.cs` Save_Click | **متوسط** | لا تحذير تناقض وزن الكرتون vs وزن العبوة المرتبطة عبر `ProductShiftCapacities` | السبب الجذري لخطأ سكري فاخر: يدخل 2 كجم بينما العبوة 0.5 كجم → mismatch لاحقاً في الخطط | فحص `packWeights` + إذا diff > max(0.5,5%) → Confirm تحذيري "تنبيه: وزن الكرتون يختلف عن وزن العبوة..." | 9340a80 |
| 7 | 2026-09-15 19:00 | `DeliveryView` Save() | **متوسط** | `_items.Count` يشمل صف فارغ placeholder → زر حفظ يتشفر أو يحفظ صف فارغ | المستخدم يضيف صف جديد فارغ → لا يستطيع الحفظ | `validItems = !IsEmptyRow && ProductId!=0 && Packages>0` + تجاهل الفارغ | 9340a80 |
| 8 | 2026-09-15 19:15 | `ReceivingView` Save() | **متوسط** | نفس مشكلة `DeliveryView` — يطلب عدد وحدات للصف الفارغ | صف جديد بعدد 0 يعطل الحفظ | `allValid vs validRows` + رسالة "أكمل عدد الوحدات..." | 9340a80 |
| 9 | 2026-09-16 08:00 | تقرير `warehouse_movements` سطر 688 | **متوسط** | `Take(3000)` — يعرض أحدث 3000 فقط، الصافي محسوب على 3000 بينما الرصيد من كل الحركات → عدم تطابق | عميل له 5000 حركة → تقرير الحركة يقول صافي 5000 بينما المخزون 12000 → المستخدم يظن تقرير خطأ | `Take(10000)` + `totalCount` + تحذير في العنوان والـ Summary + إضافة عمود العبوة | af11528 + 0e8d186 |
| 10 | 2026-09-16 08:20 | تقرير `warehouse_full` سطر 778 | **حرج** | لا عمود `PackagingTypeId` + `lastTxn` لا يشمل `PackagingTypeId` → سطران متطابقان لنفس الصنف بعبوتين 4كجم/8كجم بدون توضيح + آخر حركة قد تأخذ حركة عبوة أخرى | خلط 100 كرتون 4كجم (400كجم) مع 50 كرتون 8كجم (400كجم) → يظهر سطرين متطابقين | إضافة عمود العبوة + تصحيح `lastTxn` ليشمل `PackagingTypeId` + إضافة محجوز/تحت المعالجة/المتاح + تنبيه سالب/تناقض | af11528 + 0e8d186 |
| 11 | 2026-09-16 08:40 | تقرير `warehouse_movements` تصدير | **بسيط** | الكمية معروضة كـ `Abs(QtyKg)` مع عمود نوع منفصل — في Excel جمع العمود يعطي وارد+منصرف بدل صافي | تصدير Excel خطأ | إضافة عمودين **الوارد** و **المنصرف** + **الكمية الموقعة** +/- | 0e8d186 |
| 12 | 2026-09-16 09:00 | تقرير `warehouse_full` إجمالي | **بسيط** | `perWh.Sum(pkg)` يجمع عبوات 4كجم مع 8كجم كعدد — 150 عبوة لا معنى لها + لا تحذير سالب | إجمالي عبوات مضلل + رصيد سالب لا يظهر | إضافة `المتاح العام` + `⛔ أرصدة سالبة` + تحذير `⚠️ كجم ≠ N×W` | 0e8d186 |

---

## التسلسل الزمني للإصلاحات:

```
2026-09-15 14:30-15:30  → فحص شامل للشاشات — اكتشاف 5 أوزان ثابتة حرجة
2026-09-15 15:30        → commit 277a4af fix(screens): remove hardcoded 7.5kg and 5kg fallbacks
2026-09-15 18:00-19:30  → فحص متوسط — ItemsView + Delivery/Receiving completeRows
2026-09-15 19:30        → commit 9340a80 fix(screens): medium issues +  f73dd96 docs
2026-09-16 08:00-09:30  → فحص تقريري المخزون (حركة المخزون والمخزون) — اكتشاف Take(3000) + خلط عبوات
2026-09-16 09:30        → commit af11528 fix(reports): packaging type and Take limit
2026-09-16 10:00        → commit 0e8d186 fix(reports): implement all proposals
2026-09-16 10:30        → commit 839c80d docs: delivery package
```

---

## الجذر المشترك:

كل المشاكل الـ 12 سببها **نفس القرار القديم**: افتراض وزن ثابت 7.5 كجم أو تجاهل `PackagingTypeId` كمفتاح رصيد.

**القاعدة الذهبية الجديدة المطبقة في كل الإصلاحات:**
- `صفر = غير معرف ويرفض العملية` — لا fallback وهمي
- `PackagingTypeId جزء من مفتاح الرصيد` — كل عبوة منفصلة (4كجم/8كجم لا تخلط)
- `completeRows = ProductId>0 && Qty>0` — الصفوف الفارغة لا تعطل الحفظ
- `تحذير فوري عند تناقض وزن الكرتون vs وزن العبوة المرتبطة` — يمنع خطأ سكري فاخر من المصدر

---

## ما لم يتم إصلاحه (مقترحات مستقبلية):

- تقرير `item_movements` (حركة الصنف) لا يعرض العبوة — نفس مشكلة حركة المخازن قبل الإصلاح
- تقرير `warehouse_full` لا يزال يوزع المحجوز نسبياً للدفعة — يحتاج توزيع دقيق حسب `PackagingTypeId`
- شاشة `PlanClosureView` قد تحتاج نفس تحذير تناقض الوزن

---

## روابط التحقق:

- الفرع: https://github.com/salahaaa/OFFTT/tree/arena/01a091dd-offtt
- Commits: https://github.com/salahaaa/OFFTT/commits/arena/01a091dd-offtt
- الحزمة الكاملة: https://github.com/salahaaa/OFFTT/archive/refs/heads/arena/01a091dd-offtt.zip
- تقرير التقارير: https://raw.githubusercontent.com/salahaaa/OFFTT/arena/01a091dd-offtt/تقرير_فحص_تقارير_المخزون_1.50.67.md
- تقرير الشاشات: https://raw.githubusercontent.com/salahaaa/OFFTT/arena/01a091dd-offtt/تقرير_فحص_شامل_الشاشات_1.50.67.md
