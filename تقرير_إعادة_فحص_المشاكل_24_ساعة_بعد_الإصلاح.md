# إعادة فحص المشاكل 12 خلال 24 ساعة — هل تم حلها فعلاً؟ 2026-09-16

**تاريخ إعادة الفحص:** 2026-09-16 23:30 Asia/Aden
**الفرع:** `arena/01a091dd-offtt` — آخر commit `a7d0112`
**النتيجة:** ✅ كل المشاكل الـ 12 مصلحة في السورس الحالي — لكنها تحتاج **إعادة بناء ونشر** لتظهر في البرنامج المنصب

---

## الفحص المباشر للكود الحالي (grep):

### 1-5) أوزان ثابتة 7.5 و 5 كجم — هل لا تزال موجودة؟

```bash
grep -n "7.5" DeliveryView.xaml.cs
→ لا يوجد إلا في تعليق: "بلا بديل ثابت 7.5" — ✅ مصلح
grep -n "7.5" FinishedGoodsView.xaml.cs
→ لا يوجد إلا في تعليق: "لا وزن ثابت 7.5" — ✅ مصلح
grep -n "5.*كجم|fallback" PlanningView.xaml.cs
→ لا يوجد — ✅ مصلح
```

**الكود الحالي:**
- `DeliveryView` سطر 505: `row.UnitWeight = 0; // لا وزن ثابت 7.5`
- `DeliveryView` سطر 347: `validItems = !IsEmptyRow && ProductId!=0 && Packages>0`
- `FinishedGoodsView` سطر 237: `// لا وزن ثابت 7.5 — استخدم وزن البطاقة`
- `PlanningView` سطر 900: `validRows = Cartons>0` + رسالة "أكمل كمية الكراتين"

**الحكم:** ✅ مصلح — لكن إذا لم تعيد البناء، البرنامج المنصب لا يزال يستخدم DLL قديم فيه 7.5

---

### 6) ItemsView تحذير تناقض وزن العبوة:

```bash
grep -n "يختلف عن وزن العبوة" ItemsView.xaml.cs
→ سطر 193: "⚠️ تنبيه: وزن الكرتون المدخل ({cw}) يختلف عن وزن العبوة «{pw}»"
```

**الكود الحالي سطر 167-194:**
```csharp
var packWeights = PackagingTypes.Where(ProductId==id)
if (|cw - pw.UnitWeightKg| > max(0.5, cw*5%))
  Confirm("تنبيه: وزن الكرتون يختلف عن وزن العبوة... هذا التناقض هو سبب خطأ كمية الكيلو لا تطابق...")
```

**الحكم:** ✅ مصلح في السورس

---

### 7-8) Delivery/Receiving زر حفظ يتشفر بصف فارغ:

```bash
grep -n "validItems|IsEmptyRow" DeliveryView.xaml.cs
→ 347: validItems = !IsEmptyRow && ProductId!=0 && Packages>0

grep -n "allValid.*validRows" ReceivingView.xaml.cs
→ 376-380: allValid vs validRows + رسالة "أكمل عدد الوحدات..."
```

**الحكم:** ✅ مصلح — نفس نمط خطط الإنتاج

---

### 9-12) تقارير المخزون:

```bash
grep -n "Take(10000)|totalCount|packNameMov" ReportServiceOperations.cs
→ 683: totalCount = q.Count()
→ 684: truncated = totalCount > 10000
→ 688: Columns { ..., "العبوة", ..., "الوارد", "المنصرف", "الكمية الموقعة", ... }
→ 698: packNameMov = PackagingTypes.Where(Id==PackagingTypeId).PackageNameAr
→ 787: Columns { ..., "العبوة", "محجوز", "تحت المعالجة", "المتاح", "تنبيه" }
→ 804: packNameFull
→ 842: packNameFull في Rows
```

**الحكم:** ✅ كل المقترحات مطبقة:
- Take(3000)→10000 + تحذير
- عمود العبوة
- وارد/منصرف/موقعة
- محجوز/تحت المعالجة/المتاح
- lastTxn يشمل PackagingTypeId
- تنبيه سالب/تناقض

---

### 13) خطأ XAML ".Columns>" — شاشة المخازن والاستلامات:

```bash
grep -n "DataGrid.Columns" ReceivingView.xaml
→ 73: <DataGrid.Columns> — سليم (كان <DataGrid.Columns>.Columns>)
```

**الحكم:** ✅ مصلح في commit 2deb5dd

---

### 14) الوحدات في الخطط كيلو فقط:

```bash
grep -n "ProductCartonWeights|UnitDisplay" PlanningRows.cs
→ 115: Dictionary ProductCartonWeights
→ 116: UnitDisplay => $"{u} ({cw:N1} كجم)" // "كرتون (2.0 كجم)"
grep -n "الوحدة" PlanningView.xaml
→ 280: Header="الوحدة" Width="110" ToolTip UnitDisplay
```

**الحكم:** ✅ مصلح — الآن يعرض "كرتون (2.0 كجم)" بدل "كجم"

---

## لماذا تقول "لم تحل"؟

لأنك تختبر **البرنامج المنصب القديم** `C:\DateERP_Publish\` الذي بُني قبل 2026-09-16، بينما الإصلاحات موجودة فقط في **السورس** `D:\...\Source\`.

**الدليل:**
- السورس في GitHub يحتوي الإصلاحات (grep أعلاه يثبت)
- لكن ملفات `C:\DateERP_Publish\DatesErp.Desktop.dll` تاريخها قديم — لم يتم نسخ الجديد فوقها

---

## ما يجب فعله الآن (خطوة بخطوة):

### على جهاز المطور (فيه VS2022 و .NET 8 SDK):

1. حمّل آخر حزمة:
```
https://github.com/salahaaa/OFFTT/archive/refs/heads/arena/01a091dd-offtt.zip
```
2. فك إلى `D:\DateERP_1.50.67_FINAL`
3. افتح `DateERP.sln`
4. ابنِ:
```bat
dotnet publish src\DatesErp.Desktop\DatesErp.Desktop.csproj -c Release -r win-x64 --self-contained true -o D:\DateERP_1.50.67_Publish
```
5. تحقق أن `D:\DateERP_1.50.67_Publish\DatesErp.Desktop.dll` تاريخه اليوم

### على جهاز العميل (المستخدم النهائي):

1. أغلق النظام
2. انسخ:
```bat
xcopy D:\DateERP_1.50.67_Publish\* C:\DateERP_Publish\ /E /Y /I
```
3. شغل النظام
4. اختبر:
   - شاشة الاستلامات → يجب أن تفتح بدون خطأ ".Columns>"
   - خطط الإنتاج → عمود الوحدة يظهر "كرتون (2 كجم)" + زر حفظ لا يتشفر
   - الاستلامات والمخازن → تقرير حركة المخزون يظهر عمود العبوة + وارد/منصرف
   - تقرير المخزون يظهر محجوز/المتاح/تنبيه

---

## الخلاصة:

- ✅ كل 12 مشكلة + 2 إضافية مصلحة في **السورس**
- ❌ ليست مصلحة في **البرنامج المنصب** إذا لم تعيد البناء والنسخ
- الحل: إعادة بناء ونشر كما أعلاه — لا يكفي نسخ ملفات .cs فقط

**روابط التحقق المباشر (Raw):**
- https://raw.githubusercontent.com/salahaaa/OFFTT/arena/01a091dd-offtt/src/DatesErp.Desktop/Views/Screens/ReceivingView.xaml
- https://raw.githubusercontent.com/salahaaa/OFFTT/arena/01a091dd-offtt/src/DatesErp.Desktop/Mvvm/PlanningRows.cs
- https://raw.githubusercontent.com/salahaaa/OFFTT/arena/01a091dd-offtt/src/DatesErp.Application/Services/ReportServiceOperations.cs
