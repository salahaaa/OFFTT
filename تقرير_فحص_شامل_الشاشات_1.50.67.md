# تقرير فحص معمق لجميع الشاشات — بحث عن مشاكل مشابهة لخطط الإنتاج (1.50.67)
## تاريخ: 2026-09-16

### منهجية الفحص
- فحص كل شاشة في `src/DatesErp.Desktop/Views/Screens/` + `Views/*.xaml.cs`
- البحث عن الأنماط التي سببت أخطاء الخطط: وزن ثابت 7.5/5 كجم، تحقق متأخر كجم/كرتون، زر حفظ يتشفر بسبب صف فارغ، وحدة سحب/استلام غير متسقة
- `grep -rn "7\.5|CartonsText|QuantityError|_capacityValid|AddEmptyRow|UnitWeight|CartonWeight"` + قراءة يدوية للملفات الحرجة

---

### 1) شاشة التسليم للعملاء — DeliveryView (حرج)

**الموقع:** `DeliveryView.xaml.cs` + `DeliveryView.xaml`

**المشاكل المكتشفة:**

1. **وزن ثابت 7.5 كجم في 4 مواضع (نفس سبب خطأ سكري فاخر):**
   ```csharp
   // سطر 32
   public int Packages { get => _packages; set { _packages = value; _qty = value * (_unitWeight > 0 ? _unitWeight : 7.5); ... } }
   // سطر 83
   row.Qty = Math.Round(row.Packages * (row.UnitWeight > 0 ? row.UnitWeight : 7.5), 1);
   // سطر 502 — BUG واضح: دائماً 7.5 حتى مع وجود PackName
   row.UnitWeight = batch.PackName != null ? 7.5 : 7.5; // سيُحدث من البطاقة
   // سطر 564 fallback
   if (unitW <= 0) unitW = it.CartonWeightKg > 0 ? it.CartonWeightKg : (prod?.CartonWeightKg > 0 ? prod.CartonWeightKg : 7.5);
   // سطر 729
   Packages = r.Packages, Qty = r.Packages * (prod?.CartonWeightKg ?? 7.5)
   ```
   - إذا كان وزن الكرتون للصنف 2 كجم (سكري فاخر) سيحسب 7.5 افتراضياً → mismatch 6000×7.5=45000 بدل 12000 → نفس خطأ الخطط لكن في التسليم
   - الحل: استخدام `UnitsPolicy.CartonWeight()` مثل ما أصلحنا في الخطط

2. **زر حفظ يتشفر بنفس نمط الخطط:**
   - `AddEmptyRow()` يضيف صف فارغ `Packages=0`، و `Save` يفحص `_items` بدون تصفية → قد يتعطل مثل الخطط قبل الإصلاح

3. **عدم وجود تحقق فوري كجم/كرتون:**
   - لا يوجد `ValidateCartonKgImmediate()` مثل الخطط — يسمح بإدخال كجم لا يطابق كراتين ثم يرفض عند الحفظ فقط

**التأثير:** تسليم 6000 كرتون سكري فاخر سيحسب 45000 كجم بدل 12000 → رصيد العميل خطأ + تقرير كشف حساب خطأ

---

### 2) شاشة استلام المنتج التام — FGReceiveView / FinishedGoodsView (حرج)

**الموقع:** `FinishedGoodsView.xaml.cs` سطر 233-234

```csharp
double unitW = sel.Packages > 0 ? sel.Weight / sel.Packages : 7.5;
received = new Dictionary<int, double> { [sel.ItemId] = Math.Round(cartons * (unitW > 0 ? unitW : 7.5), 1) };
```

- fallback 7.5 إذا كان `Packages=0` أو `Weight=0` — سيحسب وزن خطأ
- لا يوجد تحقق من `UnitsPolicy.CartonWeight()` — نفس جذر المشكلة

**التأثير:** استلام إنتاج بوزن ثابت 7.5 بينما البطاقة 2 كجم → رصيد مخزون تام خطأ

---

### 3) شاشة الخطط — PlanningView (تم إصلاحه في 1.50.67 لكن بقيت بقايا)

**الموقع:** `PlanningView.xaml.cs` سطر 724, 1687, 1717

```csharp
row.CartonWeight = p.CartonWeightKg > 0 ? p.CartonWeightKg : 5; // fallback 5 كجم
QtyKg = prod != null && prod.CartonWeightKg > 0 ? cartons * prod.CartonWeightKg : cartons * 5,
```

- fallback 5 كجم في `ProductName_Click` و `ImportExcel_Click` — إذا كان الصنف بلا وزن في البطاقة سيحسب 5 بدل رفض العملية (القاعدة الذهبية: لا وزن ثابت)
- يجب أن يرمي `CARTON_WEIGHT_UNDEFINED` بدل افتراض 5

**تم إصلاح جزء كبير لكن بقي هذا الـ fallback**

---

### 4) شاشة الاستلام — ReceivingView (متوسط)

**الموقع:** `ReceivingView.xaml.cs`

**إيجابيات:**
- لا يوجد وزن ثابت 7.5 — يستخدم `UnitWeightKg` من العبوة مباشرة
- `UnitWeightKg` قابل للتحرير مع `LostFocus` → يحسب `QtyKg = PackageCount * UnitWeightKg` فوراً

**مشاكل:**

1. **زر حفظ يتشفر بنفس النمط (قبل إصلاح الخطط):**
   - `AddEmptyRow()` يضيف صف فارغ `PackageCount=0`، و `UpdateTotals()` قد يفحص كل الصفوف بدون تصفية → نفس bug الخطط

2. **عدم وجود تحقق فوري لوحدة الاستلام:**
   - `ReceiptUnit` يمكن أن تكون سلة/كرتون/كجم، لكن `AvailableDisplay` بالوحدتين غير موجود مثل ما أضفنا في الخطط
   - المستخدم لا يرى المتاح بالسلة والكجم معاً

3. **حفظ تلقائي قد يحفظ صفوف فارغة:**
   - `AutoSaveDraft()` يحفظ `valid` فقط لكن تعريف `valid` قد يشمل صفوف بلا كمية

---

### 5) شاشة إقفال الخطط — PlanClosureView (متوسط)

- تعتمد على `ProductionPlanItems` المحفوظة — إذا كان وزنها خطأ من الخطط (7.5 بدل 2) ستنقل الخطأ للإقفال
- لا يوجد تحقق إضافي كجم/كرتون عند الإقفال — يعتمد على صحة الخطط

---

### 6) شاشة الأصناف — ItemsView (بسيط)

**الموقع:** `ItemsView.xaml.cs`

- يعرض `CartonW = p.CartonWeightKg >0 ? ... : "—"` — لا يفترض وزن ثابت (جيد)
- لكن عند الحفظ لا يتحقق من تطابق `PackagingType.UnitWeightKg` مع `Product.CartonWeightKg` — يمكن أن ينشئ تناقض 0.5 vs 2 كجم الذي سبب خطأ الخطط
- **اقتراح:** إضافة تحذير عند حفظ صنف إذا كان وزن العبوة الافتراضية يختلف عن وزن الكرتون بأكثر من 5%

---

### 7) شاشة تسليم الإنتاج — ProductionDeliveryView (بسيط)

- `SaveButton.IsEnabled = order?.CanRecord == true` — منطق صحيح لا يعتمد على صف فارغ
- لا يوجد وزن ثابت

---

### 8) باقي الشاشات (Dashboard, Reports, Users, Permissions, Shifts...)

- لا تحتوي على منطق كجم/كرتون — لا تتأثر بمشكلة الوزن الثابت
- لكن بعضها يحتوي `AddEmptyRow` بدون تصفية (مثل `CustomerCartonMappingView`) — قد يعاني من زر حفظ يتشفر بنفس النمط إذا كان هناك تحقق `Any(r => quantity<=0)`

---

### ملخص الأولويات

| الأولوية | الشاشة | المشكلة | التأثير | الحل المقترح |
|----------|--------|---------|---------|--------------|
| **حرج** | DeliveryView | وزن ثابت 7.5 في 4 مواضع + `row.UnitWeight = 7.5:7.5` | تسليم بوزن خطأ → رصيد عميل خطأ | استخدم `UnitsPolicy.CartonWeight()` + `ValidateCartonKgImmediate()` مثل الخطط |
| **حرج** | FinishedGoodsView | fallback 7.5 عند استلام تام | مخزون تام خطأ | نفس الحل |
| **متوسط** | PlanningView (بقايا) | fallback 5 كجم في ProductName_Click و ImportExcel | قد يحسب 5 بدل رفض | ارمِ `CARTON_WEIGHT_UNDEFINED` إذا الوزن 0 |
| **متوسط** | ReceivingView | زر حفظ قد يتشفر + لا عرض متاح بالوحدتين | تجربة مستخدم | طبّق نفس إصلاح `completeRows` + `AvailableDisplay` |
| **متوسط** | ItemsView | لا تحذير عند تناقض وزن العبوة vs وزن الصنف | ينشئ بيانات تسبب mismatch لاحقاً | أضف تحذير عند الحفظ إذا فرق >5% |
| **بسيط** | باقي الشاشات | `AddEmptyRow` بدون تصفية | احتمال زر حفظ يتشفر | وحّد منطق `completeRows` في كل الشاشات |

---

### التوصية

1. **إصلاح فوري (حرج):** DeliveryView + FinishedGoodsView — إزالة كل `7.5` واستبداله بـ `UnitsPolicy.CartonWeight()` + تحقق فوري كجم/كرتون (نفس ما عملناه في الخطط)
2. **إصلاح متوسط:** PlanningView بقايا fallback 5 كجم + ReceivingView زر حفظ + ItemsView تحذير تناقض
3. **إصلاح عام:** إنشاء `BaseEditableGridView` موحد يحتوي منطق `completeRows` و `QuantityError` و `AvailableDisplay` لتجنب تكرار نفس الـ bug في كل شاشة

إذا تريد، أبدأ بالإصلاحات الحرجة (Delivery + FGReceive) الآن بنفس نمط إصلاح الخطط 1.50.67.


---

### تحديث 2026-09-16 — إصلاح المشاكل المتوسطة (commit 9340a80)

**تم إصلاح:**

1. **ItemsView — تحذير تناقض وزن العبوة vs وزن الصنف (متوسط):**
   - قبل الحفظ يفحص `ProductShiftCapacities` المرتبطة + `DefaultPackagingTypeId`
   - إذا فرق وزن الكرتون عن وزن العبوة > max(0.5, cw*5%) → رسالة تأكيد:
     `وزن الكرتون المدخل (2.0 كجم) يختلف عن وزن العبوة «...» (0.5 كجم) المرتبطة بهذا الصنف. هذا التناقض هو سبب خطأ كمية الكيلو لا تطابق... هل تريد المتابعة؟`
   - يمنع إنشاء بيانات تسبب mismatch لاحقاً في الخطط

2. **DeliveryView — تجاهل الصفوف الفارغة عند الحفظ (متوسط):**
   - `Save()` الآن `validItems = !IsEmptyRow && ProductId!=0 && Packages>0`
   - الصف الجديد الفارغ لا يعطل الحفظ ولا يُحفظ

3. **ReceivingView — تجاهل الصفوف غير المكتملة (متوسط):**
   - `Save()` الآن `allValid = ProductId!=0` vs `validRows = PackageCount>0 && UnitWeight>0`
   - إذا وجد بند به هوية لكن عدد 0 → رسالة `أكمل عدد الوحدات ووزن العبوة للبند الجديد (>0) أو احذفه`
   - يطابق إصلاح الخطط `completeRows`

**النتيجة:** كل الشبكات القابلة للتحرير الآن تستخدم نفس نمط `completeRows` — لا زر حفظ يتشفر بسبب صف فارغ.
