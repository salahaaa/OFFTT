# فحص واجهة وأوامر الإنتاج — 1.50.74 DEV

التاريخ: 2026-09-19
المرجع المستقر: `stable-1.50.73`

## 1. قراءة الصورة المرفقة

الصورة تُظهر أن جدول «أمر إنتاج اليوم» فارغ، وأن الرسالة تقول:

> لا توجد بنود إنتاج معتمدة ومجدولة لليوم.

هذا لا يعني أن الخطط المستقبلية مفقودة. مسار الجدول الرئيسي مقيد بيوم العمل القادم من الخادم (`GetTodayProduction`). إذا كانت الخطة مجدولة ليوم لاحق فلن تظهر في هذا الجدول.

كما تظهر في الصورة أزرار إصدار اليوم باللون المعطل؛ وهذا صحيح عندما لا توجد بنود معتمدة لليوم الحالي. زر «إضافة أمر من الخطة» هو المسار الذي يجب أن يعرض الخطط المعتمدة المجدولة لليوم أو الأيام القادمة.

## 2. الأزرار المكررة

### التكرارات المؤكدة في المصدر

`OrdersView.xaml.cs` يبني شريط الأدوات العام:

- `AddFromPlan_Click`
- `Edit_Click`
- `Save_Click`
- `Search_Click`
- `Delete_Click`
- `Print_Click`
- `RefreshToday`
- `IssueToday_Click`
- خروج

وفي `OrdersView.xaml` توجد أزرار جسم الشاشة لنفس الأفعال:

- «إضافة أمر من الخطة» — تكرار `AddFromPlan_Click`
- «تعديل» — تكرار `Edit_Click`
- «حفظ» — تكرار `Save_Click`
- «بحث» — تكرار `Search_Click`
- «حذف المسودة» — تكرار `Delete_Click`
- «طباعة» — تكرار `Print_Click`
- «إصدار أوامر اليوم» — تكرار `IssueToday_Click`

الأزرار الخاصة بالجدول وليست تكراراً هي:

- «إصدار المحدد فقط»
- «تحديد الكل»

### القرار المقترح للواجهة

الإبقاء على الأفعال الكلاسيكية في شريط واحد فقط، والإبقاء داخل جسم الشاشة على أزرار التشغيل الخاصة بالجدول فقط:

- إصدار أوامر اليوم
- إصدار المحدد فقط
- تحديد الكل

والحذف من جسم الشاشة للأزرار المكررة: إضافة من الخطة، تعديل، حفظ، بحث، حذف، طباعة.

لم يتم حذفها في هذا الفحص، حتى لا يتغير شكل النسخة المستقرة أو يتم تسليم تحديث Windows غير مختبر.

## 3. هل تنتقل الخطة بنفس البنود والعملاء والأصناف؟

### النتيجة من القراءة الساكنة

نعم، مسار الإصدار الجديد ينقل مرجع الخطة ويمنع التغيير الصامت:

1. المجموعة تُبنى حسب:
   - رقم الخطة
   - التاريخ المجدول
   - العميل
   - الوردية
   - الخط
2. كل بند يُنقل بمعرف `PlanItemId`.
3. الخادم يتحقق من تطابق:
   - `ProductId`
   - `CustomerId`
   - `LotId`
   - `ShipmentId`
   - `PackagingTypeId`
   - عدد الكراتين
   - الكمية بالكيلو
4. عند إنشاء `ProductionOrderItem` يتم نسخ العميل والصنف والدفعة والشحنة والعبوة والكميات من بند الخطة، وليس من إدخال يدوي.
5. اختلاف العملاء ينتج أوامر منفصلة؛ لا يتم خلط عميلين في أمر واحد.
6. اختلاف الأصناف داخل مجموعة العميل يبقى في بنود منفصلة داخل نفس الأمر إذا كان التاريخ والوردية والخط متطابقة.
7. وجود أمر سابق لنفس `PlanItemId` يمنع إنشاء نسخة مكررة، باستثناء الأمر الملغى.
8. الخطة غير المعتمدة أو المغلقة لا تظهر ولا يمكن إصدار أمر منها.

### حارس إضافي

الاعتماد يسمح لأمر مجدول في المستقبل، لكن بدء التنفيذ الفعلي يبقى مقيداً بيوم العمل الفعلي للأمر.

## 4. مسارات العرض والإصدار

- `GetTodayProduction`: عرض بنود اليوم فقط، وهو المقصود بجدول التنفيذ اليومي.
- `GetPendingPlanGroups`: عرض مجموعات الخطط المعتمدة لليوم والأيام القادمة في نافذة «إضافة أمر من الخطط المعتمدة».
- `IssuePlanGroup`: إصدار مجموعة بتاريخها المجدول.
- `IssueOrdersFromPlan`: ترحيل مجموعات الخطة ضمن فترة اختيارية.
- `IssueTodayOrders`: إصدار كل بنود اليوم الحالي فقط.

تم تعيين وحدة صلاحيات الشاشة إلى `production` بدلاً من عنوان الشاشة العربي.

## 5. تصحيح مكتشف أثناء الفحص

كان ملخص `IssueOrdersFromPlan` يعيد `TotalKg = 0` رغم أن بنود الأمر نفسها تحمل الكمية الصحيحة. تم ربط الملخص الآن بإجمالي الكمية المخططة للمجموعة. هذا تصحيح للتقرير وليس تغييراً في كمية الأمر.

## 6. حالة الاختبار

- الفحص الساكن لمسار الإنتاج: PASS.
- فحص المطابقة والربط والهوية: PASS.
- فحص الواجهة الفعلي على Windows: لم يُنفذ في بيئة Arena الحالية.
- `dotnet build/test`: غير متاح هنا لأن .NET SDK غير مثبت.
- لذلك لا تُعد هذه النسخة تسليماً مستقراً حتى يتم بناؤها وتشغيلها على Windows.
